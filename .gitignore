# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.hypothesis/
htmlcov/
.coverage
dist/
build/

# Data & results (fetch scripts are versioned; payloads are not)
data/*
!data/fetch/
runs/
results/

# Frontend
node_modules/
frontend/dist/
embed/dist/

# Environment / OS
.env
.DS_Store

# SUMO scratch
*.sumocfg.tmp

# The embed data pack is versioned (the data/* rule above is for fetched payloads).
!embed/public/data/*

# Small OSM extracts the versioned scenarios depend on (ODbL attribution in data/osm/README.md).
!data/osm/
