# Changelog

All notable changes to FlowState are documented here. Every headline number
below traces to a committed, seeded artifact (CLAUDE.md §0.1/§0.5); nothing
is quoted that cannot be reproduced from the referenced runs.

## [Unreleased]

### 2026-09-24 — block 3

- The bounded hold on a weaving section's cooperating follower, derived, measured and left off (WP-58; `WEAVE_DEFAULTS["hold_release_s"]` = 0, hash-neutral; `microsim.runner._weave_hold_release`, `_weave_hold_state`, `_weave_choose_gap`'s `blocked`; `weave_sections[i].n_hold_releases`, the 37th key, also in `WeaveSectionDiagnosticsOut` and the sweep's `WEAVE_FIELDS`; docs/CONTRACTS.md §2, docs/WEAVE_MODEL_PLAN.md dated section). The follower of a changer's chosen gap is held at IDM towards the changer for as long as the changer keeps the gap; the rule drops the hold, blocks the follower for one bound and re-chooses the gap behind it once the changer's projected arrival at the gap (its deficit to the leader-side gap the acceptance asks, over the rate it closed it at during the last step) has not advanced for longer than the bound with the follower's side already open, never inside the follower's brake distance and never for a standing pair. The hold trace at the default (28 fixture runs, 17,810 changer–follower episodes) reads 5,248 episodes stalled for more than 2 s carrying 133,011 of the 206,358 held changer-steps, 53 % of the held steps an entrant still on the ramp (median 5.7 m/s) and 36 of the 40 longest stalls (22.5–43 s) such entrants holding a lane-1 follower at their speed; but every form of the release reads worse on the same 29-run grid — at 2 s (the `pair_release_s` figure) give-ups 44 → 74, exits 5,988 → 5,897, the entrances 5,944 → 5,830, T.H.52 at capacity, seed 5, standing at 0.4 m/s for two minutes (288 of 466); at 1 s and 4 s, on the section only, without the exit priority, as a 40 m distance bound and with the strict stall reading 59–80 given up; with the exiter's yield on 66 / 63 against 39; every form fails the T.H.52 no-lock pin at seed 4 or 5 — because a changer released at speed parity meets the next follower at the same speed and stalls again, and the entrants reach the section with a gap 79 % → 62 % of the time. The bound does not make the exiter's yield safe on the fixtures, so it is not a candidate for a corridor battery with the yield on. No collision, the default byte-identical (golden hash 436cd4ec9e5d, the T.H.52 rows 395 / 401 / 373 of 466 as before). Any rule of this series that ships on must first pass a 20-seed VM corridor battery.
- The entrant's entry speed on the weave's ramp, derived, measured and left off (WP-57; `WEAVE_DEFAULTS["entry_speed_bound"]` = 0, hash-neutral; `microsim.runner._weave_entry_speed_bound`, `_weave_entry_bound`; `weave_sections[i].n_entry_bounded`, the 36th key, also in `WeaveSectionDiagnosticsOut` and the sweep's `WEAVE_FIELDS`; docs/CONTRACTS.md §2, docs/WEAVE_MODEL_PLAN.md dated section). An entrant on the ramp within `lookahead_m` of the section is asked to enter no faster than the speed from which it can still halt at its own `b` one `minGap` short of the auxiliary lane's end, `v ≤ √(2·b·(D − s0))` — the first ramp-side brake since the sixth derivation, a one-step ceiling clipped at `−b`, on the ramp only, the throttle and the demand untouched. On the 29-run fixture grid it binds on 1,383 vehicle-steps on 187 entrants, all in the seven Ruth St corridor-fleet runs (the fleet defaults' `b` never falls under the curve; the T.H.52 rows and the golden are byte-identical, the entrance criterion 395 / 401 / 373 of 466 as before), takes the bound entrants from 21.5 to 18.0 m/s at the section start and the Ruth St entrants unable to stop within their lane from 40 to 27 of ≈ 590, and reads worse: give-ups 44 → 54 (crossing pairs 24 → 31 on 16 → 17 halted entrants), exits 5,988 → 5,995, the entrances 5,944 → 5,967, forced changes deferred 5,439 → 6,644, no lock, no collision. The one row it helps (Ruth St corridor fleet, exit peak, seed 3: 9 → 5 given up, lane 1 at the gore 2.8 → 6.1 m/s, 122 binding steps) is paid for on seeds 4 and 5 (1 → 12, 4 → 8; 86 / 64 steps), where the entrant slowed below lane 1's speed is held by the cooperation and the give-up chains form behind entrants that can stop within the lane at their `b` (v00945 at 19.9 m/s with `b` 2.19, five give-ups; v00960 at 19.0 with 1.35, six). The form that removes the fast arrivals almost entirely — the bound from the ramp's start, harness only: 40 → 5 unable to stop, 3 of 16 pair-heading entrants — leaves 27 crossing pairs on 16 entrants: the pair does not form by momentum. Half `b` binds 9,928 steps and moves 21 rows including the T.H.52 corridor demand and the golden; on top of the exiter's yield it reads 43 against the yield's 39. Any rule of this series that ships on must pass a 20-seed corridor battery on a VM before it stays on (VM M). `frontend/` untouched.
- **The cross-edge vacate window moves the four-hour I-94 corridor again**: the 20-seed battery departs 0.859 of its demand (0.743 with the 150 m window; every seed 0.84–0.89, no lock), speed RMSPE 0.706 (from 0.782), the first GEH passes (8 % of link-hours), given-up exits under 1 %; the 35-minute slice reads 0.968 (`docs/ONBOARDING_MNDOT.md` §11). Still not reproduced.
- **VM K: the speed-aware acceptance on the four-hour I-94 corridor** (`docs/ONBOARDING_MNDOT.md` §11, `artifacts/mndot_rounds/weave_2026-09-24/*speed_aware_20f9fcb*`): the 20-seed battery departs 0.855 (VM J 0.859), speed RMSPE 0.709 (0.706), GEH < 5 on 8 % of link-hours (unchanged), 15 collisions over 20 seeds (31), given-up exits 0.8 / 1.0 % (0.6 / 0.5 %); one seed falls to 0.743 with its upstream ramps starved (0.865 under VM J), not diagnosed; the 35-minute slice reads 0.968 as before with one collision. Still not reproduced.
- VM L: the 0.743 seed reproduces to the third decimal on a five-replicate re-run (`docs/ONBOARDING_MNDOT.md` §11); its standstill maps were not produced because the battery prunes every trajectory but the first seed's — the pipeline's `mndot_weave_seed5` stage now passes `--keep-trajectories`.
- **VM M: the crossing-pair rule on the four-hour I-94 corridor** (`docs/ONBOARDING_MNDOT.md` §11, `artifacts/mndot_rounds/weave_2026-09-24/*exiter_yields_585e588*`): the 20-seed battery departs 0.836 — 19 seeds 0.843–0.899 and one seed locked at 0.356 (0.852 without the rule; 18,259 exiter-yield vehicle-steps against 769–3,512 elsewhere) — RMSPE 0.716, GEH 8 %, 19 collisions, given-up exits 0.6 / 0.9 % (the series' lowest); VM K's 0.743 seed reads 0.865. The rule's default goes back to off until the yield is bounded; the lock's standstill map is the next round (`pipeline_i24.sh --diag-seed/--diag-reps`).
- VM N: the locked seed's standstill map (`docs/ONBOARDING_MNDOT.md` §11, `artifacts/mndot_rounds/weave_2026-09-24/locked_seed_exiter_yields_*`, thirteen replicates with trajectories kept): the seed reproduces (0.356; the other twelve 0.843–0.899), the first cells under 2 m/s appear at minute 18 at 10.20–10.35 km in lane 0 at the T.H.52 weave and never clear, five lanes stand by minute 65, and from minute 110 to the end every cell of every lane over the whole 10.7 km reads 0.0 m/s — the crossing-pair yield chain as a total gridlock. The healthy seeds' queues reach the upstream boundary in the last hour on every seed.
- **Where the I-94 corridor's queue comes from** (WP-59, analysis of committed artifacts only; `docs/ONBOARDING_MNDOT.md` §11, last subsection): the simulated corridor is capacity-short at the T.H.52 weave, not demand-high — in 06:30–07:30 it carries 3,259 veh/h at S790 against 4,911 observed (34 % short) and 3,121 at S97 against 4,586, while the injected demand is 1–4 % below the observed counts there and equals the free-flowing boundary count. The discharge is 1.95 lanes' worth of the fleet's straight-road capacity (1,670 veh/h/lane), and that capacity itself equals the observed per-lane discharge with no headroom. §11a's flow columns were half an hour off (the scorer's hours start at 06:30); superseded. The battery writes no simulated counts, so they were recovered by inverting GEH (method script committed).
- **VM O: per-lane crossing counts through the T.H.52 weave** (`docs/ONBOARDING_MNDOT.md` §11, `artifacts/mndot_rounds/weave_2026-09-24/first_hour_lane_crossings_20f9fcb_physics.txt`; the first hour, four seeds, trajectories kept): the weaving section saturates at about 4,000 veh/h over its four lanes (3,967–4,026 vehicles in the hour on the four seeds; about 1,000 per lane, 60 % of the fleet's straight-road capacity) where the real weave takes 5,137 veh/h in free flow at 05:40 and 6,275 at 06:25; it congests at its downstream end first (the auxiliary lane at 11 m/s in minutes 5–10, the crossing lane 1 carrying the least), before the standstill head forms upstream at minute 18. WP-59's lane-0 hypothesis is refuted (lane 0 before the gore carries 22 %). New pipeline stage `mndot_head60_lanes` and diagnostic `diag_lanes.py.txt`.
- Speed-aware weave acceptance (docs/CONTRACTS.md §2, docs/WEAVE_MODEL_PLAN.md dated section): the gap acceptance's leader side and both sides of the forced guard now carry the brake distance `Δv⁺²/(2·b)` of the party behind at its own `decel` (`microsim.runner._weave_brake_gap`, `_weave_lead_gap_min`, `_weave_force_gap_ok`), the movement's time gap kept as the floor. The two Ruth St collisions of the exit-side acceptance (corridor fleet, exit peak: seed 4 at the 500 m window, t = 600.5 s; seed 5 at a 271 m window, t = 717.5 s — a 23 m/s exiter dropping in 27 m behind a queue head at 1 m/s) are gone and pinned by `TestExitSideAcceptance`; a third of the same family on the guard's follower side (T.H.52 at the corridor's demand, seed 5, t = 977.5 s: a released exiter forced in front of a follower at 16 m/s with 10.7 m) surfaced under the leader-side form alone and is closed by the follower term. No collision on any of the 30 fixture runs (seeds 3–5) and no lock; the cost is later exit changes — Ruth St corridor fleet at the exit peak 9 / 1 / 4 of 290 / 280 / 274 given up (was 4 / 2 / 4), 40 / 25 / 6 forced (9 / 9 / 15), 1,168 / 402 / 132 deferred (252 / 111 / 275); the exit-peak strict marks stay at seeds 3 / 4 while seed 5 now passes every criterion, unmarked — and one exit given up per seed at the T.H.52 capacity (the no-lock test's `n_missed` pin 0 → ≤ 1; `test_th52_weave_at_corridor_demand_exit_side` gains a non-strict xfail at seed 3: lane 1's last 60 m 3.8 m/s in one minute). Measured and rejected: the IDM desired gap `s*` as the bound (locks every T.H.52 fixture), the IDM "−b" gap (slows lane 1; locks the short section on the follower side) and a give-up that waits for a held follower (locks everything). Golden `merge_weave` regenerated (hash unchanged): mean travel time 69.345 → 69.500 s, σ_v spatial 3.848 → 3.940 m/s, fuel 90.67 → 90.54 ml/veh-km, throughput / changes / exits unchanged. Fleet defaults on Ruth St and the moderate fixture unchanged.
- Bounded give-up patience for the weave's exit side, measured and shipped off (WP-52; `WEAVE_DEFAULTS["exit_giveup_patience_s"]`, default 0 = the previous behaviour, hash-neutral unless set; `microsim.runner._weave_giveup_patient`; `weave_sections[i].n_giveup_waited`, the 33rd key, also in the API schema and the sweep's `WEAVE_FIELDS`; docs/CONTRACTS.md §2, docs/WEAVE_MODEL_PLAN.md dated section). A halted exiter at the gore's end whose request is refused may wait while its auxiliary-lane follower, the same one as last step and behind it, is still braking towards the gap, no longer than the bound or the follower's own braking time at its `b`; every other state gives up at once, so it cannot lock. On the 30-run fixture grid at 10 s the give-ups read 48 against 44 (no collision, no lock), lane 1 at the gore slower on more runs than faster, the T.H.52 capacity entrance at seed 4 down to 358 of 466 (the no-lock pin's 80 %), and the golden is unchanged; the trace shows why — 34 of the 44 give-ups are a lane-0 vehicle overlapping the halted exiter, and the 6 followers braking towards the gap were caught by the cooperation's hold inside their brake distance at `b` (T.H.52 capacity, seed 3, t = 431 s: 41.7 m offered at 11.54 m/s, 48.4 m needed) and slid alongside. The bound alone (no deceleration condition) locks the Ruth St corridor fleet at the 271 m window. Negative result; the numbers in the plan's table.
- The crossing pair at the weave's lane ends, traced per pair and resolved from the exiter's side (WP-54; `WEAVE_DEFAULTS["exiter_yields"]` = 1, ships; `WEAVE_DEFAULTS["entrant_yields"]` = 0, measured and off; `microsim.runner._weave_yield_at_ends`; `weave_sections[i].n_exiter_yields` / `n_entrant_yields`, the 34th and 35th keys, also in `WeaveSectionDiagnosticsOut` and the sweep's `WEAVE_FIELDS`; docs/CONTRACTS.md §2, docs/WEAVE_MODEL_PLAN.md dated section). The per-pair trace of the 24 crossing-pair give-ups at 6e7757e shows the pair forming with both vehicles braking for their own lane ends abreast (or the entrant sliding past the exiter from behind at a speed its `b` cannot shed) and, decisive, that the entrant halted at the auxiliary lane's end is not freed by the exiter's give-up: it stays, refused into lane 1 by every lane-1 vehicle arriving inside its brake distance, and the next exiters halt beside it and are given up in turn (16 halted entrants account for the 24 give-ups, one for four). The rule that ships: an exit-bound changer inside its forced zone stops behind such an entrant (the exit priority's hold with the roles exchanged, feasible at its own `b` only) so the entrant changes ahead of it and the auxiliary lane moves again. On the 29-run fixture grid: give-ups 44 → 39, exits 5,988 → 6,015 of 6,131 → 6,142 reached, the entrances 5,944 → 5,973, pair releases 218 → 169, no lock, no collision, the T.H.52 rows and the golden unchanged (hash 436cd4ec9e5d); from the whole section instead of the zone it read worse (46). The entrant's own yield — the rule the package named — is feasible at its `b` in 2 of the 24 pairs (by 1.4 m); it binds on 21 vehicle-steps, five pairs in five runs (44 → 36 give-ups with one fewer exit; two of the five resolve as derived, the rest is the sequence moving), adds nothing on top of the exiter's yield (39 → 39, nine fewer exits) and locks the Ruth St module at the 271 m window when asked from the exiter's zone entry; shipped off. Both keys hash-neutral unless set; `frontend/` untouched.
- The forming half of the weave's crossing pair measured and left as it is (WP-55; `WEAVE_DEFAULTS["exiter_yields_halting"]` = 0, measured and off, hash-neutral; `microsim.runner._weave_halting_first`; no new `weave_sections` key, the contract stays at 35; docs/WEAVE_MODEL_PLAN.md dated section). The exiter's yield of WP-54 brought forward from "the entrant ahead is halted" to "the entrant ahead will halt at its lane end before the exiter reaches the gore" (committed at its own `b`, there first at the two speeds; the exiter stopping behind where the entrant's rear will rest) reads worse on the same 29-run fixture grid: give-ups 39 → 43, the entrances 5,973 → 5,958, 53 more binding vehicle-steps in five runs, on entrants committed only through a small drawn `b` that changed or halted regardless; without the commitment test 43 with 50 fewer exits, from the whole section 43, the time-order test never decides. The give-ups it was written for are honest: 12 of the 39 are an exiter that entered the zone at 10.8–16.6 m/s with a halted or halting entrant ahead and no stop at its `b` on any in-zone step (41–165 m needed against 60–69 m offered; closest margins 1.9, 2.8, 2.9 m); a just-in-time yield from outside the zone (harness only) reads 34 give-ups with 11 fewer exits and 30 fewer entrants, its whole gain from two T.H.52 rows where it bound on a single vehicle-step and re-rolled the run. Finding: the entrants that head a crossing pair enter the section at 16.6 m/s on average against 9.6 for all (10 of 12 at 15–21 m/s onto the 136 m Ruth St auxiliary lane; 5 of 12 cannot stop within it at their `b`, against 1 % of all). Golden `merge_weave` unchanged (hash 436cd4ec9e5d); `frontend/` untouched.
- The brake-scaled yield zone of the weave's exit side, measured and left off (WP-56; `WEAVE_DEFAULTS["exiter_yield_lead_s"]` = 0, hash-neutral; `microsim.runner._weave_yield_early`; no new `weave_sections` key, the contract stays at 35; docs/CONTRACTS.md §2, docs/WEAVE_MODEL_PLAN.md dated section). The exiter's yield of WP-54 asked outside the fixed 80 m zone from the exiter's own brake distance — within τ seconds of travel of the last stop feasible at its `b` behind an entrant halted at its lane end, bounded by a standstill lapse, no chain — binds on the same 29-run grid (the exiter's yield on) on 16 vehicle-steps at τ = 1 s, six exiters in four runs, where the cooperation already brakes the exiter at `−b` towards that entrant as the follower of its gap: give-ups 39 → 39, exits 6,015 → 6,019, the entrances equal; the 12 honest give-ups of WP-55 have no feasible stop on any section step, and the one mechanism row (the two-entrance corridor fleet, seed 4: v01298 stopping 2 m farther back) swings from the grid's best to its worst reading as τ goes 0.5 → 3 s. Without the lane-end condition the rule re-rolls the T.H.52 rows from single steps on exiters crawling at the section start (WP-55's J rows and WP-54's XY row to the number; 35–42 given up, 17–53 fewer entrants). **`WEAVE_DEFAULTS["exiter_yields"]` flipped 1 → 0** on corridor evidence: VM M, the 20-seed four-hour I-94 battery under the rule (runner 585e588, `artifacts/mndot_rounds/weave_2026-09-24/battery_corrected_inputs_exiter_yields_585e588.json`), locks seed 6904272788004776631 — 0.356 departed against 0.852 under VM K, 18,259 exiter-yield vehicle-steps against 770–3,500 on the other seeds, every on-ramp starved — while the other 19 read 0.843–0.899 (battery 0.836, RMSPE 0.716, GEH 0.077, 19 collisions against 15, given-up exits 0.6 / 0.9 %, the 0.743 seed at 0.865); a rule that locks a seed of the flagship corridor cannot ship on until it is bounded. The fixture grid with it off is WP-54's 6e7757e grid to the number; `test_defaults_and_hash` and `TestWeaveYieldsAtTheLaneEnds.test_defaults` pin the new defaults, the other yield tests set the key explicitly; golden `merge_weave` unchanged at the default (hash 436cd4ec9e5d, byte-identical); `frontend/` untouched.
- The abreast state of the weave's exit side, traced and measured, a bounded wait shipped off (WP-53; `WEAVE_DEFAULTS["exit_abreast_patience_s"]`, default 0 = the previous behaviour, hash-neutral unless set; `microsim.runner._weave_giveup_abreast`, `_weave_abreast_clear_m`; `n_giveup_waited` counts its waits with WP-52's, no key added; docs/CONTRACTS.md §2, docs/WEAVE_MODEL_PLAN.md dated section). A per-give-up trace of the 44 fixture give-ups at 6497b2d reads the vehicle beside the halted exiter: in 24 of the 34 overlap cases it is a *driven entrant* halted at the end of the auxiliary lane — the crossing pair at the lane ends, each owing the change into the other's lane, which no local rule resolves (the exiter's reroute is what frees both) — and in 10 an exit-bound vehicle sliding past the exiter's rear at 0.7–7 m/s that clears it within 1.7–8 s at its speed. The wait for the latter (a halted exiter is kept while the vehicle beside it, not an entrant, moving, would clear the acceptance's floor at its speed within the budget) rescues those exiters (10 → 1) and the give-ups still read 46 against 44 over the 29 runs, the waited exiter then meeting the next follower inside its brake distance, while the wait holds lane 1 (T.H.52 at capacity seed 4: 386 of 466 departed against 401, lane 1 at 2.7 m/s for three minutes); chained with WP-52's braking patience the give-ups are 44 with 16 fewer exits and the T.H.52 capacity entrance at seed 5 at 372 of 466, under the no-lock pin. Commanding the abreast vehicle to hold was derived inert and measured so (one vehicle-step over the grid). No collision, no lock, golden unchanged. Negative result; the tables in the plan.
- The Ruth St short-section module re-measured at the 500 m vacate window (docs/WEAVE_MODEL_PLAN.md, "the short section at the 500 m window"): against the 150 m window the study was written with, the corridor's fleet at the C-D split's exit peak gives up 4 / 2 / 4 of 290 / 280 / 274 exits at seeds 3 / 4 / 5 (was 8 / 3 / 4; every seed within the 2 % threshold now), 9 / 9 / 15 changes forced (was 33 / 15 / 17), 252 / 111 / 275 deferred (was 608 / 268 / 326), 135–145 through vehicles vacating the weave lane 430–500 m upstream (was 44–56); the entrance departs 73 of 73 and 128 of 128 at every window, fleet and seed, and the fleet defaults pass both demand points at all three seeds as before. What the exit-peak marks fail on now is lane 1's last 60 m (4.6 / 3.6 m/s and one empty minute — the weave lane carries 0.2 vehicles per sample there) and one collision at seed 4 (600.5 s, `102_0`): an exiter changes into the auxiliary lane 15 m into the section at 20 m/s onto a standing queue, the exit-side acceptance being a time gap at the changer's own speed without the leader's — handed to the vacate-window review with its trace. One variant measured: `vacate_ahead_m` capped at twice the section's length (271 m) is worse than either window at one seed per fleet (8 of 274 given up with a collision of the same trace on the corridor's fleet; lane 1 at 1.6 m/s with 716 deferred and 46 releases on the fleet defaults), so no section-length-dependent window is added and 500 m stays. `tests/test_microsim/test_microsim_weave_short_section.py`: the marks' reasons and the class docstring re-recorded with the 500 m numbers, outcomes unchanged (exit peak strict, entrance peak non-strict); no runner, fixture or golden change.
- Review of the cross-edge vacate window (cf2e4f6; `microsim.runner._weave_vacate_lanes`, `_weave_vacate_step`). The walk on the committed I-94 extract is pinned (`tests/test_microsim/test_microsim_vacate_window_review.py`): T.H.52's 500 m window is 40648738 (0, 1) / 40648738-AddedOnRampEdge (1, 2) / 40648793#1 (1, 2) / 40648793#0 (0, 1) — section lane 1's feeder shifts to lane 1 across the 40648744 acceleration lane and back — and Ruth St's lies within 638519829 (0, 1); ramp-split pieces are in the chain and the offsets. Two defects fixed: a vehicle bound for an off-ramp leaving from a window edge was "through" and asked left, away from its exit (`_weave_vacate_exempt_ids`, the state's `vacate_exempt_ids`: the paired exit's vehicles and those exiting from a window edge; no such exit lies in either I-94 window at 500 m, nor in `weave_two.osm`'s, so runs there are unchanged), and a vehicle whose change and crossing onto the section fell in one step was counted refused (`n_vacated` now reads the section's lane 2 through `lanes[2]`; the commands sent are the same). The re-issue on an edge crossing does not add to `n_vacate_requests` (one per vehicle in the default form). Golden `merge_weave` unchanged.
- The API's weaving-section diagnostics brought in line with `microsim.runner._weave_meta` (`WeaveSectionDiagnosticsOut`, docs/CONTRACTS.md `MetricsOut.merge_diagnostics` paragraph and the §2 `weave_sections` key list, the latter re-enumerated from the code — 32 keys — after lagging since the third derivation): the schema gains the optional `short_section` (`bool | null`: the section is shorter than twice `force_within_m`, flagged not scaled) and `vacate_window_edges` (`list[str] | null`: the corridor edges the cross-edge vacate window reaches, nearest first; `[]` = inert), null for a meta written before them; `n_vacate_skipped_no_gap` / `n_vacate_requests` were already carried by the schema and aggregated by `scripts/corridor_sweep.py`'s `WEAVE_FIELDS`, which stays as it is — the two new keys are facts of the section, not counters, and the sweep test's meta now carries them to show they never enter the aggregate. Additive; no frontend change. The API test gains the two keys in its fixture and its older-meta cases, plus one case for a short section with an inert window (`[]` reads as a value, not null).
- The vacate window crosses edges (`microsim.runner._weave_vacate_lanes`, `_weave_vacate_step`; docs/CONTRACTS.md §2 dated block, docs/WEAVE_MODEL_PLAN.md dated section): `vacate_ahead_m` is measured along the corridor chain from the section start across as many upstream edges as it reaches, the lane feeding section lane 1 derived per edge from netconvert's connections (its index shifts beside an added or dropped lane), an open request re-issued where the target index changes, and `weave_sections[i]` gains `vacate_window_edges`. The default rises 150 → **500 m** — a stated choice between the HCM weaving influence area (500 ft) and the MUTCD's nearest advance guide sign (1/2 mile), not a fit; hash-neutral (defaults are omitted from the hash, no committed scenario sets the key), and at 150 m the runner reproduces the previous numbers exactly. Measured (48-run grid, 150 / 300 / 500 / 800 m, both fleets, seeds 3–5): on the two-entrance fixture at 500 m the section side passes at seed 3 on both fleets (gore ≥ 7.4 / 8.0 m/s, 300 of 308 / 307 of 316 exit) and lane 1 of the 230 m before the gore is above 5 m/s in 10 of 18 minutes (was 2 / 4), but E1 departs 205 / 272 of 360 with its acceleration lane's end at 1.0–1.2 m/s — the head is now the entrance's merge and the approach, so both two-entrance tests keep their strict `xfail`; 800 m is worse everywhere (one collision at seed 5 on each fixture), 300 m suits the T.H.52-only fixtures better (entrance 423 / 394 / 429 of 470 against 398 / 394 / 395) and the two-entrance one equally. T.H.52 tests: the corridor-demand exit-side test passes, the capacity test keeps its strict marker (398 of 466), the no-lock pin holds at seed 5 and at seed 4 now fails on macOS too on the 80 % entrance pin alone (365 of 466, no lock; its non-strict marker records it); `weave_two.osm`'s test no longer pins `n_changed_in > 0` per section. Golden `merge_weave` regenerated (hash unchanged; throughput 1,687.5 → 1,680.0 veh/h, changes 17 / 20 / 1 → 14 / 19 / 2, mean travel time 70.82 → 69.35 s, fuel 93.03 → 90.67 ml/veh-km).
- The vacate rule re-derived (`microsim.runner._weave_vacate_step`, `_weave_vacate_gap_ok`, `_weave_vacate_bound_veh_h`; docs/CONTRACTS.md §2 dated block, docs/WEAVE_MODEL_PLAN.md dated section). Two `WEAVE_DEFAULTS` keys, hash-neutral unless set: `vacate_no_follower_braking` (default 0) selects a form that asks a through vehicle to leave the weave lane only on a step when the target-lane gap accepts it without the follower braking (the weave's time gaps plus the follower's IDM desired gap at its speed and approach rate) under mode 768 — no speed adaptation, one request per accepting step; `vacate_max_veh_h` (default 0 = the target lane's spare capacity, 2,050 veh/h less the flow it carried into the window over the last 60 s) bounds the vehicles asked per minute in both forms. `weave_sections[i]` gains `n_vacate_skipped_no_gap` and `n_vacate_requests` (API schema, sweep `WEAVE_FIELDS`, no frontend change). The two-entrance fixture also runs the corridor's own fleet block (`corridor_fleet_block`, `test_th52_with_upstream_entrance_on_the_corridor_fleet`, strict `xfail`): the middle lane's crawl comes five minutes later and in the same place (E1 247 of 360, lane 1 first at minute 7), and the third derivation's runner locks the T.H.52 gore's end at seed 5 on that fleet. **Measured and not made the default**: the gap-conditioned form never brakes a target-lane follower but at the corridor's demand the candidates reach the window at 2–6 m/s against a 10–25 m/s lane, 3 of 38 requests execute (default fleet, seed 3), vacating collapses (3–34 against 159–201) and the section's end locks (128–245 pair releases, E2 305–343 of 470, one collision at seed 5); a fixed bound of 300–500 veh/h leaves the crawl at 2.2–3.5 m/s. The target (lane 1 over the 230 m above 5 m/s every minute, no lock, both entrances ≥ 0.9) is met by no form; both two-entrance tests stay strict `xfail`. The spare-capacity default binds in bursts (0–19 skipped per 20 min): the corridor-demand exit-side test passes with the entrance at 386 / 398 / 384 of 470, the capacity test keeps its strict marker (406 / 386 / 393 of 466), the no-lock pins hold, the moderate fixture is byte-identical. Golden `merge_weave` regenerated (throughput 1,687.5 veh/h, changes 17/20/1, exits 33 and hash 436cd4ec9e5d unchanged; mean travel time 70.998 → 70.816 s, fuel 93.00 → 93.03 ml/veh-km — the bound delaying one ask by a step; lifting it reproduces the old totals exactly). Eight harness tests (`TestWeaveVacateGapConditioned`); API and sweep tests extended.
- Short weaving sections (docs/CONTRACTS.md §2 "short sections", docs/WEAVE_MODEL_PLAN.md dated section): `tests/fixtures/weave_ruth.osm` is the Ruth St twin (entrance 745524613 → C-D split 18208090, a 136 m auxiliary lane — netconvert measures 135.7 m — on three through lanes with ≈ 600 m of approach and downstream) and `tests/test_microsim/test_microsim_weave_short_section.py` runs it 20 min at the corridor's Ruth St flows read from `scenarios/mndot_i94_wb_stpaul_weave.yaml` (the entrance's peak step 3900 s: 4,049 veh/h arriving, 385 veh/h entering, 2.95 % exiting; the C-D split's exit peak 9000 s: 2,824 / 220 / 28.9 %) against the exit-side criteria plus ≤ 2 % given up and the entrance departing ≥ 90 %. With the fleet defaults every seed of 3–5 passes at both demand points; with the corridor's own fleet (`CORRIDOR_FLEET`: EIDM, the I-24 capacity-scaled population, `lc_strategic` 5) the exit peak reproduces the corridor's give-ups at every seed — 8 / 3 / 4 of 290 / 281 / 274 exits (2.8 / 1.1 / 1.5 %), lane 1's last 60 m at 3.3 / 4.1 / 4.3 m/s, 33 / 15 / 17 forced, 608 / 268 / 326 deferred, no collision — and is a strict expected failure; the entrance peak at seed 4 (2 of 41 given up) a non-strict one. The per-step trace names the mechanism: every exiter enters lane 1 at ≈ 17 m/s, 69 of 116 change within the 56 m before the 80 m zone, the rest are due 4 s after entering it with 26–40 m and ≈ 3 s left and halt at the gore's end beside an entrant halted at the end of the auxiliary lane — an abreast crossing pair, every due step refused on an overlap (one entrant stood 30 s while three exiters were given up beside it). Nine scalings of the forced zone, its delay and the exit priority's moment were measured (tabulated in the plan): the only one that cuts the give-ups (the whole section as the zone, a one-step delay: 0–3 of ≈ 285 over thirteen seeds) collides at three seeds — forced changes at speed in the auxiliary lane's first 30 m — and the rest are no better than the baseline, so **no scaling ships**: `microsim.runner._weave_short_section_rule` returns the fixed `force_within_m` / `force_after_s` for every section and flags `short` (`length_m < 2 · force_within_m`, 160 m at the defaults), carried as `weave_sections[i]["short_section"]` in `meta.json` (additive). Golden `merge_weave` (178.4 m, not short) reproduced and every T.H.52 test keeps its outcome with these hunks on 01dd5ec.
- The corridor's last 850 m as one fixture (`tests/fixtures/weave_th52_upstream.osm`: the 40648744 entrance on a 102 m acceleration lane, 230 m of three lanes, then the T.H.52 weave of `weave_th52.osm`; `TH52_UPSTREAM_DEMAND` from the scenario's 6000 s step — 3,793 veh/h arriving, 1,080 entering by conservation, 1,412 entering the weave, 21.2 % exiting; docs/WEAVE_MODEL_PLAN.md dated section). `test_th52_with_upstream_entrance_at_corridor_demand` (the exit-side criteria plus the entrance's departed share and lane 1 over its lane end; seed 3, 20 min) is a strict expected failure: the entrance departs 225 of 360, lane 1 over its lane end 2.0–5.2 m/s, the section's last 60 m 3.8 m/s in one minute. The trace, per rule, on the same fixture: the head is the middle lane of the 230 m before the gore at 1.3–1.4 m/s, formed identically with the entrance removed (its flow on the mainline) and with it on the scripted merge (three to four minutes later); it is the vacate rule's ≈ 260 requests per 20 min into that lane (125 of them E1's vehicles, who do not stall in lane 0), and with the rule off the head returns to the section's end with fewer vehicles delivered; the entrance's own merge is 14 % short of its demand but its shortfall stays at the merge. The runner is unchanged.
- VM H read per seed (docs/ONBOARDING_MNDOT.md §11a; scripts as session records `artifacts/mndot_rounds/weave_2026-09-24/vmh_per_seed.py.txt`, `vmh_flows.py.txt`; from the per-replicate `meta.json`/`metrics.json`/`observed_scores.json` only, no trajectories): the 19 running seeds are one queue whose head sits in the corridor's last 850 m (the 40648744 entrance at 9.88 km into the T.H.52 weave) from before the first scored window — S791/S790 at 5 m/s and S97 at 19 m/s at 06:00, the S790→S97 cross-sections carrying 2,600–3,300 veh/h against 4,100–4,600 observed in every hour — with the front reaching the entry at 07:10 at 7–9 km/h; the mainline's 45 % never inserted is 70 % of the shortfall, and the battery's eight starved entrances are seven from the locking seed plus Hudson Rd 18207436 (0.48; its scripted merge waits 640 s mean, 1,340 s p90, 66 % forced, at the front's arrival). The locking seed 5690692725577505498 is a structurally different state, not a late event: its S97/S790/S791 cells read 0.0 m/s in the first scored window (the T.H.52 exit end, where VM G locked), an hour before its three collisions at the McKnight Rd merge (minutes 55–59, where seven good seeds also collide); Ruth St's 152,153 deferrals are 42 entrants standing in a stopped section. Two further seeds (0.704, 0.696) semi-lock at Ruth St on their own (200/191 entrants against 764, S1947/S1069 starved to 15 m/s). The C-D re-entry's inferred 442 veh/h delivers 1.00 everywhere and is not the lever; S97 matches observed in all 19, so neither is the boundary. Named next lever: the discharge of the 40648744 → T.H.52 stretch, with the discriminating measurement (a 50 m × 1 min standstill map of one good seed's first 30 minutes on the VM) before any change; open beside it, the S1063 cell's recovered 06–07 flow (2,100 veh/h against a 2,990 veh/h demand while free-flowing).
- The dashboard shows the corridor battery's demand-integrity verdicts on a finished run: `GET /api/v1/runs/{id}/metrics` gains the additive fields `insertion` (`InsertionOut`) and `weave_exits` (`WeaveExitsOut`), the battery artifact's blocks of the same names pooled over the run's replicates by `validation.battery.aggregate_insertion` / `weave_exit_summary` (`api.main._insertion_and_weave_exits`; null when no replicate records the counters or lists a weaving section, NaN shares null), and the run detail's new "Demand integrity" panel (`frontend/src/components/InsertionPanel.tsx`, above the merge diagnostics) shows the insertion verdict and the weave-exits verdict as badges beside each other with a per-section table (ramp → exit, reached, given up n and share, a red "above 2 %" badge on a flagged section). docs/CONTRACTS.md API section; `tests/test_api/test_runs_insertion.py` (4), `frontend/src/test/insertion.test.tsx` (6).
- **The exit-side weave rule moves the four-hour I-94 corridor**: on the corrected scenarios the 20-seed battery departs 0.743 of its demand (0.247 the round before; 19 seeds at 0.70–0.80, one seed locks at 0.23) with speed RMSPE 0.782 (from 0.963), given-up exits under 1 % per section, no GEH pass yet; the 35-minute slice departs 0.979. Still not reproduced ( §10).
- The corridor battery surfaces the weave step's given-up exits (`validation.battery.weave_exit_summary`, `degraded_verdict`, `MISSED_EXIT_SHARE_THRESHOLD`; `scripts/corridor_battery.py`; `validation.report._weave_exit_notes`; docs/CONTRACTS.md "Corridor battery artifact"). The exit-side review (bullet below) had recorded that an exit-bound vehicle rerouted through at the gore's end leaves the exit's link flow short by `n_missed_exit` and every mainline link downstream carrying it, which the GEH rows cannot tell from a demand error, and that nothing reading a battery saw the counter. Now, additively (schema version unchanged): the artifact's new optional `weave_exits` block pools per section over the replicates' `meta.json` (`missed_exit = {n: Σ n_missed_exit, share: n / Σ n_reached_section_exiting}` with `reached`, `n_runs`, `exit`, `flagged`; null when the corridor has no weaving section; a meta written before the counter contributes nothing), the console prints one `weave exits` line per section beside the `insertion` line, whose verdict is degraded to `exits given up: k % at <ramp>` above 2 % of the section's reached exiters, and the report renders one "Weave exits at <ramp>" sentence per section below its insertion line. `--criteria-only` re-reads the stored metas. Threshold derivation (on the constant): GEH of an hourly flow `c` short by the share `s` is `s √c √(2/(2−s))`; the full GEH-5 tolerance at ~1,000 veh/h is `s ≈ 15 %`, the whole criterion spent on the artefact, so the threshold is the share at which the reroute alone moves a 1,000 veh/h exit link-hour's GEH by ≈ 0.64 (an eighth of GEH 5) and a 4,900 veh/h downstream link-hour's by ≈ 0.29. Fifteen tests (unit summary and verdict, a two-replicate fake run tree through `--criteria-only` with a clean section and one giving up 30 of 400, the report lines).
- Review of the exit-side weave rule (ecf6d79; `microsim.runner._weave_step`, docs/CONTRACTS.md §2, docs/WEAVE_MODEL_PLAN.md rule (2)). One defect fixed: the give-up at the gore's end was read before the acceptance, so an exit-bound vehicle halted within `exit_giveup_m` on the very step both auxiliary-lane gaps cleared — the state the priority hold produces, its follower at rest `s0_F + s0_c` behind — was rerouted through instead of changing (fake harness: halted 3 m from the end with the gaps that are accepted at once 8 m from the end). The acceptance and the forced guard are now read first and the give-up applies only when neither can be requested that step. Golden `merge_weave` unchanged (no give-up fires there). Recorded, not changed: the reroute is to the corridor's last edge (the paired exit dropped; the exit and downstream link flows are wrong by `n_missed_exit`, which only `weave_sections` records — the battery artifact does not surface it); the pair release fires on a priority hold whose rest gap `s0_F + s0_c` is at or below one vehicle length (5.0 m in the harness; about 4 m with the fleet's `s0` ≈ 2 m), dropping the hold after `pair_release_s`; the priority is due `force_after_s` after entering the zone whatever the speed (a 1 m/s crawler holds the auxiliary lane from 76 m out); `build_corridor_plan` draws an on-ramp's vehicles for an off-ramp at the same attach edge, so 109 of 470 entrants of `TH52_CORRIDOR_DEMAND` (96 of 466 at the fixture's demand) are ramp-to-ramp, never weave, and count in `n_exited` / `n_reached_section_exiting`; the documented 4,919 veh/h arriving is the propagation with the exit applied before the on-ramp at a shared edge (the plan's order gives 4,873). Five harness tests added (`TestWeaveExitPriority`).
- Weave, the exit side (`microsim.runner._weave_step` / `_weave_choose_gap`; docs/CONTRACTS.md §2, docs/WEAVE_MODEL_PLAN.md dated section). The I-94 WB slice's first standstill (minute 11, cloud VM, session record) is at the **end** of the T.H.52 section — exited 88 of 133 reached, forced deferred 8,971 vehicle-steps, 21 unfinished, 88 pair releases — where the fixture work had optimised the entrance. The corridor's flows at that weave (4,919 veh/h arriving, 21.2 % exiting, 1,412 veh/h entering; the scenario's series at its 600–900 s step) are now a fixture parametrisation, `TH52_CORRIDOR_DEMAND`, and `test_th52_weave_at_corridor_demand_exit_side` judges the exit side (≥ 90 % of reached exiters exit, lane 1 over the last 60 m above 5 m/s every minute, ≤ 10 % unfinished, no collision); at 3f47446 it fails on lane 1 (4.0 / 3.7 m/s in minutes 4–5, a standstill at the gore's end). Trace: an exiter at the lane end has its front ahead of every auxiliary-lane front, the abreast rule discards every gap, nobody is held and lane 0 streams past overlapping it; a follower finally held stops at its minGap, which the guard's `s0` floor never passes. Two rules added: an exit-bound vehicle whose forced change is due has **priority** (the gap behind the vehicle beside it stays its, the follower holds one changer minGap farther back so the guard passes at rest, the commitment is kept while the follower is behind its rear), and one **halted within `exit_giveup_m`** (new `WEAVE_DEFAULTS` key, 5 m, hash-neutral unless set) of the gore's end is rerouted through (`changeTarget`), handed back and counted in the new `n_missed_exit` (a subset of `n_missed`; also `WeaveSectionDiagnosticsOut` and the sweep's `WEAVE_FIELDS`). Fixture at the corridor's demand, seeds 3–5: the last 60 m never at or below 5 m/s, no stopped sample in the last 20 m, exit 300/310, 307/315, 317/325, no give-up, no collision; at the fixture's demand the entrance reads 419 / 414 / 419 of 466 (sixth derivation 411 / 412 / 420), so the strict `xfail` stays (419 against 419.4, two minutes at 5.0 m/s). Rejected: priority from zone entry (407 / 416 / 402), the give-up on distance alone (took the moderate fixture's v00010 at 2.9 m/s with 4.7 m left, which forced in at 3f47446). Golden `merge_weave` regenerated (mean travel time 70.83 → 71.00 s, fuel 93.02 → 93.00 ml/veh-km, counts and hash 436cd4ec9e5d unchanged). Six harness tests (`TestWeaveExitPriority`), API and sweep tests extended; no frontend change.
- Re-onboarding over a scenario that had been through the demand step now warns that the calibrated demand (inflow steps, ramp flows, boundary schedule) was rebuilt as the placeholder and names the demand step to run again.
- Review of the re-onboarding rules (`scripts/onboard_corridor.py`; docs/CONTRACTS.md "Re-onboarding keeps the fleet block"). One gap closed: a file at `--out` that parses as a ring or straight-corridor scenario was kept silently, its fleet, `sim` block, seed and replicates carried onto the rebuilt freeway corridor; it is now refused with exit 2 and a message naming the kind, like a file that does not parse, before the builder is called (`--fresh-fleet` overrides). Verified and pinned: a fresh onboarding of `tests/fixtures/splits.osm` is byte-identical to the pre-change CLI's output with the same `config_hash` (`7db7a266e5bf`, run on a `ccc2a90~1` worktree); `POST /api/v1/corridors` hands the build no `defaults` and installs `corridor_10km`'s fleet with `fd_calibration` / `macro` null; `--duration-s` / `--seed` / `--replicates` follow flag → kept file → default (1800 s / 42 / the builder's 20), `--fresh-fleet` drops `fd_calibration` and `macro` as the help says; `fleet_non_defaults` omits exactly what the config hash omits (an int `1` for a float `1.0` default, `idm_calibration: None`) and equals the hash payload's `fleet`; `fleet_settings` is additive to `flowstate.demand/1` and `DemandArtifact.from_dict` reads a payload carrying it; the committed I-94 scenario prints exactly the report line the contract gives. Tests in `tests/test_scripts/test_onboard_corridor.py` (`TestReonboardingReview`), `tests/test_flowstate_core/test_config_hash.py`, `tests/test_calibration/test_calibration_onboarding.py`, `tests/test_api/test_corridors.py`.
- Weaving sections and scripted merges are stepped upstream-first, by the section's start offset along the corridor chain, whatever the ramp list's order (`microsim.runner.run_micro`, the `weave_states` / `scripted_states` setup; docs/CONTRACTS.md §2 dated sentence). The open item of the derivations 3–6 review: the vacate guard covers a downstream section reading a vehicle the upstream one already drives, but with the downstream pair listed first the upstream section's exit-bound vehicle found the downstream section's vacate hold (512) on it first, captured it as its original mode and restored it at hand-back after the real one — the fake-TraCI harness pins the vehicle left at 512 under that stepping order and at its real mode under position order. New fixture `tests/fixtures/weave_two.osm` (two T.H.52-shaped sections on a three-lane mainline, 308 m each, 560 m apart): a mainline-only scenario listing the downstream pair first writes byte-identical trajectories and identical `weave_sections` entries (matched by ramp name) to the upstream-first listing, both with every driven vehicle handed back; with entrants on both ramps (mainline 0.4 veh/s, entrances 0.10 veh/s) `weave_sections` lists the upstream section first although the ramp list does not, and the per-section counters at four demand levels and both listings are identical to the runner's before the change (the sections are too far apart for the order to matter; at 0.5 / 0.12 and seed 3 section A locks at its gore on both — the known one-sided weave lock). Listing the on-ramps the other way round with entrants cannot be byte-identical: the fleet plan draws each on-ramp's departures in config order (`microsim.vehicles.build_corridor_plan`, unchanged). Golden `merge_weave` (one section) unchanged.
- CI: the T.H.52 no-lock test at seed 4 locks on the Linux runner (entrance 331 of 466, a lane-1 minute at or below 2 m/s) while it passes on macOS (412); the case is a non-strict expected failure with the finding recorded in `docs/WEAVE_MODEL_PLAN.md` — the fixture tables are macOS records, the corridor rounds run on Linux.
- Review of the weave step's third to sixth derivations (`microsim.runner._weave_vacate_step`, `_weave_pair_release`, `_weave_easing_ok`, `_weave_cooperate`, `_weave_meta`; docs/CONTRACTS.md §2 dated sentence). One defect confirmed in the fake-TraCI harness and fixed: the vacate rule captured as a vehicle's "original" `laneChangeMode` whatever mode it read, so a vehicle already under a scripted hold on the edge before the section — the exit-bound vehicle of a weave whose last edge is that edge, driven at 512 / 256 — had the other section's one-step mode 256 restored onto it after the real restore, and the vacate request itself overrode the forced request in the same step; a vehicle already at 512 / 256 is now not asked while that hold lasts (and not marked seen). No fixture or scenario has the geometry (on I-94 the edge before the T.H.52 weave is the scripted merge 178547099's, whose driven lane 0 dead-ends and is not a vacate candidate; the scripted step hands a merged vehicle back before the weave step asks it — pinned in the harness). Refuted with tests: a vehicle exiting at a later ramp is through for this section and a vehicle already in the lane feeding section lane 2 is never asked; the pair release compares positions on the section axis across edges (a ramp follower at a negative offset), keys on the creep speed as the contract says, keeps `since` while the pair stands and resets it when it breaks up; the easing bound refuses `remaining_m ≤ 0` and a hair of section without a division error; a dual-role vehicle (an entrant's follower and a changer eased towards that entrant) gets the lower target whichever role is processed first; every `_weave_meta` counter is zero and every mean `None` without participants. `TestDeterminism.test_th52_weave_at_capacity_is_byte_identical` runs the T.H.52 fixture twice at seed 5 (vacates, releases, easings and forced changes all fire) and compares trajectories and the `weave_sections` entry byte for byte. Golden `merge_weave` unchanged (reproduced after the fix; the vacate edge of `weave.osm` carries no held vehicle).
- Re-onboarding keeps the fleet block (`scripts/onboard_corridor.py`, `microsim.scenarios.corridor_from_bbox(defaults=)` / `scenario_from_osm(defaults=)`; docs/CONTRACTS.md "Re-onboarding keeps the fleet block", docs/ONBOARDING_MNDOT.md §3). The §11 reset had one cause: `--out` over an existing scenario rewrote the whole file from the builder, so the fleet fell back to the `FleetSpec` defaults. Now an existing `--out` that parses as a `ScenarioConfig` keeps its `fleet`, `sim`, `seed`, `replicates`, `fd_calibration` and `macro` blocks byte-for-byte and only the network is rebuilt (`--duration-s`, `--seed`, `--replicates` given explicitly still win; a fresh onboarding is unchanged); the report prints `fleet block kept from <path> (model EIDM, …, lc_strategic 5.0, lc_strategic_ramp 1.0, lc_keep_right 0.0)` — the fields that differ from the defaults — or `fleet block: builder defaults`; new flag `--fresh-fleet` asks for the builder's defaults; a file at `--out` that does not parse is refused with exit 2 and the reason before anything is built (`--fresh-fleet` overrides). `POST /api/v1/corridors` already refuses an existing name (409) and is unchanged. The demand record now states the fleet block the scenario carries: `calibrate_scenario` (so `scripts/corridor_demand.py` and the API job) adds the additive top-level key `fleet_settings` (`model`, `heterogeneity_frac`, `lc_strategic`, `lc_strategic_ramp`, `lc_keep_right`, `idm_calibration`; `flowstate_core.config.fleet_settings`) to the `flowstate.demand/1` payload and a `fleet: …` line to the summary, so a reset shows up in the artifact. Pinned on `tests/fixtures/splits.osm` (netconvert only) in `tests/test_scripts/test_onboard_corridor.py` and in `tests/test_calibration/test_calibration_onboarding.py`. The committed artifacts are unchanged (the key appears on the next `corridor_demand.py` run).
- Correction caught by the block-3 traceability audit: the §11 regeneration of the I-94 scenario had reset the fleet's lane-change settings to the builder's defaults (`lc_strategic` 1.0, `lc_keep_right` 1.0); the corridor's deliberate 5.0 / 1.0 / 0.0 are restored in the three scenarios (new hashes); the 20-seed battery of VM F ran on the reset values and is recorded as such.
- Weave, seventh derivation — the abreast pair resolved symmetrically: **measured and rejected, the runner unchanged** (docs/CONTRACTS.md §2, docs/WEAVE_MODEL_PLAN.md dated paragraph). The sixth derivation's remaining bound — an entrant and a lane-1 vehicle side by side at speed parity, one braking at −b while the other holds — was given a symmetric resolution (each side half the offset the pair needs over a horizon `pair_tau_s`, default 3 s = 2 × the IDM's T; the rear one at ≤ b/2; the front one let open ahead by its own model at half its headway, restored at the end; the commitment kept; each pair counted once). Measured first, before the rule: under SUMO's default `speedMode` a `slowDown` / `setSpeed` target above an IDM vehicle's own next speed is inert (the influencer clamps at the model's speed — 0.095 m/s² against the model's 0.104 for a target of +0.42 m/s per step; only `speedMode` 30, the safety check off, gives a_max), so the runner's one-step targets are ceilings and `_weave_command`'s docstring, which claimed a higher target would accelerate the vehicle beyond its model, is corrected. Fifteen forms on `tests/fixtures/weave_th52.osm` at seeds 3–8 (the offset re-read each step or the relative speed fixed at entry; horizons 1.5–6 s; with and without the headway concession; each geometry alone; the −b easing kept beside it): every one departs fewer entrants on average than the sixth derivation (the rule as derived 392 / 368 / 409 / 387 / 217 / 397 of 466 at seeds 3–8, a lock at seed 7, against 411 / 412 / 420 / 397 / 424 / 407 and no lock) and each locks some seed the sixth does not (the 2-s form seed 8, with a collision; the entrant-behind-only forms seeds 4 or 5). The 2-s form meets the whole T.H.52 criterion at seed 3 alone (421 of 466, every minute above 5 m/s) — the horizon chosen by one seed, so `test_th52_weave_at_capacity_flows` keeps its strict marker with the sixth derivation's numbers and `test_th52_weave_at_capacity_does_not_lock` its pins (docstrings record the derivation). Nothing added to `WEAVE_DEFAULTS`, `meta.json`, the API schema or the sweep summary; golden `merge_weave` unchanged.
- Weave, sixth derivation — the ramp's throttle (`microsim.runner._weave_cooperate`; docs/CONTRACTS.md §2, docs/WEAVE_MODEL_PLAN.md dated paragraph). The per-step trace of every entrant on `tests/fixtures/weave_th52.osm` (seeds 3 and 4) placed the entrance bound on the ramp itself, not at the gore: a congested IDM equilibrium at 4–4.5 m/s and ≈ 75 veh/km over the ramp's first 100 m discharging at 3.1-s headways (1,150 veh/h against 1,400 of demand), SUMO refusing the `departPos="base"` insertions behind it (backlog 71 at the end, depart delay 187 s); no entrant stopped at the gore, the change made a median 5 s and 47–52 m after it with the acceptance passing on 18–22 % of auxiliary-lane steps, no entrant forced; the free IDM queue discharge is 2.2–2.3 s, so the queue's head was throttled — by the easing rule: 34–38 % of the eased ramp steps had the gap leader overlapping the entrant (63–70 % within 5 m), the entrant faster by a median 0.15–0.27 m/s, the IDM term against that leader −19 to −41 m/s² clipped to −b for a positioning that needs hundredths of a m/s². One condition added: an entrant still on the ramp is not eased towards a gap leader that overlaps it (`s_l < 0`); on the section the same geometry still eases (removing it there, or softening the brake by a pass test or a fixed-horizon constant deceleration, locks the section by minute 4–5, session record). Seeds 3 / 4 / 5: entrance 395 / 392 / 389 → 411 / 412 / 420 of 466, the ramp's first 100 m 5.0–5.9 m/s at seeds 3 / 4 (6.2–7.7 at seed 5) and 2.95-s headways (1,215 veh/h), unfinished 3 / 6 / 2, forced 22 / 23 / 24, releases 14 / 0 / 16, no collision; lane 1 at the section start now dips to 4.4–5.0 m/s in three minutes at seed 3 and one at seeds 4 / 5 (the abreast pair resolves on the section, at lane 1's cost). `test_th52_weave_at_capacity_flows` stays `xfail(strict=True)` (411 against 419, and the lane-1 criterion); `test_th52_weave_at_capacity_does_not_lock` keeps its release pin at seed 5 (seed 4 releases none); `TestWeaveRampBesideLeader` (fake harness). Golden `merge_weave` regenerated: mean travel time 69.82 → 70.83 s, p90 85.41 → 85.45 s, σ_v spatial 4.280 → 4.274 m/s, σ_v temporal 3.939 → 3.944 m/s, fuel 93.08 → 93.02 ml/veh-km, throughput 1,687.5 veh/h, collisions 0 and config hash unchanged.
- The three consumers of `meta.json["weave_sections"]` show the third and fifth derivations' counters: `WeaveSectionDiagnosticsOut` gains the optional `n_vacated` and `n_vacate_refused` beside the `n_pair_releases` it already carried (null for a meta written before the rule); the run detail's "Merge diagnostics" weaving table gains the columns "Through vacated" (through vehicles asked to leave the weave lane upstream that moved over), "Vacate refused" (requests that expired or reached the section unchanged) and "Pair releases" (stopped crossing pairs released), each with a plain-English header title and a dash for null; `scripts/corridor_sweep.py`'s `WEAVE_FIELDS` aggregates `n_vacated` / `n_vacate_refused` per on-ramp beside `n_pair_releases` and the console line prints the three. Additive; the mock, the panel test, the API test (one case for a meta with the cooperation counters but none of the three) and the sweep test carry the new keys (docs/CONTRACTS.md, `MetricsOut.merge_diagnostics` paragraph).
- Weave, fifth derivation — easing only when needed, and a stopped changer–follower pair released (`microsim.runner._weave_easing_ok`, `_weave_pair_release`; `WEAVE_DEFAULTS["pair_release_s"]` = 2 s; docs/CONTRACTS.md §2, docs/WEAVE_MODEL_PLAN.md dated paragraph). (1) A changer is eased towards its gap's leader only when the drop is needed as well as feasible (`0 < a_req ≤ b`): no brake for a leader that opens the gap by itself within the horizon — the fourth derivation's one lead, now with the ramp follower on. Alone it reaches 395 of 466 at seed 3 of `tests/fixtures/weave_th52.osm` but locks seeds 4 and 5 from minute 16 (25 / 36 driven vehicles unfinished, 10 collisions in the seed-5 jam). The per-step trace names the state: an exiter stopped at the gore in lane 1 (held by SUMO, lane 1 not on its route) with the follower of its committed lane-0 gap — a done exit-bound vehicle, not an entrant — held bumper to bumper behind its rear by the exiter's own cooperation command every step, the change refused for ever. (2) Such a pair (a driven changer and its committed gap's follower within one vehicle length behind it, both below the creep speed for more than `pair_release_s`, two ≈ 1 s human reaction times) is released: the one farther from the section end yields for the step (no target in either role, commitment dropped) and the other forces at once when in the zone, guarded against closing only. The task's entrant–exiter form of the pair was measured first and is inert on the lock (6 / 11 releases, same lock). Result at seeds 3 / 4 / 5: entrance 395 / 392 / 389 of 466 (was 325 / 307 / 295), lane 1 at the section start above 5 m/s in every minute at seeds 3 and 4 (one minute at 5.0 at seed 5), 6 / 2 / 4 driven vehicles unfinished, 0 / 9 / 1 pairs released, no collision. New counter `n_pair_releases` (meta, API schema, sweep summary). `test_th52_weave_at_capacity_does_not_lock` pins seeds 4 and 5; `test_th52_weave_at_capacity_flows` stays `xfail(strict=True)` on the entrance criterion (395 against 419). `TestWeavePairRelease` (fake harness) and the flipped `TestWeaveEasingFeasibility` cases. Golden `merge_weave` regenerated: throughput 1,661.5 → 1,687.5 veh/h, mean travel time 69.51 → 69.82 s, p90 85.1 → 85.4 s, σ_v spatial 4.34 → 4.28 m/s, σ_v temporal 4.04 → 3.94 m/s, fuel 92.87 → 93.08 ml/veh-km, changes in/out/forced 18/17/1 → 17/20/1, exits 33, collisions 0, config hash unchanged.
- Review of the scoring memory diet (bullet below), outputs re-proven against the pre-diet code on 640 probes (every public trajectory function, `compute_metrics` with and without warm-up, `analyse_replicate`'s files; frames time-major, vehicle-major, shuffled, with duplicated rows, 2–1000-row tie groups all-equal and in runs of three, NaN `x`/`v`, integer-like and whitespace/case-variant ids, integer id columns dense and with gaps, a ring, single-row and empty frames). Two divergences found and fixed in `validation.metrics`: a vehicle with equal timestamps *and* NaN `t` rows was re-sorted with the NaNs inside the argsort (pandas' `nargsort` argsorts the finite values alone and appends the NaN rows; `mean_tt_s` moved in the sixth digit), and a missing `veh_id` (factorize code −1) formed a run of its own that shifted every vehicle's run by one (crossings doubled, travel times read from the wrong vehicle; the `groupby` it replaced dropped such rows) — `_ByVehicle`, `_travel_span` and the σ_v temporal grouping now skip code −1, and `read_scoring_frame` refuses a null id instead of casting it to an arbitrary code (contract §3 has none). Neither input is produced by the runner. Verified, not assumed: the quicksort tie reproduction is the same `argsort(kind="quicksort")` call on the same contiguous array pandas makes, independent of the group's size; the batched reader equals `read_trajectories` + `factorize(sort=True)` on a 359k-row file (batch boundary inside a vehicle, short last batch), row groups of 7,777 / 100k, dictionary-encoded ids, extra and reordered columns, NaN `x`/`v`, empty and one-row files, dtypes `float64` / `intp`; `time_window_rows` falls back to the mask for vehicle-major or NaN `t` rows; the per-window position shift of the observed speed matrix equals the shifted column with an origin offset and a station beyond the run. Re-measured (fresh process per call, macOS `ru_maxrss`, 169 MB import floor): `analyse_replicate` 338 MB at 1 M rows (178 B/row over the floor), 435 MB at 2 M (140 B/row; 97 B/row marginal), both under `SCORE_WORKER_BASE_BYTES + 160 B/row × rows`; the constant stands. Out of contract and left alone: a dictionary-encoded or float32 trajectory file differs from the pandas path in `sigma_v_temporal_ms` at 1e-16 (category summation order; float32 group std). Pinned by `TestByVehicleTieOrder`, `TestMissingVehicleIds`, `TestVehicleCodes`, `TestTimeWindowRows`, `TestReadScoringFrame`, `TestSimulatedSpeedMatrix`, `TestClean`.
- Corridor battery scoring worker cut to a quarter of its memory, outputs byte-identical (`validation.battery.analyse_replicate`, `validation.metrics`, `validation.observed`, `validation.fields`). The review of the scoring pool (bullet below) had measured one worker at ≈ 545 B per trajectory row; re-measured here on synthetic contract-schema trajectories at 0.97 / 1.97 / 3.88 M rows (fresh process per function, `ru_maxrss` minus the 177 MB import floor, at 3.88 M rows): `compute_metrics` 341 B/row, `score_replicate` 239, `replicate_wave_speed_kmh` 130, the whole `analyse_replicate` 416 (slope 418 B/row over 1→4 M). The copies were what the review named: `_crossing_times` sorted a copy of the frame and turned `veh_id` into an object array (once per station cross-section in `link_hour_geh`), the two `groupby("veh_id")` iterations each took a sorted copy, `traj.loc[first_t >= t_lo]` and the window were further copies, `_simulated_speed_matrix` shifted a copy of `x`, and the three measurements each read the file. Now: `analyse_replicate` reads the file once in row batches into four arrays (`read_scoring_frame`: `veh_id` dictionary-encoded per batch, the batch dictionaries united and ordered by `metrics.vehicle_codes` — `pd.factorize(sort=True)`, so the integer codes sort and group exactly as the strings — 32 B/row live, the strings never held beside the numbers) and hands the frame to the three measurements (new keyword `trajectories=` on `compute_metrics`, `score_replicate`, `replicate_wave_speed_kmh`; the path-based calls are unchanged); one stable `(veh_id, t)` sort (`metrics._ByVehicle`: `np.lexsort`, or a stable argsort on the codes when the rows are time-ordered as the runner writes them) serves the crossing count, the travel times and the VMT/VHT integration through views, with the `groupby(sort=False)` first-appearance order kept for every sequential sum and the old per-vehicle `sort_values("t")` quicksort tie order reproduced for a vehicle with equal timestamps (`_ByVehicle.rows`); `link_hour_geh` sorts once per call, not per station; window selections are slices when `t` is non-decreasing (`metrics.time_window_rows`), `fields._clean` no longer copies a frame with nothing to drop, and the observed speed matrix shifts each window's positions rather than the column. After: `analyse_replicate` 115 B/row (slope 95), `compute_metrics` 147 (path-based, its own read included), `score_replicate` 134, `replicate_wave_speed_kmh` 102, `read_scoring_frame` 78; `SCORE_WORKER_BYTES_PER_ROW` 640 → 160 (an 80 M-row replicate ≈ 13 GB per worker instead of 45; the default six workers fit a 125 GB machine again), `SCORE_WORKER_BASE_BYTES` unchanged. Identity: `metrics.json` / `observed_scores.json` of the five synthetic replicates (0.97, 1.97, 3.88 M rows; a 0.45 M-row variant with 2 % duplicated `(veh_id, t)` rows at other positions, all rows shuffled, a single-sample vehicle and an all-equal-timestamp vehicle; a variant with NaN speeds) and a 78-entry snapshot of every public trajectory function (`travel_times`, `count_crossings`, `crossings_per_window`, `default_travel_span`, `link_hour_geh`, `score_run_against_observed`, `compute_metrics` with and without warm-up and explicit span, `replicate_wave_speed_kmh`, `analyse_replicate`) on hand-made frames with ties, out-of-order rows, NaN `v` and `x`, incomplete journeys, a ring, and mixed-width ids are byte-for-byte equal before and after (`filecmp`); replacing the quicksort tie rule by a stable one changes the snapshot, so the tie handling is exercised, not assumed. Goldens unchanged.
- Weave, fourth derivation — the entrant side (`microsim.runner._weave_easing_ok`; docs/CONTRACTS.md §2, docs/WEAVE_MODEL_PLAN.md dated paragraph). One bound added to the easing rule: a changer is eased towards its gap's leader only while dropping in behind it by the section end needs no more than its comfortable deceleration (`a_req = 2·(d + Δv·t_a)/t_a² ≤ b`, `d` the accepted gap it still has to open, `t_a` the time to the section end at its speed); in the last metres it keeps its own speed and waits for the follower or the forced mode. It binds rarely and its effect on `tests/fixtures/weave_th52.osm` is within seed noise (seed 3: entrance 317 → 325 of 466, lane 1 at the section start above 5 m/s in every minute; seed 4: 311 → 307, two minutes below 5; seed 5: unchanged). The entrant-side rules the derivation set out with were measured at seeds 3–5 and rejected: no easing of an entrant on the ramp gives a 3–5 m/s crawl of lanes 0 and 1 in every minute (the easing on the ramp is the entrant's positioning, not the head of the ramp queue as the third derivation had read it), and no cooperation from a ramp vehicle for an exit-bound changer locks the section at seed 4 (the entrant's yielding before the gore resolves the crossing pair). Easing only when the drop is needed within the horizon — the one variant that moves the entrance criterion, 400 / 394 of 466 at seeds 3 / 5 — locks at seed 4 and is the lead for a fifth derivation. `test_th52_weave_at_capacity_flows` stays `xfail(strict=True)` with the new numbers; `_weave_easing_ok` unit-tested (`TestWeaveEasingFeasibility`); golden `merge_weave` unchanged.
- Corridor battery scoring pool sized by memory (`validation.battery.score_pool_size`, `scripts/corridor_battery.py --score-procs`). Review of the parallel scoring: one scoring worker's peak RSS is ≈ 545 B per trajectory row (measured on synthetic contract-schema trajectories at 1, 2 and 4 M rows: `compute_metrics` ≈ 400 B/row from the sort and the two per-vehicle groupby iterations, `score_replicate` ≈ 470 B/row, the high-water of the whole `analyse_replicate` ≈ 545 B/row), so an 80 M-row replicate (a 4-hour corridor at 2 Hz) costs ≈ 45 GB per worker and the previous default of six workers would ask a 125 GB machine for ≈ 260 GB — the kernel would kill the scoring phase after the simulation had run for hours. The default is now `min(--procs, 6)` capped so `640 B/row × rows + 512 MB` per worker fits 80 % of `MemAvailable` (Linux; two workers on that machine), the console line states the estimate, and an explicit `--score-procs` that does not fit is warned about. Also: the report's run set is the battery's own `<out>/<config_hash>/` tree, not `<out>/` — with `--ring-seeds N` the ring benchmark's runs under `<out>/ring/` were discovered as extra groups (a second baseline, no Δ column, their seed count feeding the replicate criterion; a controlled corridor would have been contrasted against the ring). Pinned by `test_ring_benchmark_runs_are_not_report_groups` and `TestScorePoolSize`.
- Weave, third derivation — through traffic vacates the weave lane upstream of the section (`microsim.runner._weave_vacate_step`; docs/CONTRACTS.md §2, docs/WEAVE_MODEL_PLAN.md dated paragraph). One rule added, the cooperative-follower rules untouched: a through vehicle in the lane feeding section lane 1, within `vacate_ahead_m` of the section start (new `WEAVE_DEFAULTS` key, default 150 m = the HCM weaving segment's 500-ft upstream influence area; `0` disables; hash-neutral unless set), is asked once to move one lane left by `changeLane` under mode 512 (SUMO's safety check decides, speed adapted), the request living for the travel time to the section; the mode is restored on the change or at the section, an open request ended with a one-step stay (the request is by lane index and on the section's edge that index is the weave lane). New `meta.json["weave_sections"]` counters `n_vacated`, `n_vacate_refused`. On `tests/fixtures/weave_th52.osm` (seed 3): lane 1 at the section start 11.5, 10.0, 6.9, 8.1, 11.2, 12.9, 12.6, 8.6, 11.7, 11.1, 5.4, 4.5, 7.1, 6.3, 12.0, 13.4, 12.5, 10.8 m/s in minutes 2–19 (was 12.7, 11.8, 12.4, 10.3 then 2.3–6.4), the entrance departs 317 of 466 (was 225), 0 of 307 driven vehicles unfinished (was 2 of 334), 228 vacated / 28 refused, no collision; sensitivity 100 / 150 / 200 / 300 m (300 = 500, the edge before the section is 282 m): entrance 240 / 317 / 316 / 286, all windows above 5 m/s only at 200 m — non-monotone on one seed, not a trend to tune on. The strict `xfail` stays: the ramp is now held at 3 m/s over its first 100 m by the second derivation's easing rule at the anticipation-zone entry (845 easing commands on ramp vehicles, 412 of them at the zone's entry bin at −1.5 m/s²), which blocks the entrance's insertion. Mode 768 (no speed adaptation, 5 of 159 executed) and the weave's own 0.6-s acceptance (8–14 executed) were measured and rejected. Golden `merge_weave` regenerated: throughput 1,658 → 1,662 veh/h, mean travel time 70.1 → 69.5 s, σ_v spatial 4.57 → 4.34 m/s, fuel +0.2 %, changes in/out/forced 20/13/2 → 18/17/1, config hash unchanged.
- **I-24 strategy grid complete** (six cells × 20 seeds, resumed on a clean VM): FollowerStopper 10 % under ALINEA recovers most of what the controller costs alone — throughput -49.6 % → -28.9 %, travel time +124.7 % → +26.7 %, fuel +208.0 % → +61.7 %, σ_v -54.8 % — while under VSL it is worse than alone on every capacity metric (`docs/I24_STRATEGIES.md`, dated section). The slice probe of the regenerated I-94 corridor with the cooperative weave departs 0.892 (4 seeds; was 0.878 on the old inputs and rules).
- The three consumers of `meta.json["weave_sections"]` show the follower-cooperation counters of the second weave derivation: `WeaveSectionDiagnosticsOut` gains the optional `n_cooperations`, `mean_follower_decel_ms2`, `n_changer_eased` (null for a meta written before the rule, and the decel null when none was commanded); the run detail's "Merge diagnostics" weaving table gains the columns "Follower cooperations (vehicle-steps)", "Mean follower decel [m/s²]", "Changer easings" (a dash for null); `scripts/corridor_sweep.py`'s `WEAVE_FIELDS` aggregates the three per on-ramp (an older meta contributes nothing to their intervals) and the console line prints them. Additive; the mock, the API and panel tests and the sweep test carry the new keys, with one old-meta case each (docs/CONTRACTS.md, `MetricsOut.merge_diagnostics` and sweep summary `diagnostics`).
- Corridor battery post-run phase parallelised (`scripts/corridor_battery.py`, `validation.battery.analyse_replicates`, `validation.report.generate_report`). The cloud round's log showed the 20 seeds of a 4-hour corridor (1.15 GB of trajectory each) scored one after another in the parent for about 5 min each — 100 min while 31 CPUs idled — and the report then re-read every trajectory three more times (metrics, the wave-speed criterion, one contour per replicate). Scoring now runs in a second spawn pool (`--score-procs`, default `min(--procs, 6)`, sized for memory: one trajectory frame per worker), the per-seed `metrics.json` / `observed_scores.json` are byte-identical to the in-process result (pinned by `test_score_pool_matches_in_process_scoring`), and the report takes every replicate's metrics and wave reading from that scoring (`metrics_by_run`, `wave_readings_by_run`) and renders its contour from the first seed only (`figure_runs`) — the seed that survives pruning, so `--criteria-only` can regenerate the report after pruning. The console prints each phase's wall-clock (simulate, score, ring, report, prune) and a summary line. Artifact shape unchanged.
- Why two sweep VMs died mid-run (lesson 33): the guest journal archived on exit shows the kernel OOM killer taking a FollowerStopper-under-strategy run (anon-rss 8.8 GB; 32 of them on a 125 GB machine). The pipeline caps that stage's pool at 12. The strategy sweep summary is rebuilt from 117 runs: the four complete cells are unchanged, and the ALINEA cell's meter counters are now in its `diagnostics` block (0.13 % unstoppable passes at Hickory Hollow, none at Old Hickory).
- Weave, second attempt — follower cooperation as a car-following target (`microsim.runner._weave_step`; docs/CONTRACTS.md §2, docs/WEAVE_MODEL_PLAN.md dated paragraph). The max-speed caps are gone (no speed matching, no station-keeping, no exchange rule; `courtesy` accepted but inert): a changer lists the target lane on the section axis (section edges, the through lanes of the corridor edge before and after, the on-ramp), picks the nearest gap it is in or abreast of whose follower can open it at no more than its own comfortable deceleration, and that follower is driven towards the changer as a virtual leader by one-step `slowDown(v, 0.0)` targets (IDM with the follower's own parameters, clipped at its `decel`, only below its own model's acceleration); the changer eases towards its gap's leader the same way (only the rear one of an abreast pair), an entrant on the ramp is anticipated within `lookahead_m`, both movements are forced in the last `force_within_m` under the guard, and acceptance adds the immediate follower's comfort to the time gaps. New `meta.json["weave_sections"]` counters `n_cooperations`, `mean_follower_decel_ms2`, `n_changer_eased`. On `tests/fixtures/weave_th52.osm` (seed 3) the lock is gone but a crawl remains: lane 1 at the section start 12.7, 11.8, 12.4, 10.3 m/s in minutes 2–5 then 2.3–6.4 (was 20.2, 10.9, 2.9, 0.2 then 0.0), the entrance departs 225 of 466 (was 81), 1,386 of 1,966 vehicles depart (was 583), 334 driven with 2 unfinished and 0 deferred forced changes (was 92 with 49 and 16,576), 273 of 280 exit-bound vehicles that reach the section exit (was 3 of 62), no collision; `test_th52_weave_at_capacity_flows` stays `xfail(strict=True)` with the new numbers. `test_moderate_crossing_demand_does_not_crawl` passes (19–21 m/s, waits 2.4 s). Golden `merge_weave` regenerated for the rule change: mean travel time 97.1 → 70.1 s, σ_v spatial 7.59 → 4.57 m/s, throughput 1,429 → 1,658 veh/h, waves 1 → 0, fuel −9.6 % (102.51 → 92.71 ml/veh-km), changes in/out/forced 23/27/7 → 20/13/2, config hash unchanged.
- Review of the regenerated I-94 inputs: the observed wave-speed context had been computed with the excluded loop still in S792's 30-s series (`station_speed_series` now takes the same exclusions; context recomputed, median 21.3 km/h unchanged); the slice variant's boundary schedule was not shifted with its demand (fixed, pinned by a test); two doc numbers corrected (the downtown entrance receives 889 veh/h from the balance against a passage loop reading 552 — evidence the remaining +366 residual is spurious; rnd_87205 sits 428 m from the gore).
- Weave at capacity: `tests/fixtures/weave_th52.osm` mirrors the T.H.52 weaving section (3 through lanes, 308 m auxiliary lane, mainline 4,500 veh/h with 25 % exiting, entrance 1,400 veh/h) and `test_th52_weave_at_capacity_flows` pins the failure as `xfail(strict=True)`: lane 1 at the section start falls to 0.0 m/s by minute 4, the entrance departs 81 of 466. The per-step trace names the rule (station-keeping commanding 0.0 m/s to an exit-bound vehicle in the middle through lane while its forced change is deferred; the speed-matching chain and the exchange yield stop lane 1 at the start), and the eight zipper re-derivations tried each locked the section as hard or harder, so the runner is unchanged (docs/CONTRACTS.md §2, docs/WEAVE_MODEL_PLAN.md dated paragraph).
- Sweep resume counts a run as done on `metrics.json` alone (`meta.json` only feeds the optional diagnostics): the 2026-09-24 resume of 112 stored runs had started all 120 over because the older archive carried no `meta.json`.
- MnDOT loader: a reviewer can exclude a faulty detector by name (`station_frame(exclude_detectors=...)`, `scripts/mndot_fetch.py --exclude-detectors … --exclude-reason …`); the lane it served falls under the existing dead-lane scaling, a ramp node left without a flow detector is dropped, and the names, reason and scaled station-days are written into the observations' `source`. Found by the I-94 WB Mounds Blvd bracket: the +775 veh/h residual before Kellogg Blvd is S792's degraded lane-3 loop 3240, not the split detector (whose lane-1 loop 3241 chatters at 3,600 veh/h all night and never touched the demand); with 3240 excluded the Mounds exit reads 701 instead of 1,226 veh/h and the downtown entrance 524 against its own passage loop's 552 in §7's cache estimate, which keeps the net +238 apart as a residual (`docs/ONBOARDING_MNDOT.md` §7 item 2; the regenerated artifact of §11 carries its +366 residual onto that entrance instead, 889 veh/h — the review bullet above). The committed observations and demand artifact are unchanged.
- Why the capacity-scaled Minnesota population plateaus: two more capacity grids on a cloud VM (`artifacts/idm_capacity_probe_mnfleet_4l.json`, `..._i24fleet_3l.json`) show the lane count is not the cause (4 lanes: 1,609–1,689 veh/h/lane) and the corridor's fleet block is — it runs `model: EIDM` with 15 % heterogeneity on an IDM-fitted population, about 11 % below plain IDM at every headway; the I-24 replica's IDM fleet block on the same 3-lane road meets the 1,907 veh/h/lane target at T × 0.795 (`docs/ONBOARDING_MNDOT.md` §10, "Why the plateau"; the 3-lane row is §8's). The corridor's car-following model is a calibration decision to make deliberately.

## [2.4.0] — 2026-09-24

**Release summary.** The blocker on the third corridor moved. Two 20-seed rounds on I-94 WB St. Paul had locked at the merges; reading the weaving-section slice put the queue's origin at the corridor's downstream end, where netconvert had compiled two right-hand exits onto the leftmost lanes (an added left lane under ramp guessing at 12th Street; the two leftmost of five at Mounds/Kellogg). Through traffic trapped in exit-only lanes stalled the corridor from t = 87 s. The defect is fixed on the committed extract (`--ramps.unset` plus a connection patch through the new `OSMNetwork.patch_files`), pinned by a test, and turned into a **split audit** that every onboarding runs and fixes by default (CLI, builder, API, dashboard table). On the corrected map the 4-hour battery with the new **weaving-section model** (`merge: weave`) still locks, now at the start of the T.H.52 weave; a parameter probe moved nothing, and a fixture-level fix of the conflict handling (an accepted entering change executes instead of being refused by SUMO's brake gap) landed last — its effect on the corridor is the next round's question (`docs/ONBOARDING_MNDOT.md` §9–§10, lesson 32). The corridor is **not reproduced**.

Also in this release: **ALINEA ran** on the I-24 replica after the ramp-meter stop-placement fix — 20 seeds paired, mean travel time −33.4 %, fuel −15.6 %, σ_v −11.7 %, throughput −5.8 % at the reference section, many more and larger waves (`docs/I24_STRATEGIES.md`); a **hosted tester** on Cloud Run with a deploy script and record (`docs/HOSTED_TESTER.md`); a Minnesota driver population by capacity scaling whose target is **not** reached and whose artifact says so; ramp discovery for collector–distributor split/re-entry pairs and lane-add joins with the balance step conserving the pair; onboarding defaults (ramp guessing on, split fixes on, opt-outs everywhere); dashboard panels for the split audit and merge diagnostics; sweep summaries with meter and weave counters; three adversarial reviews (runner, discovery, split audit) with their fixes and 60-odd tests; a traceability audit of the night's documentation; and a test-suite fix for the libsumo/pyarrow library clash. All compute ran on self-deleting cloud VMs; one instance was deleted mid-sweep at 71 minutes (cause not recovered; the pipeline now archives the guest's exit logs).

### 2026-09-24 — split audit on every onboarding

- The weave-step fix above, run on the I-94 slice on the cloud VM (4 seeds, session record under `artifacts/mndot_rounds/weave_2026-09-24/probe_fix_462b731.json`): departed 0.879 against 0.878 before — no material change; the conflict handling of a weaving section at capacity stays open (`docs/ONBOARDING_MNDOT.md` §10).
- Weave crawl reproduced on the synthetic fixture and two rules changed (`microsim.runner._weave_step`; the T.H.52 lock of docs/ONBOARDING_MNDOT.md §10). On `tests/fixtures/weave.osm` at 1,620 veh/h (mainline 0.3 veh/s, entrance 0.15 veh/s, 30 % exiting) the section's auxiliary lane ran at 1.7–2.9 m/s in its first 50 m and lane 1 at 5–6 m/s (seeds 3, 4; 300 s), and at 3,240 veh/h every lane at 3–4 m/s with 1,100–1,300 veh/h passing the gore. The step trace shows the mechanism: an entering vehicle with accepted gaps had its mode-512 request refused by SUMO while the through-lane follower closed from 130 m back, braked at 1.8 m/s² from its first metre on the auxiliary lane and merged at 7 m/s; the exiting vehicle behind it in lane 1 matched that speed (a jump of its speed cap, up to 9 m/s² of braking) and the queue behind it took the section down to the 3 m/s creep floor. Ablating the station-keeping rule, the exchange rule or the courtesy alone moved nothing. Changes: an exiting vehicle matches and holds station only inside the last `force_within_m`; an entering vehicle with accepted gaps that pass `_weave_force_gap_ok` executes its change under mode 256 for one step. On the fixture at 1,620 veh/h the auxiliary lane now runs at 11–17 m/s, the mean wait 4–6 s (was 10–14 s), nobody unfinished; at 3,240 veh/h the section still crawls at ~4 m/s (1,540–1,600 veh/h past the gore, +30 %) — not resolved, the exiting movement from lane 2 is next. Golden `merge_weave` regenerated: `n_weave_forced` 0 → 7, `n_weave_changed_in/out` unchanged (23/27), throughput 1437 → 1429 veh/h, mean travel time 93.0 → 97.1 s, p90 138.8 → 123.1 s, σ_v spatial 8.65 → 7.59 m/s, fuel 105.8 → 102.5 ml/veh-km, no collision. Tests: `TestWeaveRun::test_moderate_crossing_demand_does_not_crawl` (regression), `TestWeaveLockRules` (two fake-TraCI tests); the bookkeeping expectation for an exiting vehicle outside the zone updated. docs/CONTRACTS.md §2, weaving sections.
- Adversarial review of the split audit and the onboarding defaults (22f351b, 159f425), six defects confirmed by targeted tests and fixed (`tests/test_microsim/test_microsim_split_audit.py::TestReviewFindings*`, `tests/test_api/test_corridors.py`): (1) a connection patch restating one exit made netconvert drop every computed connection of that edge, so a second exit at the same node (a right and a left link at one junction) lost its way in; the patch now repeats the sibling exits as compiled (`SplitFinding.exit_connections`, internal). (2) A `wrong_side` finding on a `-AddedOffRampEdge` piece (possible when the way has no `lanes` tag) produced a patch with the piece's lane count, which netconvert refused at load time ("Could not insert connection ... after build") and failed the onboarding; the patch now uses the load-time count (`SplitFinding.load_time_lanes`) and `ramps_unset_edges` includes the edge, since guessing rebuilt the piece over the patch otherwise. (3) A `--` in the patch's XML comment (a scenario name like `i94--wb` reaches it through the note) made netconvert refuse the file; runs of hyphens are collapsed. (4) A caller's `--ramps.unset=<edge>` (the `=` form) was not recognised and a second `--ramps.unset` was added, which netconvert refuses as a repeated option; both forms are merged in place. (5) Two corridors onboarded from one `--osm-file` shared `<extract stem>.splits.con.xml` and the second overwrote the first's patch, silently changing what the first scenario compiles; a patch already there that states other connections is left alone and the new one goes to `<extract stem>.<scenario name>.splits.con.xml` (`apply_split_fixes(fallback_path=)`; identical connections share the file; the API job's per-corridor path is unaffected). (6) The API job registered the split patch for the failure cleanup before knowing whether it would write it, so a patch already beside the extract's path was deleted by a failed job; only a path that did not exist is registered. Also: the continuing-mainline fallback (used when the corridor ends at the split) took the first same-class way in file order, which at a genuine fork listed first was the branch and flipped a right link to `left`; it now takes the straightest continuation (synthetic graphs in every heading). An untagged split whose mainline keeps its lane count is restated as an option lane (netconvert's own layout), not a drop lane. Refuted with evidence: the sign convention in every heading and across the 180° wrap; `middle` / `all` / option-lane verdicts (an exit fed from the middle or from an option lane on the side opposite the drawn one is still `wrong_side`, and the remedy is right); the audit runs on the piece that holds the diverge; the generated XML compiles on the fixture with the connections as written; applying the fixes to a fixed build changes nothing; `with_ramp_guessing` de-duplicates `--opt` and `--opt=value` with `--ramps.no-split` present; `--fail-on-split-defect` exits after a complete YAML (the same order as `--fail-on-lane-mismatch`). docs/CONTRACTS.md "Split audit" limits updated.
- Review of the C-D discovery and pair balance (fee67c3), two defects in `microsim.geo._cd_path` confirmed on hand-built nets and fixed: a link loop returning to the split's own node (the path feeding `chain[split + 1]`, which starts on that node) was paired as a zero-length C-D road with split and re-entry at one `x`; it is an exit now, like a loop returning upstream always was. And the search kept one dominance record per edge, so a route reaching an edge in fewer metres but more than `max_cd_edges` edges hid an admissible route through the same edge and the C-D road went unfound; the record is per (edge, edges walked) and the bounds are applied before a path is queued. Refuted with fixtures: the 3 km bound is metres walked along the path; two splits into one C-D road claim each link once (the second is a plain exit, its joining leg an inventory-only attachment); `oneway=-1` on a joining arterial is resolved by netconvert; shallow one-way crossings, split-way crossings, bridges and tunnels through a shared node are rejected; discovery is deterministic and leaves the net untouched; the balance step with a dead split detector and no re-entry detector gives the split nothing and the re-entry the bracket's remainder (station counts reproduced, pair record says out 0), a drop across the re-entry's bracket is a residual, never a negative return; `flowstate.demand/1` round-trips with the additive keys. Open (heuristic, unchanged): a crossing road that bends more than 35° at the shared node passes the pass-through test and is accepted as `shallow_join`; a two-way road ending on a mainline node is accepted while its reverse leg (an exit onto the same road) is not inventoried. Tests in `tests/test_microsim/test_microsim_geo_cd_review.py`, `tests/test_calibration/test_calibration_onboarding_cd_review.py`.
- Tests: the parquet path shim for the libsumo/pyarrow Arrow clash (libsumo's bundled `libarrow.2400` next to pyarrow's `libarrow.2500` makes every `pyarrow.fs.LocalFileSystem` construction raise `ArrowKeyError: ... scheme 'file' ... already registered` for the rest of the process) moves from `tests/test_microsim/conftest.py` to the root `tests/conftest.py`, so it is installed for every selection under `tests/` and not only when `tests/test_microsim` is collected — `pytest tests/test_scripts tests/test_validation -m "not slow"` went from 24 failed / 26 errors (eight `test_doctor.py` tests load libsumo in-process through `run_checks` / `run_smoke`; every later `DataFrame.to_parquet(path)` in `tests/test_validation` then died) to 289 passed; the whole tree and each directory alone were already green and stay so (`docs/AUDIT_2026-09-03.md` finding on `tests/test_microsim/conftest.py:11`).
- Dashboard: the two onboarding build switches are on the Onboard form (this supersedes the "no netconvert options in its Advanced section" note in the onboarding-defaults entry below). The Advanced disclosure gains two checkboxes, both ticked by default as on the API and the CLI — "Guess acceleration lanes (netconvert ramps.guess)" and "Fix exits compiled on the wrong side (split audit)", each with one line of plain-English help naming the I-94 WB St. Paul fault behind it — sent on every `POST /corridors` as the `ramp_guessing` / `split_fixes` form fields (`"true"` / `"false"`; the rest of the multipart body is unchanged). The corridors panel prints the summary's `applied` line above the split-audit table and, when `split_audit_before_fixes` is present and differs from the audit that stands, `before fixes: N defects` (verdicts other than `ok` / `unknown`). `CorridorSummary` in `frontend/src/api/types.ts` carries the additive fields (`split_audit_before_fixes`, `ramp_guessing`, `split_fixes`, `split_fixes_applied`, `split_defects_remaining`, `split_patch_file`, `applied`); the demo corridor shows one fix applied and one defect remaining. Tests in `frontend/src/test/onboard.test.tsx` (defaults ticked and sent, an unticked switch sent as `"false"`, the applied and before-fixes lines rendered).
- **ALINEA ran on the I-24 replica** (stage `sweep_i24_strat` after the meter fix; `docs/I24_STRATEGIES.md`, dated section): ALINEA alone versus baseline, 20 seeds paired — mean travel time -33.4 %, fuel per vehicle-km -15.6 %, σ_v -11.7 %, throughput at the reference section -5.8 %, wave count +326.8 %, wave amplitude +26.8 % (all resolved); the ramp queue wait is outside the travel-time span and the meter counters were not archived. The instance was deleted at 112 of 120 runs (cause not recovered from the guest; the pipeline now archives the idle-guard log and the journal tail on exit), so the FollowerStopper-under-ALINEA and -under-VSL cells stay unreported (the artifact names them under `incomplete_cells` and carries no aggregate for them; their run counts, 15 and 17 seeds, come from the fetched run directories — a session record).
- Sweep summaries carry the meter and weave counters the ALINEA round could not report: `scripts/corridor_sweep.py` reads each run's archived `meta.json` and adds an additive `diagnostics` block per cell (`n_runs_with_meta`; per ramp meter the seed mean, 95 % t-interval and n of `n_released`, `n_passed_unstoppable` and the unstoppable share; per weaving section the same for `n_entered`, `n_exited`, `n_reached_section_exiting`, `n_forced`, `n_forced_deferred`, `n_unfinished`, `wait_s_mean`), printed in the console summary. On the local `runs/i24_strat_sweep` archive (metrics only) `--analyze-only --allow-partial` reproduces `artifacts/sweep_i24_strategies_summary.json` byte-for-byte in every existing key with `n_runs_with_meta: 0`; the committed artifact is unchanged. Tests in `tests/test_scripts/test_corridor_sweep.py` (fake tree, two cells × three seeds). docs/CONTRACTS.md, "Sweep summary `diagnostics`".
- Onboarding defaults: ramp guessing on, split fixes on. Every onboarding path (`microsim.scenarios.corridor_from_bbox`, `scripts/onboard_corridor.py`, `POST /corridors`) now compiles with `--ramps.guess --ramps.ramp-length 250` (`RAMP_GUESSING_OPTIONS`, the MnDOT values; `with_ramp_guessing` adds them in front of the caller's `netconvert_extra` and never duplicates an option the caller gave) and, when the split audit finds a `wrong_side` / `added_lane_wrong_side` exit, applies the fixes and re-audits: the connection patch is written beside the extract as `<extract stem>.splits.con.xml` (the committed `data/osm/mndot_i94_wb_stpaul.splits.con.xml` pattern; the API job puts it beside the installed extract under the data root and removes it on a failed job like the extract), `--ramps.unset` is added, the network re-imported. Both audits are kept (`CorridorBuild.split_audit_before_fixes` / `split_audit`, `CorridorSummaryOut.split_audit_before_fixes`, additive with `ramp_guessing`, `split_fixes`, `split_fixes_applied`, `split_defects_remaining`, `split_patch_file`, `applied`) and the inventory prints `splits before fixes`, `splits` and one line saying what ran: `applied   ramp guessing on; split fixes: 2 applied, 0 remaining`. Opt-outs: `--no-ramp-guessing` / `--no-split-fixes` (CLI), `ramp_guessing=false` / `split_fixes=false` (API form); `--write-split-patch PATH` now only chooses where the patch goes (refused with `--no-split-fixes`, exit 2); `--fail-on-split-defect` judges the final audit. Tonight's I-94 record (docs/ONBOARDING_MNDOT.md §6, §9) is why both are on: without guessing the entrances starve, with it a split can compile on the wrong side. On the committed extract under the defaults: 8 exits, 2 defects before the fixes, 0 after, the generated patch equal to the committed one's connection lines; on the synthetic API fixture, five chain pieces with two 250 m guessed lanes and no defect. Tests in `tests/test_microsim/test_microsim_split_audit.py`, `tests/test_scripts/test_onboard_corridor.py` (new), `tests/test_api/test_corridors.py`, `tests/test_microsim/test_microsim_geo.py`. The dashboard's Onboard form gained the two opt-outs the same night (bullet above). docs/ONBOARDING_MNDOT.md §3 and §7 item 1b, docs/CONTRACTS.md "Onboarding defaults".
- Weave exit counting fixed (the open finding of the review below). `n_exited` was a per-step sighting on the exit ramp's first edge, which undercounts a ramp edge shorter than one step of travel (12.5 m at 25 m/s, 0.5 s) on a real corridor. An exit-bound vehicle is now counted once when seen on any edge of the paired off-ramp (`exit_edges`, new in the meta) or when it leaves the network from the section (or an internal lane after it) — with teleporting off and `collision.action warn` that can only mean it drove the ramp to its end; a vehicle that leaves by the mainline is never counted. New counter `n_reached_section_exiting` (exit-bound vehicles that entered the section during the run) is the denominator of `n_exited`; `n_departed_exiting` stays and also counts vehicles still upstream at the end. `WeaveSectionDiagnosticsOut.n_reached_section_exiting` (null for an older meta) and the dashboard column "Exited / reached" (falls back to the routed count when the field is null). The `merge_weave` golden is unchanged (`n_weave_exited` 33 before and after: the fixture's exit edge is ~715 m as compiled, so every exit was already sighted); six fake-TraCI tests in `TestWeaveExitBookkeeping`. docs/CONTRACTS.md §2, weaving sections.
- Dashboard: the three diagnostics a tester needed `meta.json` for are on screen. The Onboard view's corridors panel renders a **Split audit** table when `CorridorSummaryOut.split_audit` is non-empty (x, split → exit, OSM side, compiled lanes with the added lane marked, verdict badge: `ok` green, `wrong_side` / `added_lane_wrong_side` red, `unknown` grey, the remedy under a defect row; `frontend/src/components/SplitAuditTable.tsx`). The run detail gains a **Merge diagnostics** section, shown only when the run has a ramp meter or a weaving section: one table per kind with the counters under plain-English headers (Released, Passed unstoppable, Entered, Changed in/out, Exited / routed (renamed Exited / reached by the exit-counting fix above), Forced, Deferred (vehicle-steps), Missed, Unfinished, Mean wait [s]), labelled with the seed they came from (`frontend/src/components/MergeDiagnostics.tsx`). Additive API field `MetricsOut.merge_diagnostics` (`MergeDiagnosticsOut`: `seed`, `ramp_meters`, `weave_sections`) reads the first replicate's `meta.json["ramp_meters"]` / `["weave_sections"]` beside `fd_source`; null when the run has neither or the meta cannot be read. Demo mode carries one example of each. Tests: `splitaudit.test.tsx`, `mergediag.test.tsx`, the Onboard and Run detail view tests, `tests/test_api/test_runs_merge_diagnostics.py`.
- Review of the two runner pieces committed tonight (meter stop placement, weave step), seven fake-TraCI tests added: one defect confirmed and fixed — on a weaving section of several pieces run with `internal_links: true`, a driven vehicle reporting an internal junction lane for a step was booked as missed and re-entered on the next piece with its timers reset; it is now held in transit (latent: every checked-in scenario compiles without internal links; the `merge_weave` golden is unchanged). Refuted with evidence: distance and braking arithmetic, seen/stopped/release bookkeeping, queue-length contract, decel semantics, determinism, forced-change gap signs, deadlock-rule oscillation, non-participant handling; `n_forced_deferred` counts vehicle-steps. Open: `n_exited` is a per-step sighting on the exit's first edge (an exit edge shorter than one step of travel would undercount on a real corridor).
- The map fault behind the I-94 WB lock (the entry below: two right-hand exits compiled from the leftmost lanes) is now a generic onboarding check. `microsim.split_audit.audit_splits(net_path, osm_path, corridor_edges)` audits every exit leaving the chain: the OSM side of the leaving way (signed lateral offsets of the link's first nodes from the continuing mainline way, cross product in local metres, plus the `turn:lanes` reading when tagged) against the compiled side (which `fromLane` indices feed the exit, rightmost / leftmost / middle / all, and whether the edge gained a lane over its OSM `lanes` tag), with a verdict `ok` / `wrong_side` / `added_lane_wrong_side` / `unknown` and the remedy in the engine's terms (a `patch_files` connection patch restating the split, `--ramps.unset <edge>` for a lane ramp guessing added on the wrong side). `corridor_from_bbox` runs it on every build (`CorridorBuild.split_audit`, the `splits` block of the printed inventory, `CorridorSummaryOut.split_audit` on `GET /corridors/{id}`, additive); `scripts/onboard_corridor.py` gains `--fail-on-split-defect` (exit 4) and `--write-split-patch PATH` (`microsim.scenarios.apply_split_fixes`: writes the patch, adds it and the `--ramps.unset` entries to the scenario, re-imports and audits again before the YAML is written). On the committed extract compiled without its fixes the audit reports exactly the two known defects (`45608485 → 18207912` wrong_side, link 8–9 m to the right, compiled lanes 3–4 of 5; `1001426896 → 82150350` added_lane_wrong_side, link 1–45 m to the right, lane 3 of 4) and the 6th Street exit `42165869` (5–19 m to the left) as `ok`; with the fixes, none; the generated patch equals the committed `mndot_i94_wb_stpaul.splits.con.xml` connection lines; re-onboarding the extract from its bbox and applying the fixes leaves zero defects (`tests/test_microsim/test_microsim_split_audit.py`, synthetic fixture `tests/fixtures/splits.osm`). docs/CONTRACTS.md, "Split audit".

### 2026-09-24 — ramp discovery: collector–distributor roads and non-link entrances

- Collector–distributor roads (docs/ONBOARDING_MNDOT.md §7.2): `microsim.geo.ramps_for_chain` searches the links leaving the chain for a way back onto it within 3 km before calling the link an exit; the split is an `off` candidate with `cd_road`, `rejoin_edge`, `rejoin_x_m`, the re-entry an `on` candidate with the same `cd_pair`, ramps attaching to the C-D road are inventoried with `attach_via_cd` (not simulated), and `RampSpec` carries `cd_road`/`cd_pair` (additive, hash-neutral). The balance step pairs the ends — a re-entry without a live detector first takes back what its split sent out — and the demand artifact lists `cd_pairs`. On the committed MnDOT I-94 WB extract, re-onboarded into a scratch directory (the pair itself is pinned by `tests/test_microsim/test_microsim_geo_ramps.py`; the flows that follow are a session record, not in any committed artifact), the White Bear Ave road is found as a pair (split 5.317 km, re-entry 5.930 km): the split sends out 374 veh/h, the re-entry returns 442 veh/h, and the S1068→S1947 residual of +442 veh/h is closed (5 brackets with residuals, was 6; the committed scenario and artifact are unchanged). The Mounds Blvd split is not a C-D road (it ends on North Mounds Boulevard at grade) and the +775 veh/h S792→S791 residual has no map explanation.
- Non-link entrances (docs/ONBOARDING_MNDOT.md §7.1): a drivable non-link way ending on a mainline node within 35° of its heading, not continuing past the node, is an entrance with `discovery="lane_add"` (the mainline gains a lane) or `"shallow_join"`; a crossing road through a shared node is not a ramp. Every `RampCandidate` records its `discovery` and the inventory prints it. The White Bear Ave entrance itself turned out to be the C-D re-entry above; on a motorway-only extract no non-link entrance can exist (synthetic fixtures cover the rule: `tests/test_microsim/test_microsim_geo_ramps.py`, plus a slow check on the real extract).


### 2026-09-24

- **Map defect behind the I-94 WB lock, found and fixed.** The weave-model slice diagnosis (read from the run, no new simulation) placed the start of the corridor-wide queue at the corridor's downstream end, on edge `1001426896` (10.73–10.96 km), where through vehicles trapped in a lane that led only to the 12th Street exit stalled the other lanes and the queue spread upstream at about 8 km/h. The compiled network is wrong there: OSM tags that exit as a right-hand option lane (`turn:lanes none|none|through;slight_right`) and its link leaves 4–29 m to the right of the mainline, but under `--ramps.guess` netconvert added a fourth lane on the left and fed the exit from it alone; the Mounds/Kellogg exit (`18207912`, two right lanes of five, link 8–14 m to the right) is compiled from the two leftmost lanes with or without guessing. Every MnDOT round of 2026-09-23 ran with the second defect and round 2 with both. Fix: `--ramps.unset 1001426896` (the 12th Street split compiles as three lanes with lane 0 an option lane) and an explicit connection patch for `45608485` (`data/osm/mndot_i94_wb_stpaul.splits.con.xml`), pinned by `tests/test_microsim/test_microsim_osm_split_patch.py` on the committed extract; the five guessed acceleration lanes survive. New `OSMNetwork.patch_files` carries such patches (repo-relative, inside the allowed data roots, in the config hash); the three MnDOT scenarios (committed, weave, and the new 35-minute `mndot_i94_wb_stpaul_weave_slice.yaml`) use it. Pipeline stage `mndot_slice_weave` (4 seeds on the slice) precedes the 20-seed weave batteries.
- Weaving-section merge model (`RampSpec.merge: "weave"` with a `WeaveSpec` naming the exit ramp; `docs/WEAVE_MODEL_PLAN.md`): an entrance whose auxiliary lane also serves a downstream exit is run as one section with two crossing movements under the scripted gap-acceptance rules (`WEAVE_DEFAULTS` = the scripted defaults plus `exit_accept_gap_s`), the section found by walking lane 0 from the entrance to the exit (`microsim.networks.weave_sections`; the same walk now backs `accel_lane_end`), per-section counters in `meta.json["weave_sections"]`, a bad pairing refused with the edges named, and two deadlock rules for an entering and an exiting vehicle side by side at the lane's end. Golden `merge_weave` on the synthetic fixture `tests/fixtures/weave.osm` (both movements complete, no unfinished vehicle). On the I-94 WB St. Paul corridor the walk finds the Ruth St and T.H.52 sections at 135.8 m and 305.2 m; a single-seed 35-minute peak slice with both entrances as weaves (session record, not committed) raised T.H.52's departed share from 0.52 to 0.68 and left the downstream lock from S1070 to S97 in place with one collision inside a weave section, so the plan's slice criterion (both weave entrances ≥ 95 % departed) is **not met**; the scenario `scenarios/mndot_i94_wb_stpaul_weave.yaml` (two weaves, two scripted merges, otherwise the committed corridor) and the pipeline stages `battery_mndot_weave` / `battery_mndot_weave_mnpop` carry the 20-seed test.
- Hosted tester on Cloud Run (`flowstate-tester`, us-west1; the image built from the repository by Cloud Build, an inline queue, results on a mounted bucket): Google's front end answers `/healthz` on a Cloud Run URL with its own 404 before the container sees the request, so the server now also serves the health probe at `/health` (auth-exempt, not in the OpenAPI schema) and the dashboard polls that path.
- Ramp-meter stop placement fixed (the ALINEA defect of 2026-09-23, docs/LESSONS.md row 31): a ramp vehicle is decided once, at its first step on any of the ramp's edges, and receives the meter's stop only if it can still brake for the line (v²/2b + v·Δt with its own comfortable deceleration); a vehicle already inside that distance passes the meter that cycle and is counted in `meta.json["ramp_meters"][i]["n_passed_unstoppable"]`. SUMO's own "too close to brake" refusal is handled the same way; every other TraCI error still fails the run. Checked on synthetic ramps only (a two-edge ramp with a 95 m last edge, the I-24 shape, on which the old code raised; a single-edge ramp where some vehicles cannot stop); the I-24 ALINEA cells are rerun in this night's cloud round. The queue length passed to the meter controller now includes stopped vehicles still upstream of the last edge (ALINEA does not read it).
- Pipeline stages `mndot_population` and `battery_mndot_mnpop`: the I-24 episode-fitted population scaled to the I-94 WB St. Paul fundamental diagram's capacity by the US-101 procedure (`scripts/calibrate_capacity.py`), and the 20-seed battery with that population.
- `final.tgz` (a pipeline results archive, 25 MB) is no longer tracked; it was committed by mistake on 2026-09-23 and is ignored from here on.

### 2026-09-23 — after 2.3.0

- Defect found by the I-24 strategy sweep, not fixed tonight: the ALINEA meter places its stop line 30 m before the end of the ramp's last edge, and on the Hickory Hollow Pkwy entrance (`19441652#1`) SUMO refuses it for a vehicle already too close to brake (`TraCIException: stop … is too close to brake`), so every ALINEA cell fails at the same vehicle in every seed. The meter had only ever run in a single-seed experiment (docs/I24_VALIDATION.md §0.5(j)). The stop must be set only for vehicles with braking distance to spare, or on the ramp's first edge; until then ALINEA results exist on no corridor.
- Pipeline stage `sweep_i24_strat`: the first strategy sweep on a validated corridor — VSL, ALINEA and FollowerStopper at 10 % (alone and under each strategy) on the I-24 ramps arm, six cells × 20 seeds, ALINEA target from the equilibrium-capacity artifact (`docs/I24_STRATEGIES.md`). First result, 20 seeds paired: the VSL threshold ladder at its defaults lowers σ_v by 29 % and the wave amplitude by 24 % but costs 7.4 % throughput, 7.9 % mean travel time and 16 % fuel, with more and slower waves — the smoothing-for-capacity trade of the FollowerStopper sweep, paid at a higher price; FollowerStopper 10 % alone on the same arm: σ_v -59.5 %, throughput -49.6 %, fuel +208.0 %; the FollowerStopper-under-VSL cell was cut at 6 seeds by the VM's deadline and is not reported (`artifacts/sweep_i24_strategies_summary.json`).
- Fifth review (the last two packages), six findings fixed: an inline corridor job that crashes before claiming its row now fails the row with a plain message instead of leaving it queued; a corridor name is taken in one write transaction at row creation so a second POST for an active name answers 409 (the refused request's uploads are removed); the wave-speed lag floor applies to the refined lag; a gap width below one bin is refused; usable events are counted on the detrended mask; the lane summary counts rows it could not use.
- Second browser walkthrough (the guided panel, Advanced onboarding, corridors panel, observations selector): the guided panel keeps its identifiers in session storage and re-derives every step from the server on remount (a step ticks only on an answer; a stale id is cleared with a message); the offline text no longer claims "nothing was sent" while a write is in flight; the inline-queue corridor job runs as a background task after the 202 so a long onboarding no longer blocks the health probe; demo rows show a fixed count instead of wall-clock progress; an Overpass 504 is named as a server outage with the body still withheld; a supplied observations artifact with no comparable window says so on the criteria rows; accessible names for the corridor rows and the submit button, and error toasts that persist 15 s with a dismiss button.
- Fourth review (wave context, guided panel, third-review fixes), its findings fixed: the wave-speed context stores and prints its leave-one-date-out range beside the median; lags whose window crosses the gap between dates are masked; a lag under two bins is rejected as below resolution; the sign test precedes the bound test; `min_events` is documented as runs; the detrend residual is left undefined where its window is one-sided; patch-file stems carry a digest of the raw edge id (no collisions); `--strict-lanes` with a tolerance is refused; the guided panel ticks the metrics step on a server answer; an impossible inventory lane count is unusable.
- Third adversarial review (the late packages), its findings fixed: the warm-up guard now covers `POST /sweeps` (one 422 for the grid) and exempts the macro tier, whose metrics report over the whole run; the lane check's tolerance defaults to 0 with the benign ramp-guess case suppressed unless `--strict-lanes`, its hint names both causes of a missing lane, an empty station table is reported as unchecked and a non-integral lane count is unusable; the battery abort fires on an empty plan, a missing arrival counter stays missing (mean over the runs that have it), unnamed ramps are labelled by their scenario index, and lane-end patch files tolerate dotted edge ids.
- Guided first run: a six-step panel (connect, store the ring preset, launch a 2-replicate smoke, watch it finish, open metrics and heatmap, create and download a report) reachable from the rail and shown on the Scenarios view; each step is done / current / blocked with the reason, a step is marked done only by a server answer, and offline demo mode blocks every write with its reason. The mock backend's corridor record no longer shadows the API's `CorridorRow` type (the app build failed on it).
- Observed wave speed from detectors, as report context: `calibration.waves_observed.detector_wave_speed` cross-correlates detrended 30-s speed series of adjacent stations over the downstream station's congested episodes and turns the lag into a backward wave speed per pair (rejections listed by reason); `scripts/mndot_fetch.py --wave-context` stores it under the observations artifact's new `context` key (old artifacts unchanged), and the report's Observed-data block prints one computed line beside the profile's band. On I-94 WB (nine weekdays, 13 pairs) the median is 21.3 km/h, IQR 18.4–24.2, from 6 usable pairs, and 18.4–21.6 km/h under leave-one-date-out — the fast edge of the 14–22 km/h band the model is held to (`docs/ONBOARDING_MNDOT.md` §4a).
- Onboarding flow gaps closed: the onboard form's Advanced section sends a column map, a driver-population artifact path and a source; `GET /api/v1/corridors` lists onboarded corridors newest first and the Onboard view's corridors panel prefills the run and report actions from any of them; the report launcher offers the finished corridors' observations (or a typed server path) so a report can be scored against observations from the dashboard.

## [2.3.0] — 2026-09-23

**Release summary.** Onboarding a freeway corridor is now a product path: a bounding box, a bearing and a detector export become a validated SUMO scenario with discovered ramps and station positions, a typical-weekday observations artifact, a demand profile that closes the station balance, and a report that scores GEH and RMSPE against those observations — from the dashboard or the CLI. The third corridor, MnDOT I-94 westbound through east St. Paul, was onboarded this way from the Regional TMC's public 30-second loop data (nine weekdays, 14 stations, 17 ramps). Its first battery exposed a map defect that the path now guards against: OSM carries no acceleration lanes, so the compiled network starved its entrances (four of seven by the session's reading of the first seed); ramp guessing with split-piece expansion fixed the starvation, and the second battery on that network then gridlocked at the merges the model cannot represent (the dated entry below); a lane-profile-versus-inventory check is printed by the onboarding step and can fail the build.

Also in this release: the infrastructure axis on sweeps (VSL, ALINEA, both) with a strategy-comparison table in the report and an ALINEA target that traces to the corridor's fitted diagram; the screening tier on a calibrated fundamental diagram with macro options through the API; a `doctor` preflight and a ten-minute quickstart for a first-time tester; adversarial reviews and browser walkthroughs applied (the findings are listed in the dated entries below; the review notes themselves are session records). The corridor itself is not reproduced: two 20-seed cloud rounds failed for two documented reasons (missing acceleration lanes, then SUMO's lane-change merge locking at three large entrances near capacity — the I-24 merge limitation, fatal here), both recorded under artifacts/mndot_rounds; no sweep ran on it, and the record says what would be needed next.

### 2026-09-23 — Any-corridor onboarding from public data; MnDOT I-94 WB St. Paul (third corridor)

Onboarding path (product)
- `microsim.geo` (new): corridor geometry without corridor-specific code — the one-direction motorway chain discovered from a bounding box by bearing (`mainline_chain`), on/off ramp discovery from motorway links (`ramps_for_chain`), lane profile along the chain, and detector positions from latitude/longitude projected onto the chain (`x_of_lonlat`, with the perpendicular offset so a station 200 m off the road is rejected). `networks.osm_import` can download an Overpass extract restricted to motorway ways (`download="overpass"`); `scenarios.corridor_from_bbox` builds a validated OSM scenario with placeholder demand; `scripts/onboard_corridor.py` is the CLI (35 tests on a synthetic two-carriageway fixture). The I-24 gallery chain and the US-75 chain are reproduced by the generic discovery.
- Detector observations contract `flowstate.observations/1` and demand artifact `flowstate.demand/1` (docs/CONTRACTS.md): a tidy detector frame (`timestamp, station, flow_veh_h, occupancy_pct, speed_ms, lanes, kind, x_m`) that any state-DOT export can be mapped onto (`calibration.loaders.detector_csv`, `column_map`), a per-window typical-weekday profile with spread and coverage, and the mainline/ramp demand derived from it. `POST /calibrations/demand` writes both artifacts; `POST /calibrations/fd` accepts `loader: detector_csv` and `column_map`.
- MnDOT loader (`calibration.loaders.mndot`): the Regional TMC's IRIS metro configuration (151 corridors, 10,377 detectors) and the Mayfly archive API (30-s counts, occupancy and speed per detector-day, HTTPS only; the older `trafdat` path is firewalled). Station totals are cross-section counts (mainline plus auxiliary lanes); a detector that never reports on a day is excluded from the validity denominator instead of voiding its station. `scripts/mndot_fetch.py` writes the station table, the tidy frame and the observations artifact for a station span and a list of dates from a local response cache.
- `scripts/corridor_demand.py`: fills an onboarded scenario from observations — entry inflow from the upstream station, ramp flows that close the mainline balance bracket by bracket (live ramp detectors fix the shares; the closing kind absorbs the remainder; unexplained residuals carried and listed), the downstream speed boundary from the last station, warm-up and the driver population — and records the derivation in the demand artifact.
- `calibration.onboarding.calibrate_scenario` (new): the demand glue as a library — station positions onto the SUMO chain, discovered ramps matched to ramp detectors, the bracket balance, the downstream boundary and the `flowstate.demand/1` artifact, with the residuals and zeroed ramps it could not explain. `scripts/corridor_demand.py` is now a thin CLI over it and reproduces the committed MnDOT scenario, demand artifact and observations byte for byte.
- `POST /api/v1/corridors` (new, 202, multipart) runs the whole path as one job — Overpass extract, chain/lanes/ramps/station positions, observations artifact, demand, and the calibrated scenario stored *and* installed as a `scenarios/<name>.yaml` preset (409 rather than overwriting an existing one). `GET /corridors/{id}` reports the stage it is on and, when done, what was discovered and derived, the `scenario_id` to run and the `observations_path` to report against (docs/CONTRACTS.md). The dashboard's **Onboard corridor** view is the three-input flow over it: form → stages → summary → *Run 20 seeds* → *Report against observations* (docs/ONBOARDING_MNDOT.md §3).
- `scripts/corridor_sweep.py`: penetration × compliance × controller × strategy (`none`, `vsl`, `alinea`, `vsl+alinea`) grid on any scenario with the baseline and infrastructure-only cells, per-run metrics on the analysed span, paired per-seed deltas versus baseline with 95 % CIs, resumable by config hash.
- Cloud pipeline: stages `battery_mndot` and `sweep_mndot`, archive extras for onboarded corridors, `launch_i24_pipeline.sh --data-set none` (no I-24 data upload for a corridor round).
- Sweeps gain the infrastructure axis end to end: `strategies` (`none`, `vsl`, `alinea`, `vsl+alinea`) on `POST /sweeps` with the no-AV `strategy_<s>` cells and the baseline, one shared config patch (`flowstate_core.strategies.apply_strategy`) used by the CLI sweep and the API, an ALINEA target that comes from the request or the scenario's fitted diagram (ρ_c per lane) and is refused otherwise, a `## Strategy comparison` table in the report (one row per run group: mean [95 % CI] and the paired delta versus baseline with its CI), and a dashboard launcher and matrix that name the deployment.
- Merges on onboarded corridors: `OSMNetwork.netconvert_extra` passes options to every netconvert import (recorded in the config hash when set); with `--ramps.guess --ramps.ramp-length 250` netconvert adds 250 m acceleration and deceleration lanes by splitting the highway edge, the importer expands each scenario edge onto its split pieces (`networks.expand_ramp_splits`) and the runner points every ramp at its piece while the scenario keeps load-time ids; `corridor_from_bbox(max_chain_m=…)` ends a chain before a downstream widening. Found on the first MnDOT battery: OSM tags I-94 as three lanes straight through its merges, and a link joining lane 0 at a plain junction starves under SUMO's yielding — GEH passed at 17 % of link-hours and the corridor never congested (the session's reading of the first seed's metadata put the never-departed share at 27 %; the round's metadata was not kept); on the split network every upstream merge delivers its demand in the peak-slice smoke — and the 20-seed battery on it then gridlocked (see the MnDOT entry).
- Adversarial review of the night's changes (engine and calibration): a lane with no reporting detector all day is now scaled from the lanes that report and listed under `frame.attrs["scaled_station_days"]` instead of silently under-counting the station (the MnDOT numbers are unchanged: the never-reporting loops there sit on lanes other detectors cover); the demand balance uses the observation window count rather than the observed-step count, carries what an exit fraction clipped at 95 % cannot take, and reports carried windows per ramp; the runner's merge-model path indexes the expanded chain after ramp-guess splits; `expand_ramp_splits` is idempotent and never duplicates a piece; the onboarding summary places an entrance at the start of its split piece and an exit at its end; a sweep summary writes `null`, not `NaN`, for a single-seed CI; `OSMNetwork.inflow` rejects negative or unordered steps like the corridor network does.
- Adversarial review (service, validation, dashboard): a station the simulated span never reaches is excluded from the GEH link-hours and the speed matrix and listed in the report's observed-data block instead of scoring as a zero-flow failure; an artifact that cannot form a comparison leaves the GEH and RMSPE rows NOT EVALUATED with the reason rather than failing the report; pooling refuses replicates scored over different windows with a plain message; the two onboarding uploads are stored under fixed names; a corridor name whose preset or OSM extract already exists is refused at POST time, the extract and preset are installed by atomic rename and removed if the job fails so the name can be retried; corridor names are matched in full; duplicate station ids are rejected; the report's observed coverage (link-hours, speed cells, excluded stations) is shown in the dashboard's report list.
- Dashboard, after a 14-step browser walkthrough against a local API: failed runs show the service's reason (run detail panel and row tooltip); demo rows when the API is offline carry a DEMO tag, no fabricated hash and no animated progress; an OSM (onboarded) preset is read-only in the composer instead of being downgraded to a 10 km one-lane corridor; both launchers and `POST /runs` refuse a duration that leaves no measurement window after the warm-up (422 with the two numbers); the runs launcher lists presets; the onboarding form states every failing field at once; the scenario library refreshes on reconnect and the shell names the corridor once an onboarding finishes; a reconnect race that let an in-flight demo read overwrite live rows is closed.
- MnDOT batteries (honest outcome): two 20-seed cloud rounds, both recorded under `artifacts/mndot_rounds/`. Round 1 (no acceleration lanes) ran in free flow with four starved entrances (GEH pass 17 %, RMSPE 93 %); round 2 (acceleration lanes, chain ended before the widening) gridlocked in every seed (41 % of planned vehicles departed) because SUMO's lane-change merge locks at the three large downtown-approach entrances — the I-24 merge limitation at a corridor whose peak sits at the discharge the model cannot reach. No validated baseline and no sweep exist for this corridor; `docs/ONBOARDING_MNDOT.md` §6 states what would be needed next. A lane-profile-versus-inventory check (`CorridorBuild.lane_check`, the onboarding summary, `--fail-on-lane-mismatch`) and per-seed insertion diagnostics with an early abort in the battery now catch both failure modes before an hour of cloud time.
- Battery insertion diagnostics: every replicate records planned/departed/arrived vehicles and per-entrance departure fractions (`validation.battery.insertion_stats`, verdicts `ok` / `backlog` / `starved ramps`), the validation artifact pools them, the report prints one computed Insertion line, and `scripts/corridor_battery.py --abort-if-departed-below F` stops the pool after the first finished replicate (exit 4, partial artifact marked aborted) — the two MnDOT failure modes would have cost minutes instead of an hour of 20 seeds each.
- Merge models on guessed acceleration lanes: when netconvert adds the lane along a short attach edge and runs it into the next edge's lane 0, the merge branch now ends it with a connection patch applied to the compiled net (`networks.accel_lane_end`, `lane_end_patch_file`, `patch_net`; `meta.json["ramps"][*].acceleration_lane_terminated`) so `scripted`, `zipper` and `acceleration_lane` apply; a lane that feeds a weave or runs on as a through lane is refused with the lane counts. On the MnDOT peak slice (a session run) this took the scripted merge onto four of seven entrances; the three that lock (a weave into an exit, a long added through lane at T.H.61 NB, a two-lane exit piece) are outside every current merge model, and the slice still gridlocked — recorded in `docs/ONBOARDING_MNDOT.md` §6.
- `scripts/doctor.py`: a preflight that reports Python, the SUMO pin, pyarrow, netconvert, Redis, writable roots, every preset and its files, disk, memory and a 10-second ring run with a fix for each failure; `docs/QUICKSTART.md`, the ten-minute clone-to-report path with a symptom/cause/fix table.

MnDOT I-94 westbound, east St. Paul (chosen from a coverage-and-congestion scan of twelve Twin Cities corridors on 2026-09-16)
- Stations S1063 (west of the I-494 east junction) to S97 (I-35E east junction): 10.0 km of inventory, 14 mainline stations at 0.77 km mean spacing, 17 ramp nodes, 3–5 lanes; nine weekdays (2026-09-01 … 09-17), 05:30–09:30 local, 5-min windows; every mainline station 100 % valid over the span (`data/mndot/mndot_i94_wb_stpaul/observations.json`).
- `scenarios/mndot_i94_wb_stpaul.yaml`: 11.5 km chain of 29 corridor edges, 31 of 31 stations placed within 22.1 m; demand from S1063 (peak 4,275 veh/h), 16 ramps (8 from live detectors or their shares, 7 by conservation, 1 outside the observed span set to zero), boundary from S97; driver population `artifacts/idm_i24_capacity.json` (the I-24 fit, stated as such — no Minnesota trajectory data exists to refit). Three ramp detectors read zero and one "exit" pair reads more than the mainline (a collector–distributor split), which is why ramp flows close the balance rather than trusting detectors alone (`artifacts/demand_mndot_i94_wb_stpaul.json`).
- Fundamental diagram `artifacts/fd_mndot_i94_wb_stpaul.json` from 300k per-lane one-minute samples: v_f 26.9 m/s, w −2.8 m/s, ρ_jam 0.210 veh/m, ρ_c 0.0199 veh/m, q_max 0.536 veh/s (fitted on a seeded 50,000-row subsample; bootstrap CIs in the artifact). A fit from 5-min station means fails the plausibility check (flat congested branch) — the loader keeps the raw cache for this reason.

### 2026-09-20 — CI builds the image on release tags

- The workflow's `docker` job was guarded by `refs/tags/` but the push trigger listed only `main`, so no tag ever reached it: v2.1.0 and v2.2.0 shipped without a CI image build (no run exists for either tag). The push trigger now includes `v*` tags and the workflow accepts a manual dispatch, which also builds the image. The v2.2.0 image was built once by a manual dispatch of the fixed workflow on the release commit (see the run linked in the release notes).

## [2.2.0] — 2026-09-20

**Release summary.** The I-24 MOTION flagship's record, its 500-run controller sweep, the headway-cap sweep and the published reports now share one metric definition and one hash policy, and the service has been through two adversarial production-readiness audits. Every number in the documentation traces to a committed artifact regenerated in this release.

Validation and research
- Warm-up is discarded from every metric (it was documented and never applied; 2 to 13 percent bias on throughput and travel time); travel times come from a median span with the sample size recorded instead of one vehicle; the report scores the wave-speed criterion with the profile's own detector and lists every run's versions. The canonical I-24 arms, US-101, the 500-run sweep and the cap sweep were re-simulated under these definitions on self-deleting cloud VMs: every criteria row reproduced to the digit, and the conclusions stand (5 of 7 rows on each congested arm; smoothing by gap-keeping is paid for in capacity; the headway cap is not the lever).
- The Old Hickory merge is recorded as a model-form limitation with its residual stated (about 5,880 veh/h discharged where the recording sustains 6,630). Six rounds of merge levers, a coverage-weighted scoring target, the ramp demand level, a lane-placed heavy population, the diverge and a merge-zone recalibration of the driver population each excluded a cause with a committed artifact. New engine options from that work: a scripted late merge, heavy vehicles placed by lane, entry lane shares measured as flow, and sub-corridor IDM fits.

Service and dashboard
- Scenario file paths confined to allowed roots (API and worker); request bodies and uploads capped, chunked bodies included; errors carry no frames, paths, file contents or input values; request ids and an access log; key rotation without downtime; jobs survive a dead worker or Redis through reconciliation; sweeps resume without duplicate runs; reports carry a criteria profile; the image ships its presets; OpenAPI can authenticate.
- The dashboard was driven end to end in a browser: a rejected key now surfaces, demo data is labelled and never written to, runs and sweeps launch deliberately with confirmation, reports download with figures and PDF, validation errors are readable, and the criteria profile is selectable.

Operations
- docs/DEPLOYMENT.md, the pilot runbook. Cloud rounds run on self-deleting VMs with an on-VM idle guard and a launcher that deletes an instance whose launch fails; the CI perf workflow re-verifies the performance targets.

Full detail: CHANGELOG.md, section 2.2.0.

The dated entries below are this release's record, newest first.


### Sweep and cap sweep re-run under the corrected metrics (2026-09-19)

- VM `flowstate-sweep` (n2-standard-32, us-west1-b; 7 h 32 min of work, about twelve dollars; idle guard armed, self-deleted after the bucket copy): the 500-run penetration × compliance battery on the fitted arm (`artifacts/i24_sweep_summary.json`, now under hash-policy-v2 base `43def6306dd6`, the arm's own hash) and the headway-cap sweep resumed from its three recovered configurations (`artifacts/i24_cap_sweep_summary.json`), both with the corrected metric definitions (warm-up discarded, travel times over the measured span). Every number in docs/I24_SWEEP.md, the dossier's sweep table and the README's sweep line is re-derived from the new summaries and the dose-response figure is regenerated. The baseline now reads 5,839 veh/h, 590 s, 12.9 waves (the same values as the validation battery's table for the same arm); at 5% / 100% FollowerStopper costs 38% of throughput (was 36%), doubles travel time (+102%, was +82%) and fuel (+111%, unchanged: a whole-run ratio), and cuts σ_v 59% (was 56%). The conclusions of both documents stand: smoothing by gap-keeping is paid for in capacity, and the headway cap is not the lever. The sweep's 500 per-run metrics and the cap sweep's 120 are installed under `runs/` for re-analysis.

### Cloud VMs can no longer sit idle (2026-09-18)

- The relaunched sweep VM's data transfer was cut by a connection reset; the launcher stopped, and the created instance sat idle for 9.5 hours (about fifteen dollars) until the next human turn. Two guarantees now hold, both on the VM side: `scripts/gcp/idle_guard.sh` runs as root from the boot script of every pipeline VM and deletes the instance (power-off as the fallback) whenever no pipeline, setup or SUMO process is running after a 75-minute grace, checked every five minutes with two strikes; and `scripts/gcp/launch_i24_pipeline.sh` deletes the instance itself if any step after creation fails, retrying the transfer three times first. The guard was installed by hand on the running VM. Neither depends on the laptop being awake.

### Sweep re-run: first attempt lost to an archive omission (2026-09-18)

- VM `flowstate-sweep` (n2-standard-32 in us-west1-c after us-west1-b and -a had no capacity; 7 h 50 min, about twelve dollars) ran the 500-run penetration sweep to completion (6 h 33 min, 0 failed) and 3 of the cap sweep's 6 configurations before its boot cap ended it. The sweep's per-run metrics live under `runs/i24_sweep/`, which the archive rule did not include, and its summary is built by `scripts/i24_penetration_analyze.py`, which was not a stage: nothing of the sweep came back. The cap sweep's 60 per-run metrics did and are installed locally so the next VM resumes from them. Fixed: the archive lists the sweep's metrics and manifest, `analyze_sweep` runs on the VM, the launcher ships any resumable metrics, and the cap is 660 minutes. docs/LESSONS.md row 24. The round is relaunched.

### Cloud round, part three: the merge-zone population and the regenerated reports (2026-09-17, evening)

- VM `flowstate-r3` (n2-standard-32, 1 h 50 min, about three dollars; self-deleted). **The discharge-capacity question is answered (docs/I24_VALIDATION.md §0.12):** the IDM population refitted on the merge zone alone (`scripts/fit_idm_i24.py --x-range 750 2500 --tag merge`, 4,193 episodes, holdout gap RMSE 4.53 m against 5.29 m corridor-wide; `artifacts/idm_i24_merge.json`) keeps a 4.6% longer time headway, its closed-form capacity is 1,720 veh/h/lane against 1,788 corridor-wide and 1,986 capacity-scaled (`artifacts/idm_i24_capacity_equilibrium.json`, with the grid's simulated-to-closed-form factor of 0.86 to 0.91 recorded), and the fitted arm driven by it discharges 5,246 against 5,748 veh/h at the peak sections (`artifacts/i24_merge_experiment_mergefleet.json`). The missing capacity is in the merging process, not in car-following; the model-form limitation of §0.11 stands at the calibration level.
- The five canonical batteries and both US-101 arms re-ran once more and reproduced every criteria value and metric mean to the digit (artifacts unchanged); the published reports under `docs/reports/` were regenerated from the full 20-replicate run sets before pruning, under the corrected metric definitions with the criteria profile, the profile's wave detector, every run's versions and NOT EVALUATED rows. `scripts/i24_extract_episodes.py --positions` writes the episode-position sidecar (`data/`, gitignored; archived with the round); `scripts/i24_calibrate_capacity.py --equilibrium`; `scripts/i24_merge_experiment.py` `_fleet<tag>`; pipeline stages `merge_positions`, `merge_fit`, `merge_eq`, `probe_mergefleet`, `reports_0917`, `reports_us101` (the report stages sit beside their batteries, before the prune).
- Process: the round was run entirely on the cloud after the laptop's forced shutdown of the afternoon (three concurrent data-heavy agents on 16 GB; docs/LESSONS.md row 23). The 500-run sweep and the cap sweep re-run on the next VM of the chain.

### Dashboard: criteria profile on reports (2026-09-17, evening)

- The reports view has a criteria-profile selector fed by `GET /api/v1/criteria` (the registry's default preselected, each option titled with its source), sends the chosen profile with `POST /reports`, and shows the profile on every report row (server rows carry it; local records store it). Against a service that predates the endpoint (404) the selector degrades to a single `fhwa_default` option and the POST omits the field. The demo mock serves the registry and refuses an unknown profile like the API. 91 vitest cases across 12 files.

### Merge round six closed: candidates 3 and 4 probed on the cloud (2026-09-17)

- VM `flowstate-probe` (24 minutes, about sixty cents): `artifacts/i24_merge_experiment_ohlevel.json` (Old Hickory × 0.55 / 0.75 / 1 / 1.25 / 1.6 on the flow family's fitted arm) and `artifacts/i24_merge_experiment_heavylanes.json` (no heavy population / uniform / placed by lane on the canonical fitted arm). Below the fitted ramp level the corridor free-flows; above it admittance saturates near 2,240 vehicles and the peak sections stay at 5,860 to 5,890 veh/h against the 6,225 / 6,238 needed for GEH 5. The lane-placed heavy population moves segment speeds by at most 2 km/h and lowers admittance. Both candidates excluded by their pre-registered deciders; all five closed. docs/I24_VALIDATION.md §0.11 records the merge as a model-form limitation with its residual (about 5,880 veh/h discharged where the recording sustains 6,630) and points the next work at the calibrated population's discharge capacity under merging.

### Cloud round, part two: the canonical arms and US-101 re-run under the corrected metrics (2026-09-17)

- VM `flowstate-canon` (n2-standard-32, 1 h 15 min, about two dollars; self-deleted): the five I-24 arms (`artifacts/i24_validation_{tracked,corrected,speedcal,ramps,speedcal_heavy}.json`) and both US-101 arms (`artifacts/us101_validation_calibrated.json`) re-simulated with their committed seeds. Every I-24 criteria row reproduces to the digit; the metric tables now carry the corrected definitions (fitted arm: throughput 5,839 veh/h, mean travel time 590 s, σ_v 4.94 m/s, 12.9 waves per replicate against 5,710 / 564 / 4.98 / 14.2 before). The four 2026-09-05 artifacts carried hash-policy-v1 hashes; the re-run records the v2 hashes of the same configs (mapping in docs/I24_VALIDATION.md §0.1). US-101's speed and wave rows are re-scored on the windowed field and move within a point with unchanged verdicts. The I-24 sweep and cap-sweep summaries (`i24_sweep_summary.json`, `i24_cap_sweep_summary.json`) are not re-run: their comparisons are internally consistent under the old definitions and are labelled as such. First-seed replicates of every arm are kept locally for figures; the flow and canonical archives are deleted with the bucket at the end of the round.

### Merge round six, candidate 5 closed on the data (2026-09-17)

- `scripts/i24_diverge_lanes.py` (`artifacts/i24_diverge_lanes.json`, 15 synthetic tests): the lane split over the kilometre upstream of both off-ramps, the fragments that exit and the lanes they use. The recording's exit shares equal the replica's planned ones in the builder's units (8.98% against 9.13%, 5.26% against 5.29%), exiting vehicles enter the ramp lane from lane 4 only and only in the last 500 m, and the replica's right-lane deficit decays with distance from the Old Hickory merge and is gone before the diverge. No diverge mechanism to test; the downstream split is the merge queue's downstream face. Candidates now standing: 3 (Old Hickory level, probe grid widened) and 4 (lane-placed heavy population); their probes run on the next VM of the chain.

### Engine: heavy vehicles placed by lane (2026-09-17)

- **Schema:** `HeavyVehicleSpec.lane_shares` (per-lane departure distribution for heavy vehicles, hash-neutral when unset; drawn from an independent seeded stream so light vehicles' draws are unchanged with or without it). `meta.json.heavy_lane_shares` records the effective shares. `microsim.vehicles.heavy_lane_shares_from_artifact` derives the I-24 distribution (0.048 / 0.172 / 0.459 / 0.321 left to right) from the measured per-lane heavy fractions and the entry flow shares. Nine tests including a SUMO run whose written routes and first-seen lanes follow the shares. The candidate-4 probe (three single-seed variants on the fitted arm) follows.

### Cloud round, part one: the flow-share family through the FHWA sequence (2026-09-17)

- VM `flowstate-flow` (n2-standard-32, 1 h 45 min, about three dollars; archive by bucket, self-deleted): `scenarios/i24_replica_flow{,_corrected,_speedcal,_speedcal_ramps}.yaml`, `artifacts/demand_scale_i24_flow.json` (scale 0.80), `artifacts/i24_boundary_ramps_fit_flow.json` (Old Hickory × 0.75, Hickory Hollow × 1.25, exit × 1.125), and 20-seed batteries `artifacts/i24_validation_flow_{corrected,speedcal,ramps}.json`: GEH 17.4 / 21.5 / 21.5%, RMSPE 33.7 / 37.2 / 41.8%, wave speed 15.4 / 15.9 / 15.8 km/h, 5 of 7 rows each. Against the canonical family: one to three points better on flow, unchanged to seven points worse on speed. Not adopted; the canonical family stays the record (docs/I24_VALIDATION.md §0.10). At thirty processes a 20-seed battery of the replica takes twelve minutes, which resets the cost model of these rounds. The global CPU quota (32) allows one such VM at a time; the canonical re-run launched automatically when this one finished.

### Merge round six, candidate 3 data half: no coverage estimate for the ramp lane (2026-09-17)

- `scripts/i24_coverage_lane5.py` (`artifacts/i24_coverage_lane5.json`, 28 synthetic tests): the gap-mixture estimator is not identified on the Old Hickory acceleration lane (79.5% of its tracked flow merges out along its length, spacing dispersion above the model's ceiling in 14 of 39 speed classes, 1.7× scale drift), and a synthetic lane with only the measured merge-out reproduces the confounded readings; the coverage is bounded to [0.327, 1.0], which puts the implied Old Hickory demand multiplier in [0.533, 1.63]. The plan's probe levels did not bracket it; the grid is widened to {0.55, 0.75, 1.0, 1.25, 1.6}. docs/I24_DATA.md §4's mainline coverage range corrected to the capacity population's 0.481 to 0.605.

### Merge round six, candidate 4 data half: proceed (2026-09-17)

- `scripts/i24_heavy_share.py --by-lane` and `calibration.lanechange.heavy_share` / `aux_lane_fragments` (synthetic tests): heavy share by lane and by acceleration-lane use, `artifacts/i24_heavy_by_lane.json`. Lane 3 carries 19.0% heavy fragments (14.4% of vehicle-time), lane 1 1.4%, against the corridor's 9.2%; ramp-origin traffic is at the corridor share. Exceeds the plan's 3-point band, so the candidate proceeds to a lane-placed heavy population (schema, capacity calibration and probes to follow the cloud round).

### Merge round six, candidate 2 closed (2026-09-17)

- Data only, no cloud cost: the coverage-weighted scoring target (`scripts/i24_weighted_target.py`, synthetic validation in `tests/test_calibration/test_i24_weighted_target.py`, `artifacts/i24_validation_weighted_target.json`) moves the fitted arm's 5-min RMSPE by +0.54 points (35.95% to 36.49%), an order of magnitude inside the replicate-noise floor; the target itself shifts by 1.52% RMSPE. The criterion keeps the unweighted target; docs/I24_VALIDATION.md §0.9a, docs/MERGE_ROUND6_PLAN.md addendum.

### API and worker: the regression review's items closed (2026-09-17, later)

- **One travel-time span per run on the API path, for OSM corridors too.** The runner records the corridor geometry as built (`meta.json["corridor"]`: kind, total length, start of the corridor proper) and `api.results.analysis_span` derives the same span for every replicate of an OSM run (up to the exit-buffer edge when a boundary schedule is hosted there); runs written before the block existed keep the observed-extent behaviour. The per-replicate cache schema is 3, so every OSM cache is recomputed. The report job reads the span from a replicate's meta, so an all-OSM run set gets one span there as well.
- The pool wrapper no longer re-embeds a failed replicate's message (a pydantic ValidationError quoting the file it validated), so stored run errors keep only the seed and the exception type with the scrubbed cause chain. The store's column migration tolerates the concurrent-creation race by re-reading the table after a failed ALTER, and its WAL conversion waits out the cold-start lock that made one of two processes creating the same fresh database fail at once. `CalibrationParams` exposes the fitter's `max_fit_rows` (null = every row), `max_dropped_fraction`, `max_speed_ratio_factor` and `max_out_of_range_fraction`. 1,238 non-slow tests, coverage 95.9%.

### Dashboard: the regression review's items closed (2026-09-17, later)

- Write actions never fall back to the in-browser demo backend: during an outage `createScenario`, `createRun`, `createSweep` and `createReport` reject with "API offline, reconnect before launching (nothing was sent to the server)", and the Launch run, Launch sweep, Generate report and Run buttons are disabled with that title (reads still fall back so the demo dashboard renders). A queued real report can no longer be reported done from demo data: the report polls capture their source at call time, never persist or toast a demo-derived status, and demo rows carry a DEMO tag; an unknown report id is a transient error in the mock, never minted as done. The confirm dialog's Escape handler reads the current cancel callback through a ref with both effects mount-only. 83 vitest cases across 12 files, typecheck and build green.

### Regression review of the day's changes: confirmed and carried (2026-09-17)

A final adversarial review of everything above (four reviewers, every finding attacked by a skeptic: 6 confirmed, 6 refuted) ran out of the block's time budget before fixes could be built and reviewed. The confirmed items are recorded here with the reviewers' evidence kept in the session's scratch and are the first work of the next block; none of them changes a published headline number:

  - (high, `packages/api/api/results.py:243`) OSM corridors (the I-24 flagship) get no geometric span, so every replicate's travel time is measured over a different distance and the CI is a measurement artifact.
  - (medium, `packages/microsim/microsim/runner.py:1769`) Pool wrapper re-embeds the child exception's message, defeating the ValidationError input scrubbing in stored run errors.
  - (medium, `packages/api/api/store.py:163`) The new `profile` column migration is TOCTOU — `duplicate column name: profile` crashes Store() when the API and worker containers start together on an upgraded database.
  - (high, `frontend/src/components/ConfirmDialog.tsx:34`) ConfirmDialog re-focuses its confirm button on every parent render, defeating the new launch gates.
  - (high, `frontend/src/views/ReportsView.tsx:243`) ReportsView polls reports through the offline demo fallback, so a queued report is reported done and badged SERVER from invented data.
  - (medium, `frontend/src/api/client.ts:342`) Write actions fall through to the in-memory demo backend during an outage and report success.

Two of the six were fixed before the block closed: the confirm dialog focuses its button once on mount (it re-focused on every render, so an Enter keypress could confirm a launch the user was dismissing), and the store's column migration tolerates the "duplicate column" race when the API and a worker open the same database at once. The most consequential one: on the API and sweep path, travel times are still measured over a per-replicate span (the median furthest position of that replicate's vehicles), so a heavily congested controlled arm whose non-completion share crosses one half gets a shorter span than its sibling replicate and reports a *lower* travel time (a 10% FollowerStopper cell on the fitted I-24 arm: 1,787 s over a 6.7 km span against 2,633 s over the full 8.6 km; the baseline replicates are unaffected). The report deliverable already derives one span per run set and is not affected. The fix belongs in the worker: one span per run, recorded in the run's meta and reused by every replicate.

### API and image after the second audit (2026-09-17)

- **Reports carry a criteria profile.** `POST /reports` takes `profile` (default `fhwa_default`, validated against `validation.criteria.CRITERIA_PROFILES`, 422 naming the choices), stores and echoes it, and `GET /api/v1/criteria` lists every profile with its source, thresholds and wave detector. Rows whose inputs the service does not have (observed counts and speeds) are reported as not evaluated, never as FAIL; no measurement is accepted from a request (CLAUDE.md §7.4).
- **Travel times on the product path** use a span derived from the corridor geometry (from the insertion buffer to 100 m before the exit) instead of the degenerate default; the ring reports no travel time (a loop has no traversal); the per-replicate metrics cache schema is 2, so caches written under the old definitions are recomputed.
- **One baseline cell per sweep.** `include_baseline` appended one uncontrolled cell per controller, which ran identical simulations and, with two "baseline" groups, removed the report's contrast table; now a single `(0, 1.0, none)` cell, skipped when the grid already holds an uncontrolled cell, and identical grid triples are de-duplicated.
- **The image can run its presets.** The Dockerfile never copied `artifacts/` (13 of 17 shipped presets failed at run time in the container); it copies `artifacts/` and `data/osm/`, with `.dockerignore` keeping the local datasets out.
- **Operability.** OpenAPI declares the `X-API-Key` scheme (the interactive docs can authenticate) and the 401/404/413/503 responses; `reconcile_store` re-reads the started registry before failing a row so a job that started between the listing and the check is not killed; a re-run report cleans its staged directory (the previous attempt's hard links raised `SameFileError`); empty, header-only or non-numeric calibration CSVs fail with a FlowState-authored message naming the column and row, never the cell value. The runbook states the error-text guarantee exactly: no frames, no server source paths, no file contents, no pydantic input values; a results-root path may appear.
- The report's criteria table renders a row the run set could not evaluate as NOT EVALUATED (with its reason) instead of FAIL, so an unevaluated row can no longer be counted as evidence either way.
- Carried: `api.schemas.CalibrationParams` does not yet expose the fitter's new `max_fit_rows`, `bounds` and dropped-fraction knobs (the defaults apply).

### Metrics, report, runner and calibration after the second audit (2026-09-17)

The second audit (six lenses, 24 confirmed findings after adversarial verification, 5 refuted) reached the numbers themselves. What changed, and what it does to published values:

- **Warm-up is now discarded from every metric.** `SimSpec.warmup_s` was documented as "discarded from metrics" and applied nowhere; the audit measured a 2 to 13% bias on throughput and travel time and +57% on wave count for the I-24 arms. `compute_metrics(..., warmup_s=None)` takes the run's own warm-up (0.0 = the whole record); throughput counts crossings after it, σ_v, VMT/VHT and the wave field use rows after it, travel times use whole journeys that begin after it, fuel per vehicle-km keeps a whole-run denominator until the runner records a post-warm-up fuel total. A warm-up that covers the whole run raises instead of measuring the warm-up. The report's figures and criteria use the same window and state it.
- **Travel times no longer come from one vehicle.** The default span's exit bound was the farthest point any vehicle reached, so `mean_tt_s == p90_tt_s` from a single vehicle in every micro golden; the default is now the median of the vehicles' furthest positions, `Metrics.n_travel_time_veh` records the sample, and fewer than two completers gives NaN rather than a "mean". The report derives one span for the whole run set so controller and baseline are compared over the same distance. On the ring benchmark no vehicle completes a whole journey after the warm-up, so its travel-time fields are null (a capability loss on that case, recorded).
- **The report's wave-speed criterion is scoreable.** The generator measured with the `standard` detector while every profile scores with the `stack` detector, so the flagship row was always "not evaluated"; it now measures each unseeded reference replicate with the profile's detector on that detector's bins and says how many replicates contributed ("mean over 2 of 4 ... 2 detected no backward front"). Provenance lists every run's package and SUMO versions (not the first run's) and warns when they differ.
- **Runner.** `edges.parquet` density and flow were scaled by the nominal `1/output_hz` instead of the realized cadence after step rounding; fixed, and `meta.json` records `output_hz_realized`. A killed replicate worker no longer hangs `run_replicates` forever: a bounded wait fails the run naming the seed and cleans up SUMO children. A replicate is complete only when its `meta.json` is written last; `is_run_complete` / `require_complete_run` refuse a footer-less `trajectories.parquet` with a clear message.
- **Calibration inputs.** The fitted diagram is range-checked (free-flow speed, capacity, jam density, wave speed within named per-lane bounds; refusal names the offending value); non-physical rows (negative, zero density with flow, occupancy above 100%) are dropped and counted in the artifact; the fit subsamples above `max_fit_rows` (50,000, seeded) and bounds the bootstrap payload; the loader cross-checks flow over density against the reported speed. `FDCalibration.schema_version` 2 with defaulted new fields; old artifacts still load. `scripts/fit_fd_i24.py` passes `max_fit_rows=None` so the published I-24 diagram (236,717 rows) stays reproducible.
- **Goldens regenerated** (all micro cases and the two macro cases; the two existing cases' counts are unchanged and their metric values moved only through the corrected definitions above; the smoke case gained `sim.warmup_s: 0.0` because a 120 s run with a 120 s warm-up has no window).
- Known and carried: the published reports under `docs/reports/` and the artifact metrics of the I-24 and US-101 batteries were computed under the old definitions (whole-run window, one-vehicle travel times) and are not regenerated here; `scripts/i24_cap_sweep.py` and `scripts/i24_lane_profile.py` still detect a replicate by its `trajectories.parquet`; the calibration and sweep scripts' own pools keep the unbounded `Pool.map`.

### Dashboard, second pass (2026-09-17)

- The reports table runs on `GET /api/v1/reports` (server rows newest first, merged with the browser's older local records, each row badged SERVER or LOCAL; an API without the endpoint falls back to local records and says so). Metric cards, the sweep matrix and the run detail render the three CI states from the API: quotable mean with interval, UNDERPOWERED, and "no observations" (no zero, no interval) when `reason = "no_observations"`. The PRESET badge follows the server's marker. `report_path` is never rendered. Duration and seed inputs are clamped (an emptied box means the scenario's own value, never `duration_s: 0`). The 401 latch retries once automatically after 30 s and offers a Retry button. The demo mock emits the same three CI states and results-relative report paths. 72 vitest cases across 11 files, typecheck and build green.

### Merge round six, candidate 1 probed (2026-09-17)

- Cloud probe of the entry lane **flow** shares (three single-seed runs on an n2-standard-8, 11 minutes, about ten cents; `artifacts/i24_merge_experiment_entryflow.json`, docs/I24_VALIDATION.md §0.9): insertion 0.935 → 0.952, held-out RMSPE 0.457 → 0.406, fit hour 0.350 → 0.369, Old Hickory admittance 2066 → 2029 of 2,241, entry segments unchanged at 23 to 27 km/h. The right observable, a real but small gain, and not the merge defect; the next full FHWA sequence should be built on the flow shares. With the heavy population added every number is worse at this seed. The bucket-copy and self-delete path worked again; bucket deleted after ingest.

### API after the walkthrough (2026-09-17)

- `FLOWSTATE_CORS_ORIGINS` (comma-separated; `*` any, `-` none; default the two loopback dev origins) replaces the hard-coded `http://localhost:5173`: a dashboard served from any other origin could not call the API at all, and the only feedback was an "API offline" banner. A disallowed origin gets a response without the allow header, not a 400 that echoes the policy.
- `GET /api/v1/reports` (newest first, `limit` up to 200; `Store.list_reports`), so the dashboard's report table need not live in the browser; `ReportOut.report_path` is results-root-relative instead of an absolute server path; `POST /reports` refuses a run set that mixes micro and macro tiers with 422 naming the macro runs (the generator listed the screening run in the report's provenance without a word; all-macro was already refused); `PresetOut.preset: true` / `ScenarioOut.preset: false`; a CI with no observations (`n == 0`, for example wave speed with no detected wave) reports `mean/lo95/hi95 = null`, `underpowered = false` and `reason = "no_observations"` instead of an underpowered estimate. 15 new API tests (239 in the package).

### Dashboard after a real browser walkthrough (2026-09-17)

An agent drove the dashboard through the pilot flow in Chrome against the inline-queue API (14 steps, screenshots kept locally) and found five pilot-blockers; all fixed in the dashboard, 57 vitest cases across 10 files, typecheck and build green:

- **A rejected API key no longer looks like a healthy link.** 401/403 on any call latches an auth-failed state: a red banner with an Open Settings button, the status rail reads KEY REJECTED instead of a green API LINK (the health route is auth-exempt, which is why the dot was green while every call failed), and every poll pauses until the key changes. Previously: an empty dashboard, "loading" forever, and 95 retries a minute.
- **Demo data is labelled as demo.** The offline fallback showed fabricated presets with fake config hashes that looked real and never refreshed once the API returned. Now: a DEMO badge, dashed cards, "demo, no server hash" in place of a hash, Run disabled, and the library keeps polling so the real presets replace the demo list in place.
- **Runs are launched deliberately.** Duration, seed and replicates fields on the launcher (sent as an `overrides` patch), a confirmation naming scenario, tier, replicates and total sim-minutes past 600, and the preset card's one-click Run (which fired 20 full-length replicates) became a launcher. The composer carries every field it does not model through unchanged and says so, instead of silently replacing the demand profile and dropping calibration, boundary and closure fields.
- **Sweeps default to 5 replicates per cell with the 20-seed standard stated,** a confirmation lists cells and total runs before the POST, the baseline header no longer names a controller for penetration 0, and cells whose whole aggregate vector is identical to another cell's are marked with a note.
- **The report can be downloaded.** The markdown download revoked its object URL synchronously and left an unfinished `.crdownload`; fixed, and the archive (zip with figures) and PDF routes that the API already served are now offered. Validation errors render as `field: message` lines instead of raw pydantic JSON; replicate and lane limits mirror the API's; run tables show scenario names, and the PRESET badge is derived from the endpoint the item came from.

### Hardening round two: chunked-body caps, key rotation, request ids, worker-side path roots, macro closure golden (2026-09-17)

- **Chunked bodies capped (API).** A pure-ASGI `BodyCapMiddleware` counts bytes from `receive()` on every `/api/` request and answers 413 the moment the running total passes the cap (upload plus body cap on `/api/v1/calibrations/`, body cap elsewhere), so chunked JSON to `/runs`, `/sweeps`, `/reports` and chunked multipart uploads are bounded before Starlette spools them; nothing is enqueued for a refused request; unauthenticated oversized requests stay a plain 401. Tests drive the cap as a running total across chunks.
- **Key rotation without downtime.** `FLOWSTATE_API_KEYS` (comma-separated, trimmed, empties dropped) is accepted beside `FLOWSTATE_API_KEY`; any listed key authenticates (every key compared in constant time, no short circuit); the published default is refused anywhere in the list under the Redis queue; a blank key is unset, never a usable empty key. `docker-compose.yml` forwards the variable; docs/DEPLOYMENT.md has the add, redeploy, move clients, remove flow.
- **Request ids and an access log.** Every response carries `X-Request-Id` (a client-supplied id of at most 64 `[A-Za-z0-9._-]` characters is reused, else a uuid4; exposed through CORS); one INFO line per request on logger `api.access` (method, path, status, milliseconds, request id), reaching stderr under uvicorn through a handler the app installs; unhandled errors answer `{"detail": "internal error; request id ..."}` with no traceback (the traceback goes to the `api` logger).
- **Worker-side path confinement (defence in depth).** New `microsim.paths` (roots from `FLOWSTATE_WORKER_PATH_ROOTS`, `os.pathsep`-separated; unset means unrestricted, the library default for scripts); `microsim.vehicles.resolve_calibration_path` / `load_idm_calibration` and `microsim.networks.osm_import` take `allowed_roots` and refuse a resolved path outside them with a message naming only the value and the roots; `api.jobs.run_scenario_job` publishes the API's roots around the run and restores the environment after, and the spawned replicate pool inherits them (tested through a real 2-replicate pool). `RemoteTraceback` text from pool workers is withheld from stored errors. Still API-only: the calibration `data_path` read.
- **Macro-tier closure golden.** `tests/golden/macro_corridor_workzone.json` pins the CTM work-zone treatment (exact closure counters, a counterfactual without the closure); `tests/test_macrosim/test_macrosim_golden.py --regenerate <case>` is per case.
- **Artifact schema bump refused, with the reason.** The four canonical validation artifacts (`tracked`, `corrected`, `speedcal`, `ramps`) lack six schema-6 paths that the heavy arm's battery wrote (`simulated.segment_speed_*` detail), so `--criteria-only` keeps them at schema 5; a bump would have to come from a re-run battery, not a re-score. docs/ROADMAP.md notes this as a permanent fact of the 2026-09-05 battery.

### Merge round six: the plan (2026-09-17)

- `docs/MERGE_ROUND6_PLAN.md`: the defect as the artifacts now define it (GEH 18.8% and RMSPE 35.9% on the fitted arm; a 12 to 13% discharge shortfall at the two peak sections; one kilometre at Old Hickory 10 km/h slower than the recording with the lane profile inverted), what rounds one to five excluded with the artifact for each, five remaining candidates ranked with the exact experiment, observable, pre-registered threshold and cloud cost (about ten dollars for the round; three steps are data-only), a decision rule in FHWA terms, and the list of what not to retry. Lead candidate: `--entry-lanes observed` counts 5 Hz samples, so the replica's `entry_lane_shares` is a share of vehicle-time imposed as a share of flow 2.3 km upstream.

### Performance targets re-verified on CI (2026-09-17)

- The `perf` GitHub Actions workflow (slow-marked tests, ubuntu runner; run 35187702992 on commit 379c3f2, artifact `perf-results-35187702992`) passes every CLAUDE.md §3.4 target: `corridor_10km` 20 sim-min at ≥ 5× real time (4.0 s test wall time), `ring_sugiyama` 600 s at ≥ 50× (0.30 s), the 20-replicate sweep under 15 min (69 s), the Numba CTM kernel 1,000 cells × 10,000 steps under 1 s (0.71 s), plus the delayed-oracle JAD behaviour test and the full IDM population recovery (36 s). The workflow now installs `redis-server` because the API test package refuses to skip its Redis tests under `CI`; its first dispatch of the night failed at collection for that reason. Re-run on the day's final commit 354ddd5 (run 35193732861), after the runner's bounded pool wait and the metric window changes: all six slow tests still pass.

### Production-readiness round: API hardening, job recovery, dashboard contract, docs consistency (2026-09-16)

A two-stage audit (five auditors, every finding attacked by an adversarial verifier: 23 confirmed, 2 refuted) followed by fixes in five file groups, each reviewed. Test count 986 → 1,000+ non-slow; frontend 38 vitest cases, typecheck and build green.

- **Security (API).** Scenario configs could name any server file (`network.osm_file`, `fleet.idm_calibration`, `fleet.heavy.idm_calibration`): the worker read it and the run error echoed its contents. `POST /scenarios`, `/runs` (overrides) and every `/sweeps` cell now answer 422 (`type: path_outside_roots`) unless the path resolves inside the results root, the uploads dir, `FLOWSTATE_DATA_DIR`, or the repository's `artifacts/` and `data/`; relative paths resolve against the repository root, so the shipped presets keep working. Defence in depth: run/sweep/report error text is an exception chain without frames or paths, and pydantic validation errors are recorded without their input values (`api.jobs._error_text`). A non-ASCII `X-API-Key` answers 401 instead of 500. Request bodies are capped (`FLOWSTATE_MAX_BODY_MB`, default 8; calibration uploads `FLOWSTATE_MAX_UPLOAD_MB`, default 200; HTTP 413; uploads streamed in 1 MiB chunks, never read whole); calibration `params` are validated by `api.schemas.CalibrationParams` (extra forbidden, every numeric bounded). Hostile upload filenames land inside the upload directory. Request models `RunCreateRequest`, `SweepCreateRequest`, `ReportCreateRequest` forbid unknown fields.
- **Correctness (API).** `seeded` in run and metrics responses now comes from `ScenarioConfig.seeded` (perturbation *or* lane closures) instead of the perturbation alone, matching `meta.json` and the report. `POST /sweeps` gained `include_baseline` (default false; one penetration-0 / compliance-1 cell per distinct controller, counted toward the 200-cell ceiling, skipped when the grid already holds penetration 0).
- **Job recovery (worker).** RQ job ids equal store row ids (`JobQueue.enqueue(..., job_id=)`, unique enqueue); rows are claimed with a compare-and-set; `api.jobs.reconcile_store` repairs rows left at `queued`/`running` by a dead work horse, a SIGKILLed worker or a lost Redis (runs at worker start, on RQ's maintenance pass, and once at API start); the worker's `work_horse_killed_handler` fails the row of a killed horse. Sweep child rows are created in one transaction with the grid, and a re-run dispatches only cells still `queued`. Per-replicate metrics are precomputed by the worker at the end of every run and written atomically; `GET /sweeps/{id}` reads caches only (no trajectory reads in a polled request path) and `GET /runs/{id}/metrics` computes once only for runs from before this change.
- **Deployment.** `docker-compose.yml`: Redis append-only on its own volume (`redis-data`), worker with `init: true` and a 6 h `stop_grace_period` (a plain `docker compose down` lets a run finish; `docker compose kill worker` abandons it). Dockerfile installs all extras, so the deployed image renders report PDFs (`GET /reports/{id}/pdf` answers 200). CI installs `redis-server` and the Redis-queue tests refuse to skip under `CI`. Coverage now measures `packages/api/api` too.
- **Dashboard.** The sweep launcher sent `controller` (singular), which the API silently dropped: every dashboard sweep ran without its controller. Now `controllers: [..]` plus `include_baseline` (checkbox, default on), a labelled baseline row in the matrix, deltas against the penetration-0 cell (previously against the lowest AV cell), and a failed fan-out stops polling. Metric keys renamed to the `validation.metrics.Metrics` field names (a vitest contract test parses the dataclass); the default sweep metric is `sigma_v_spatial_ms`. Reports: the view polls `GET /reports/{id}` while queued/running, shows refusals and failures, and "Download .md" reads `/reports/{id}/markdown` (it previously saved the JSON status body). The demo-mode mock follows the same contract.
- **Docs.** `docs/I24_SWEEP.md` baseline paragraph rendered from the artifact (it was unrendered format placeholders). The I-24 GEH criterion row is quoted everywhere as 0.7 / 16.7 / 18.8 / 20.1% (recommended-coverage count table, the artifacts' primary rule) with the former 24.3 / 11.8 / 15.3 / 10.4% kept as a labelled own-assumption-table history row (README, ROADMAP, dossier, I24_VALIDATION, I24_CAPACITY); the dossier's reviewer Q&A says 5 of 7 rows like the rest of it; README's controller caveats match its results table; the sweep reproducer command and script defaults name `i24_replica_speedcal`.
- **Tests.** Boundary-schedule integration test (three steps applied, congestion arrives and clears on the exit edge); golden regressions for the work-zone closure, heavy vehicles, the three merge models, ramp metering and managed lanes (`tests/golden/`, summary statistics only; the two existing goldens regenerated with the new exact-compared counters, values unchanged to every digit); a checked-in interchange fixture `tests/fixtures/merge.osm`. The work-zone golden compares at 1e-4 relative instead of 1e-6: SUMO's Linux build (CI) and the macOS build differ at 2e-6 in that case's σ_v (the first CI run of this commit failed on it; the counts and every other case match to 1e-6).
- `docs/DEPLOYMENT.md`: the pilot runbook (the Compose stack, every environment variable, sizing from the M5 load test, backups of the runs and Redis volumes, key rotation, upgrade with the 6 h stop grace, job recovery and `rq requeue`, reading failures); linked from README and the docs index.
- Carried at the time and closed the next day (see "Hardening round two"): the chunked-body counter, the worker-side root checks and the macro-tier closure golden. Still open: the four canonical validation artifacts stay at schema 5 (they lack six schema-6 fields the heavy arm's later battery wrote; a re-score cannot add them).

### Scripted late-merge model and collision accounting (2026-09-16)

- **Schema:** `RampSpec.merge = "scripted"` and `RampSpec.merge_params` (keys `accept_gap_s`, `force_after_s`, `force_within_m`, `change_duration_s`, `lookahead_m`, `courtesy`; defaults in `flowstate_core.config.SCRIPTED_MERGE_DEFAULTS`, unknown keys rejected, hash-relevant). The network is the plain `lane_change` one (no netconvert patch); the runner drives every vehicle on the acceleration lane: desired speed matched to the mainline vehicle ahead through `setMaxSpeed` (never `setSpeed`, whose max-deceleration clamp overrides its safe-speed clamp in SUMO's influencer and produced rear-end collisions in the first draft), a lane change requested once the mainline gaps ahead and behind both clear the accepted time gap, a forced change (`laneChangeMode` 256, the follower yields) after a wait inside the last stretch of the lane, and optional courtesy yielding (the blocking mainline follower's desired speed is lowered until the gap opens). `meta.json` records `scripted_merges` (entered, changed, forced, unfinished, mean and p90 wait). `scripts/i24_merge_experiment.py` parses `_scripted` with `_accept<s>`, `_force<s>`, `_within<m>`, `_look<m>`, `_court<m/s>`; the cloud pipeline gained the opt-in stage `merge_scripted` (six single-seed I-24 variants against the reference arm).
- **Fixture result (tests/test_microsim/test_microsim_merge_managed_meter.py, 2-lane mainline at 0.6 veh/s plus a 0.25 veh/s ramp, 300 s, seeds 3 and 4):** no collisions in any variant after the `setMaxSpeed` change; ramp vehicles through the merge in 300 s: SUMO lane-change model 47 / 45, zipper 57 / 59, scripted 31 / 32, scripted with courtesy 2 m/s 36 / 37. On this congested fixture the scripted merge is slower than SUMO's own negotiation because the mainline no longer cooperates with a signalled change; whether it breaks the I-24 replica's right-lane lock (a different mechanism, docs/I24_VALIDATION.md §0.5) is the question the cloud probe answers. Not a headline result: single seeds, a synthetic fixture.
- **I-24 cloud probe (VM `flowstate-pipeline`, n2-standard-8, 22 minutes, about fifteen cents; `artifacts/i24_merge_experiment_scripted.json`, docs/I24_VALIDATION.md §0.8):** reference (SUMO lane change) against five scripted variants on the fitted arm, single seed. The scripted merge lifts the entry segment from 26 km/h to 36 to 41 km/h (observed 36) but admits only 1544 to 1638 of Old Hickory's 2241 planned vehicles against the reference's 2066, so insertion falls from 0.935 to 0.91 and the held-out RMSPE rises from 0.457 to 0.56 to 0.65. A negative result, documented; the fitted arms keep the lane-change model. The bucket copy plus self-delete path worked end to end (archive in the bucket 2 s after the pipeline exit, instance gone before the watcher's own delete); the bucket was deleted after ingest.
- `scripts/gcp/launch_i24_pipeline.sh`: `--allow-dirty` (the VM runs `git archive HEAD` either way) and the bucket-root parsing of the IAM grant fixed (`${BUCKET%%/*}` produced `gs:` and aborted the launch after the VM was created); `ingest_pipeline_results.sh` accepts the scripted-merge artifact; `bootstrap.sh` defaults to the published sweep arm.
- `meta.json` gains `n_collisions` (exact count of SUMO collision detections over the run; `--collision.action warn` keeps both vehicles, so a persisting overlap is counted every step) and `collisions` (the first 50 events: time, collider, victim, type, lane, position). Every scripted-merge test asserts zero.

### Business model, outreach list and the website's plain-English mode (2026-09-15)

- `docs/BUSINESS_MODEL.md`: Path A (corridor studies, agency pilots, a yearly workspace) and Path B (a live speed advisory on the agency's own signs or a partner's connected-vehicle channel, never a consumer app), the B2B sales order, unit economics and cost to build, with public anchors (HNTB and Kimley-Horn rate schedules, the USDOT ITS Costs Database, probe-data contracts, the I-24 SMART Corridor gantry cost, AMPO) and this repository's measured compute costs. Prices remain hypotheses and are labelled so.
- `scripts/business_cost_model.py` → `docs/business/flowstate_cost_model.xlsx` (Assumptions with every input, Cost to build, Unit economics A, Path B live advisory, three-year P&L with live formulas, Sources). `scripts/business_outreach_list.py` → `docs/business/outreach_targets.xlsx` and `.csv`: 200 organisations in seven segments (52 state DOTs, 45 MPOs, 35 consultancies, 28 labs, 12 federal and industry bodies, 12 fleets and OEMs, 16 data and sensor vendors) with the role to reach, the channel and a pitch angle; no personal e-mail addresses are invented.
- Website (repository `flowstate-site`): a Business page, a Business link in the primary navigation, and on the research pages a large button that switches to a playful plain-English version of the research (an overlay; the technical page stays intact underneath).

### Merge levers for the next round (2026-09-07)

- **Fourth merge round, fourteen single-seed probes (docs/I24_VALIDATION.md §0.7; `artifacts/i24_merge_experiment_{visibility,sublane2,keepright,overtake}.json`):** the zipper's interleaving distance is inert at 200–600 m (admittance 61–64%); the sublane model locks with internal links at one sublane per lane and at 0.8 m with pushiness and right alignment (runs normally for ten minutes, then loses capacity everywhere); keep-right off reproduces the references to the digit; overtaking on the right raises insertion to 95–96% and GEH to 21–22% while the 15-min speed error rises from 24.8% to 33–34%. SUMO's lane-change and junction parameter space is exhausted for this merge; the next design is a scripted late-merge behaviour for ramp vehicles, tested like a controller.
- **Schema:** `FleetSpec.lc_overtake_right` (SUMO `lcOvertakeRight`, the US passing rule), written when set.
- **Schema:** `RampSpec.merge_visibility_m` (zipper connection `visibility`, the interleaving distance; `microsim.networks.merge_patch_files(visibility_m=...)`) and the sublane fields `FleetSpec.lc_sublane`, `lc_pushy`, `lc_impatience`, `lc_accel_lat`, `max_speed_lat`, `min_gap_lat`, `lat_alignment` (SUMO `lcSublane`, `lcPushy`, `lcImpatience`, `lcAccelLat`, `maxSpeedLat`, `minGapLat`, `latAlignment`; `microsim.vehicles.sublane_vtype_attrs`). All `None` by default and hash-neutral when unset; docs/CONTRACTS.md. `scripts/i24_merge_experiment.py` parses `_vis<m>`, `_sublane<res>`, `_pushy<v>`, `_impat<v>`, `_acclat<v>`, `_latspeed<v>`, `_mingaplat<v>`, `_lcsub<v>`, `_latalign<mode>` variant suffixes in any order.

### Cloud pipeline for the calibration round (2026-09-06)

- `scripts/gcp/pipeline_i24.sh` (VM side, resumable stage markers, EXIT trap powers the machine off on success or failure): zipper junction time-gap sweep (`FleetSpec.jm_timegap_minor_s`, chosen on the fit hour), the `zip` scenario family (`scripts/i24_build_replica.py --suffix zip --osm corrected --lc-strategic-ramp 1 --entry-lanes observed --merge zipper --jm-timegap`), the FHWA sequence on it (`i24_fit_demand_scale.py --base-yaml/--scenario-out/--name`, `i24_fit_boundary_ramps.py --base-yaml/--scenario-out/--name`), the heavy arm (`scripts/i24_add_heavy_arm.py`), the 20-seed batteries (`i24_validate.py --family zip`, plus the canonical `speedcal_heavy` arm), and the headway-cap sweep (`scripts/i24_cap_sweep.py`, paired 95% intervals, trajectories pruned). `scripts/gcp/launch_i24_pipeline.sh` creates the VM with a boot-time hard cap (startup script `shutdown -h +300`), ships the data and starts the pipeline under systemd; `scripts/gcp/watch_pipeline.sh` fetches the archive, deletes the instance, never restarts it at the working size, and deletes it unconditionally at its own deadline.
- `FleetSpec.jm_timegap_minor_s` (SUMO `jmTimegapMinor`, written on every vType when set): the zipper's merged-lane lever. Builder options `--suffix`, `--lc-strategic-ramp`, `--merge`, `--merge-ramps`, `--jm-timegap`; validator `--family` and the `speedcal_heavy` arm; `scenarios/i24_replica_speedcal_heavy.yaml` (the fitted arm plus the recording's heavy share, `scripts/i24_add_heavy_arm.py`).
- **Second VM (21:33–00:55 UTC, 3.4 h at 32 vCPUs, about five dollars):** `launch_i24_pipeline.sh --bucket --self-delete --pipeline-args '--stages "battery_lost prune_lost cap_sweep rescore"'`; the archive reached the bucket after every stage and the instance deleted itself 90 s after its final archive; the bucket was deleted after ingest. The project's default compute service account needed `roles/storage.objectAdmin` on the bucket and `roles/compute.instanceAdmin.v1` on the instance (this project grants it no Editor role); the launch script now grants both.
- **Outcome of the first run (VM `flowstate-pipeline`, 08:55–13:57 UTC at 32 vCPUs, deleted 19:47 UTC; about eight dollars).** The calibration stages and both batteries completed by 11:31 UTC; the boot-time cap (300 min) killed the machine 2.4 h into the headway-cap sweep, and the archive — written only by the EXIT trap, to `/tmp`, which Debian clears at boot — did not survive (scripts/gcp/README.md, docs/LESSONS.md rows 14–16). Recovered intact from a cut-off interim transfer and installed: `artifacts/i24_merge_experiment_zipper_jm.json` (jm 0.5 s chosen on the fit hour), `demand_scale_i24_zip.json` (s = 0.925; RMSPE train 0.331, held-out 0.441), `i24_boundary_ramps_fit_zip.json` (Old Hickory × 0.75, Hickory Hollow × 1.25, its exit × 1.125; train 0.332, held-out 0.356, 96% inserted), `i24_validation_zip_corrected.json`, `i24_validation_zip_ramps.json` and the canonical `i24_validation_speedcal_heavy.json` (20 seeds each). Lost: the `zip` family's `tracked`, `speedcal` and `speedcal_heavy` battery artifacts (rerun on a second VM the same evening, stage `battery_lost`), every scenario file of the family, the VM logs and the cap sweep (rerun on the second VM with the summary now written after every configuration; until its artifact lands, docs/I24_SWEEP.md keeps the single-seed probe as the only evidence on `h_max_s`).
- The lost scenario files are rebuilt bit-for-bit from the fit artifacts: `scripts/i24_build_replica.py --suffix zip …` (config `2cda89cfbaf8`, the hash the `zip_corrected` battery recorded), `scripts/i24_fit_demand_scale.py --from-artifact` (`f2209020a42a`, the base hash the ramp fit's provenance recorded) and `scripts/i24_fit_boundary_ramps.py --from-artifact` (`b5175be6d854`, the hash the `zip_ramps` battery recorded); both fitters gained `--from-artifact` for exactly this (no simulation; the scenario is a function of the artifact) and the ramp fitter's scenario header now names the artifact it came from. `scripts/i24_validate.py --criteria-only --ring-seeds N` stores a fresh ring benchmark on an artifact whose ring rows were not run (`refresh_criteria(arm, ring_block)`): the heavy arm's ring rows are scored from 20 fresh seeds instead of standing as not evaluated.
- Zipper family against the canonical family (20 seeds each, criteria rows re-scored with the published sweep grid): `zip_ramps` RMSPE 34.2% (canonical `ramps` 34.8%), GEH < 5 on 23% of recommended-coverage link-hours (20%), stack wave speed 15.7 km/h (15.7); `zip_corrected` 37.7% / 22% / 16.1 (canonical 33.7% / 17% / 15.9); `zip_speedcal` 43.0% / 22% / 16.2 (35.9% / 19% / 15.8); `zip_speedcal_heavy` 34.1% / 20% / no stack peak (canonical heavy 35.5% / 23% / 17.3); `zip_tracked` 183.5%, free-flowing, 4 / 3 like the canonical tracked arm. The second VM's reruns reproduce the first VM's console numbers to every digit. The zipper merge, with its junction gap fitted, moves neither failing row out of failure and leaves the wave row passing: the merge-admittance defect of docs/I24_VALIDATION.md §0.5 (k) stands, and the family is kept as a documented negative result, not as a replacement.
- **Headway-cap sweep (2026-09-07, second VM, `artifacts/i24_cap_sweep_summary.json`; docs/I24_SWEEP.md last section):** baseline, FollowerStopper and `follower_stopper_capacity` at `h_max_s` 1.3 / 1.5 / 1.7 / 2.0 s, 20 seeds each with common random numbers on `i24_replica_speedcal` at 5% / 100%, paired 95% intervals. The baseline and FollowerStopper cells reproduce the 500-run sweep's cell to the digit. **The cap is not the lever:** throughput −37.6 / −37.8 / −33.9 / −34.8% against FollowerStopper's −36.0% (paired intervals about ±6 points), σ_v −55.5 to −56.1% against −56.2%, fuel +102 to +123% against +111%. The single-seed probe's halving (`artifacts/i24_controller_probe.json`) did not survive twenty seeds and is no longer quoted as a result.
- `scripts/i24_cap_sweep.py`: resumable per seed (replicates with metrics are not rerun; replicates with trajectories but no metrics are only analysed) and the per-run metrics now run in their own process pool (`--analysis-procs`, default min(procs, 8)); the second VM's sweep showed the sequential analysis idling 31 of 32 vCPUs for most of each configuration. The pooled path has not run on a VM yet.
- The cloud scripts after the post-mortem: `pipeline_i24.sh` archives to `$HOME` atomically after every stage (light: artifacts, scenarios, logs; full at exit with the first-seed replicates), copies to `PIPELINE_BUCKET` when set, runs its EXIT trap on SIGTERM, and deletes its own instance when `PIPELINE_SELF_DELETE=1` and the archive reached the bucket; `launch_i24_pipeline.sh --bucket/--self-delete`, default cap 480 min (the cap is a backstop, sized at twice the estimate); `watch_pipeline.sh` fetches the archive whenever it changed, treats a describe timeout as unknown rather than as a deleted instance, restarts a stopped instance only at two vCPUs and only when the local archive is incomplete, re-arms the cap before each fetch, pulls from the bucket when there is one, and is meant to run under `caffeinate -i`.

### Merge models, ramp metering, capacity-aware smoothing, managed lanes, report aggregation (2026-09-06)

- `RampSpec.merge` (`lane_change` | `acceleration_lane` | `zipper`): on-ramp acceleration lanes as SUMO's acceleration-lane attribute or a zipper junction, applied as netconvert patches in a second import pass (`microsim.networks.merge_patch_files`; `meta.json` `merge_models`, `net_patch_files`). Built to break the lane-discrete merge lock of docs/I24_VALIDATION.md §0.5 (single seed, corrected map, `artifacts/i24_merge_experiment_mergemodels.json`, §0.5 (j)): the acceleration-lane attribute reaches the net and changes nothing; the zipper removes the upstream crawl (entry 40 km/h against 36 observed, right lane the fastest lane as in the recording) but its merged lane admits too little (62% of the ramp's demand in, merge zone at 4–7 km/h, downstream 10–15 km/h too fast; 15-min RMSPE 40.7% against 24.8%) — the junction negotiation gaps are the next lever; ALINEA released 577 vehicles at 648 veh/h and kept the mainline in free flow, as a meter should.
- `RampSpec.meter: RampMeterSpec` with `controllers.ramp_meter.alinea` (Papageorgiou et al. 1991, density form, target = the calibrated diagram's critical density, no built-in target): a virtual stop line on the ramp's last edge releasing one vehicle per metered headway; rates, densities and releases in `meta.json` `ramp_meters`. Registry group `ramp_meter`.
- `controllers.follower_stopper_capacity` (`follower_stopper_capacity`): FollowerStopper with a time-headway cap (`h_max_s` 2.0, `g0_m` 4.0, `blend_m` 5.0) — beyond the cap the command is released toward the leader so the controlled vehicle stops holding traffic back; identical to FollowerStopper inside the cap, continuous across it, output within `[0, U]`. Single-seed comparison on the I-24 fitted arm at 5% / 100% (`artifacts/i24_controller_probe.json`, docs/I24_SWEEP.md last section): FollowerStopper −48% throughput / +128% travel time / −58% σ_v / +166% fuel; the capacity-aware variant −26% / +59% / −55% / +59% — the cap halves the cost for the same smoothing and does not remove it; `h_max_s` is the lever for the next sweep.
- `managed_lanes: list[ManagedLaneSpec]` + `FleetSpec.hov_fraction`: HOV lane rules as lane permission windows admitting SUMO class `hov`; eligible vehicles drawn per vehicle and flagged (`is_hov` column, `n_hov`). Not seeded. Not applied to the I-24 replica: the recording shows lane 1 carrying 34% of vehicle-time at general-lane speed on 30 Nov 2022, and neither the rule in force nor the eligible share is known for that day.
- `validation.report`: the speed criterion by time aggregation with the recorded field's own repeatability floor (`speed_aggregation_rows`, inputs `segment_speeds_obs/sim`, `segment_window_s`), and group labels for closures, heavy shares, managed lanes, meters and merge models so they enter the contrast table like controllers. `scripts/i24_report.py --arms all` passes the arm matrices (the I-24 run trees are pruned, so the reports regenerate with the next battery).
- Tests: patch writer, merge models and metering on an extended OSM ramp fixture, managed lanes on a corridor, ALINEA and capacity-FS unit tests, report table and labels.

### Config-hash policy v2 (2026-09-06)

- `config_hash` now hashes `{"hash_version": CONFIG_HASH_VERSION, "config": model_dump(exclude_defaults=True)}` (network `kind` kept). Adding an optional field no longer moves the hash of scenarios that do not use it; a default change must bump `CONFIG_HASH_VERSION`, which `tests/test_flowstate_core/test_config_hash.py` enforces through a pinned defaults snapshot (`tests/golden/config_defaults.json`) and a pinned ring hash. Every hash moved once (v1 → v2); run metadata carries `config_hash_version`; documents dated before 2026-09-06 quote v1 hashes, and their artifacts keep their config snapshots. Goldens regenerated with statistics unchanged.

### Temporary lane closures and heavy vehicles (2026-09-06)

- **Schema:** `ScenarioConfig.closures: list[LaneClosureSpec]` (`start_m`, `end_m` from the start of the analysis corridor, `lanes` as SUMO indices, `t_start_s`, `t_end_s`, `label`). Micro tier: the listed lanes of every overlapped corridor edge refuse every vehicle class for the window and get their permissions back afterwards (vehicles caught on a closed lane leave it; the insertion edge is never closed); `meta.json` records lane ids, skipped ids and applied/released times. Macro tier: the overlapped cells' interfaces are capped at `q_max × share of lanes open`. Any closure labels the run `seeded=True` (an imposed disturbance is not the emergent phenomenon). Example: `scenarios/corridor_10km_workzone.yaml`.
- **Schema:** `FleetSpec.heavy: HeavyVehicleSpec | None` — a heavy-vehicle share drawn per vehicle after every existing draw (fleets without the block reproduce their previous draws exactly), with its own length, SUMO `vClass`, emission class and IDM population (`idm_calibration` or all five means; no built-in truck defaults). Heavy vehicles are never controlled vehicles. New trajectory column `is_heavy` on every run; `meta.json` `n_heavy` / `heavy_fraction_realized`. The macro tier does not represent them and says so in its meta.
- **Calibration:** `scripts/i24_extract_episodes.py --classes heavy` and `scripts/fit_idm_i24.py --classes heavy` → `artifacts/idm_i24_heavy.json`: 197 semi/truck follower episodes (138 fit / 59 held out), holdout gap RMSE 7.09 m, means v0 30.4 m/s, T 1.96 s, a_max 0.67 m/s², b 1.69 m/s², s0 2.76 m (cars after capacity calibration: T 1.32, a_max 1.06). Not capacity-calibrated (no step 1 applied to the heavy population). `scripts/i24_heavy_share.py` → `artifacts/i24_heavy_observed.json`: 9.2% of mainline fragments and 5.5% of vehicle-time are heavy in the study period, median length 20.3 m. `scripts/i24_build_replica.py --heavy` puts them into the replica (off by default, keeps hashes); `scripts/i24_merge_experiment.py as_is_heavy` runs one seed with them (`artifacts/i24_merge_experiment_heavy.json`): insertion 94.5 → 88.0%, section flows 8% lower, speeds within 1–2 km/h — trucks at their fitted headways widen the replica's capacity shortfall, so an arm carrying them needs the capacity calibration redone with the mixed fleet; the validation arms stay without heavy vehicles.
- Nine new tests (schema, plan draws, route file attributes, an integration run with a closure and a heavy share, a macro closure run). The two schema fields move every config hash; goldens regenerated with statistics unchanged (`ring_sugiyama` 543ae9029c50, `corridor_10km_smoke` 6b68fec0a947, `macro_corridor` 154e14a6f4d2).

### I-24 residual taken apart: map defects, lane-level diagnosis, scoring tables (2026-09-06)

- `scripts/i24_correct_osm.py` → `data/osm/i24_motion_corrected.osm` (+ `.provenance.json`): the OSM extract ends the Old Hickory acceleration lane 244 m early and starts the Hickory Hollow deceleration pocket 345 m late against the provider's landmark layer and the recording's auxiliary-lane occupancy; the script moves the two way boundaries to the landmark positions (one interpolated node each, way membership transferred; no node, tag or lane count changed; connections unchanged after netconvert). `scripts/i24_build_replica.py --osm {original,corrected} --lc-strategic V` builds on either map (config hashes move with the map; the original stays the default).
- `scripts/i24_lane_profile.py` → `artifacts/i24_lane_profile.json`, `docs/figures/i24_lane_profile.png`: lane-by-lane share and speed profiles, recording vs replica (SUMO lane indices mapped through the edge lane count). The replica's right lane crawls at 8 km/h with 37% of vehicle-time 1.5 km upstream of the gore and ramp traffic merges at 9–13 km/h; the recording's right lane is the fastest lane at the entry and slows only inside the acceleration lane while lanes 1–3 hold 30–34 km/h.
- `scripts/i24_validate.py` results schema 6: a third GEH table scores every arm against the tracked crossings divided by the coverage artifact's recommended estimator (`vs_recommended_coverage_counts`, `primary = "recommended"`, rule in `primary_rule`; observed cache version 3), and the `rmspe` block gains per-replicate values and a leave-one-out floor when the run stored per-replicate segment speeds. Re-scored (`--criteria-only`): GEH pass fractions 0.7 / 16.7 / 18.8 / 20.1% (tracked / corrected / speedcal / ramps) — the flow shortfall at the peak sections (12–13% against a 6% tolerance) is a discharge property, not a counting one. The recorded 5-min field differs from its own 15-min moving average by 33.4%, which is where the arms sit; at 15-min aggregation the arms score 25–27% against the recording's own 15.1% floor. The criterion keeps its resolution; the table is reported (`docs/I24_VALIDATION.md` §0.5).
- `scripts/i24_merge_experiment.py`: `geometry_corrected`, `geometry_corrected_lc{1,2}` and `geometry_corrected_assertive_*` variants, 15-min RMSPE and GEH-vs-recommended in the output, `--keep-dir` for lane profiling. Single seed (`artifacts/i24_merge_experiment_geometry.json`): the corrected map admits 93% instead of 85% of the ramp's demand and leaves the mean profile within 1 km/h; strategic eagerness back at 1–2 re-creates the diverge stall even with the longer pocket.
- The missing-leader hypothesis for the fitted headways is rejected: near equilibrium the gap-to-fitted-gap ratio is unimodal (1.4% beyond 2.5).
- **Schema change:** `FleetSpec.lc_strategic_ramp: float | None = None` — the SUMO `lcStrategic` written on the vTypes of vehicles whose route starts on an on-ramp (`None` = same as `lc_strategic`). One eagerness cannot serve exiting vehicles (reach the deceleration pocket early) and entering vehicles (use the acceleration lane) at once; with the single elevated value the replica's ramp traffic merged at the gore at 9–13 km/h. Route files of existing scenarios are byte-identical. Three unit tests cover the field.
- **Schema change:** `CorridorNetwork.entry_lane_shares` / `OSMNetwork.entry_lane_shares: list[float] | None = None` — the measured share of mainline entries per lane (left to right) as an upstream boundary condition; `None` keeps the round-robin insertion byte for byte. When set, `build_corridor_plan` draws each mainline vehicle's departure lane from the run's RNG (`FleetPlan.depart_lane`) and the route writer emits it. On I-24 the right lane carries 17% of vehicle-time at the entry against 34% in the left lane (the Old Hickory off-ramp has just drained it); a replica inserting a quarter of the flow into the lane the next on-ramp merges into queues that lane 1.5 km upstream of the gore. Four unit tests.
- **Schema change:** `SimSpec.lateral_resolution_m: float | None = None` — SUMO's `--lateral-resolution`; a value switches the run to the sublane lane-change model (LC_SL2015), exposed because the lane-discrete model locked the replica's merge under every lane-change parameter tried (docs/I24_VALIDATION.md §0.5). `None` keeps every existing run identical.
- `scripts/i24_merge_experiment.py` `*_entrylanes` and `*_sublane` variants (`artifacts/i24_merge_experiment_entrylanes.json`): the measured entry lane distribution on the corrected map with ramp-origin eagerness 1 gives the best single-seed 15-min error so far (24.8% against 25.9% for the original replica) and shortens the queue by half a kilometre, but the crawl at the gore itself stays at 8–11 km/h.
- Sublane model on the replica (`artifacts/i24_merge_experiment_sublane.json`): at 0.8 m lateral resolution with default lateral parameters and no internal links, all three single-seed runs gridlock (38–45% inserted, 1–10 km/h everywhere); recorded as a negative result, the option stays for a calibrated attempt. Conclusion of the round (`docs/I24_VALIDATION.md` §0.5 (i)): the merge crawl is a lane-discrete lane-change merge lock, not a map length, eagerness, gap-acceptance, cooperation or entry-distribution effect; the criteria rows are unchanged.
- The three schema fields move **every config hash**; the three goldens were regenerated with their summary statistics unchanged (`ring_sugiyama` 59e8181c0bd6, `corridor_10km_smoke` 07e450e0632a, `macro_corridor` 5d3972b6a4de) — the physics did not move, only the snapshot.

### I-24 four-arm battery under the criterion's stack detector (2026-09-05)

- `scripts/i24_validate.py` rerun on the cloud VM for all four demand arms (tracked `4cd18bf46147`, corrected `d8c6924188eb`, speedcal `009ed0e2a7c0`, ramps `d06808e7b8e1`; 20 seeds each; results schema 5 carries every registered wave detector under `waves.by_detector`), then `--criteria-only` locally with the published sweep grid: **5 PASS / 2 FAIL on each congested arm, 4 / 3 on the tracked arm.** Link-flow GEH (10.4–24.3% of link-hours under 5) and segment-speed RMSPE (33.7–35.9%; 187.8% tracked) fail everywhere. The wave-speed row passes with the profile's `stack` detector (15.7–15.9 km/h simulated, 19.9 observed; the stack finds a peak in 7–12 of 20 replicates per arm) and would fail with the standard detector (7.9–10.4 km/h), which is reported beside it in `docs/I24_VALIDATION.md` §0.4; the three arms already run on 3 Sep reproduce that run's numbers to every digit after the `FleetSpec` schema change. Fitted-ramps arm: RMSPE 34.8%, GEH 10.4%, throughput 5,574 veh/h, 96.5% inserted — one RMSPE point gained, five GEH points lost. Figures regenerated with four arms and the criterion-detector markers (`scripts/i24_validation_figures.py`, `docs/figures/i24_validation_*.png`); README, ROADMAP, dossier, paper outline, website brief and lessons updated to the four-arm count. The auto-reports under `docs/reports/i24_replica/` remain those of the 3 Sep battery (replicate trees pruned on the VM).
- Cloud: the `flowstate-sweep` VM was deleted after the results were fetched; no compute resources remain in the project.

### I-24 fundamental diagram and audit list

- `artifacts/fd_i24.json` (`scripts/fit_fd_i24.py`, 200-resample bootstrap, seed 42): per-lane triangular FD from Edie bins; congested wave speed 16.1 km/h [15.7, 16.5]; capacity and jam density recorded as coverage lower bounds. Section added to `docs/I24_DATA.md`.
- `docs/AUDIT_2026-09-03.md`: findings of a ten-lens code audit; two frontend schema-drift bugs verified (run-detail metrics and heatmap types did not match the API), the rest recorded as unverified follow-ups.

### Fixed

- Dashboard progress bars read `done`/`total` while the API's `ProgressOut` sends `completed_replicates`/`total_replicates`, so every run and sweep cell showed an empty bar; the frontend type now mirrors the API and the mock produces the same shape.
- Dashboard scenario library: pressing Run on a repo preset sent a run request without a `scenario_id` (presets from `GET /scenarios/preset` are not stored scenarios and carry none), and the merge keyed on that missing id collapsed every preset into one card. Run on a preset now stores it first (reusing a stored scenario with the same config hash), and presets already in the library show as their stored copy. The mock backend now returns the API's `PresetOut` shape, and a test drives the flow against real API shapes.
- Frontend default API key is now the API's own inline-queue default (`dev-key-change-me`), as the README already documented; it had drifted to the Compose fallback, so the no-Docker dev path (`uvicorn` + `npm run dev`) answered 401 out of the box. Compose users paste their key in Settings as before.
- Frontend `RunMetrics`/`Heatmap` types now mirror the API's `MetricsOut`/`HeatmapOut` (replicates with per-seed metrics; bin centers, not edges); CI fields are nullable as in `CIOut`; the run-detail page no longer throws on a real finished run. Mock backend updated to the same shapes.

### Engine gaps closed (audit follow-up, CLAUDE.md §3.2.4, §4.4, §4.5, §7.4, §9)

- **VSL (§4.4).** The micro tier scales every posted limit by fleet compliance against the edge's own base limit through the pure `controllers.vsl.effective_limit` (compliance-weighted mean, clamped, never above the base) and dispatches on 0.5–1.0 km gantry segments (`NetBundle.segments()` / `controllers.vsl.gantry_segments`) instead of raw edges; compliance 1.0 on generated corridors reproduces the previous outputs bit for bit. The macro tier applies `cfg.av.vsl` for the first time, capping the free-flow branch per cell through the existing interface caps (CFL from the uncapped `v_f`, conservation intact, outputs still `tier="screening"`). Both tiers record a `vsl_dispatch` provenance block in `meta.json`. 41 tests added; no config-schema change.
- **Report (§7.4).** `validation.report` groups micro runs by `config_hash`, labels each group from its `av` block, renders one metric table and replicate check per group, and adds a controller-minus-baseline contrast table (seed-paired under common random numbers, Welch otherwise, `resolved` when the interval excludes zero) with baseline/controller contour pairs per matched seed. Seeded replicates are excluded from the wave-speed criterion input; duplicate seeds within a configuration are an error. `generate_report(..., pdf=True)` renders the same Markdown to `report.pdf` through the optional `validation[pdf]` extra (fpdf2); the API gained `GET /reports/{id}/pdf` and the report job requests the PDF with a Markdown-only fallback.
- **Golden regressions and tier sanity (§9).** `tests/golden/` holds engine-produced summary statistics for `ring_sugiyama`, a 2-minute `corridor_10km` smoke run and a seeded macro corridor (config hash, seed, SUMO and package versions recorded); `test_microsim_golden.py` and `test_macrosim_golden.py` compare re-runs at rel 1e-6 (SUMO) / 1e-9 (numpy, numba), with the PR-note rule for updates in `tests/golden/README.md`. `tests/test_integration/test_tier_sanity.py` runs one seeded shock through both tiers and requires backward fronts in each. The §3.4 20-replicate wall-clock test is slow-marked and `.github/workflows/perf.yml` runs `pytest -m slow` weekly and on demand.
- **Demand fitter, GEH aggregation, criteria profiles, ring rows (§6.3, §7.1, §7.3).** `calibration.demand.fit_inflow(scenario, counts)` is the §6.3 entry point, with a microscopic adapter (`microsim.demand_adapter.make_simulate_fn`, one seeded `run_micro` per iteration) and the legacy injected-simulator form kept as `fit_inflow_profile`. `validation.metrics` gained `count_crossings`, `crossings_per_window`, `link_hour_geh` and `geh_pass_fraction`. `validation.criteria` gained a `source`-bearing profile registry (`fhwa_default`, `fhwa_tat3_2004`, `odot_vissim_2011`, `txdot_tsap_ch13`) transcribed from the verified FHWA 2004 table, the ODOT 2011 protocol and TxDOT ch. 13 — the 2019 FHWA update prescribes no GEH target, and CLAUDE.md §7.1 now says so — plus the §7.1 `sensitivity_grid` row. `validation.ring_benchmark` turns the CI ring gate's checks into functions evaluated over seeds, and `scripts/i24_validate.py --ring-seeds` feeds them into the corridor battery so the ring rows are evaluated instead of marked FAIL (results schema_version 4).
- **Onboarding and RL hook (§3.2.4, §4.5).** `microsim.scenarios.scenario_from_osm` completes the `osm_generic` pipeline: import and prune an OSM extract (file or bbox), verify the corridor chain against the compiled net with sumolib, and return a hashable `ScenarioConfig` with the versioned `corridor_10km` defaults; the YAML round-trip preserves the config hash. `MicrosimBackend.from_scenario` and `FlowStateEnv(scenario=...)` tie the Gymnasium environment to `corridor_10km.yaml` (short episodes for smoke tests), with `close()` added to the backend protocol.

### Dossier

- `docs/FLOWSTATE_DOSSIER.md`: the long-form technical and commercial dossier (how it works, why, the full evidence and validation record with tables and figures, market, revenue hypotheses, channels, implementation options, risks, appendices), written so every number traces to a committed artifact and rendered to PDF with the repository's own renderer.

### I-24 flagship sweep (ROADMAP §1.5) and the calibration follow-ups

- `artifacts/i24_sweep_summary.json`, `docs/I24_SWEEP.md`, `docs/figures/i24_sweep_dose_response.png` (`scripts/i24_sweep_figures.py`): 500 runs on the fitted arm (cloud VM, 0 failures). FollowerStopper at its literature defaults costs throughput at every cell, growing with penetration (5% / 100%: throughput −36%, travel time +82%, fuel +111%, σ_v −56%, waves halved; lane changes 1.25 → 2.32 per vehicle-km at 20%). Documented as a sensitivity battery on an unvalidated replica; the mechanism (a held gap is a moving capacity drop against capacity-bound demand) is stated and the next controller must be capacity-aware.
- Joint out-of-sample fit (`artifacts/i24_boundary_ramps_fit.json`, `scenarios/i24_replica_speedcal_ramps.yaml`): 43 evaluations; the best point moves ramp demand from Old Hickory (×0.75) to Hickory Hollow (×1.25) with a slightly larger Hickory Hollow exit share, boundary and gap acceptance unchanged; fitted hour 35.6% → 33.2%, held-out hour 42.6% → 35.6%, 96% inserted. The lane-change grid (`artifacts/i24_lanechange_fit.json`, 18 points) prefers high gap acceptance on lane shares, which the speed objective rejects; nothing adopted from it (`docs/I24_CAPACITY.md` §7–8).
- `scripts/i24_validate.py`: the `sensitivity_grid` criterion row is fed from `artifacts/i24_sweep_summary.json` when present, and `--criteria-only` re-evaluates the criteria rows of existing arm artifacts without simulating.

### Engine refinements (2026-09-03, second round)

- **Lane-change model as a calibration target.** `calibration.lanechange` computes lane-use shares, held lane changes per vehicle-km and change locations per section from any trajectories-like frame, identically for I-24 MOTION fragments (band lanes, flicker guard) and simulated runs (SUMO lane index mapped to the band convention, verified on the compiled net), with a documented objective (RMS lane-share difference + 0.1 × rate RMSPE). New `LaneChangeCalibration` artifact records parameters, observed and simulated observables on the fitted first hour and the held-out second hour, the grid and provenance. `scripts/i24_lanechange_observed.py` wrote `artifacts/i24_lanechange_observed.json` (06:30–08:30 CST, 288,842 fragments: lane shares 0.28–0.33 / 0.23–0.27 / 0.17–0.23 / 0.17–0.33 left to right by section, 1.3–3.2 lane changes per vehicle-km); `scripts/i24_fit_lanechange.py` evaluates an `lc_*` grid on the fitted arm (one seeded replicate per point). Only the smoke was run locally; the grid runs on the VM.
- **Second corridor through the same procedure (US-101, no retuning).** `scripts/calibrate_capacity.py` and `scripts/fit_demand_scale.py` are corridor-agnostic versions of the I-24 step-1 and step-2 scripts (shared pure helpers; the I-24 artifacts are reproduced by test). Applied to the US-101 replica: capacity calibration scales the population's mean T by 0.794 (1.285 → 1.020 s) to meet its FD's q_max lower bound (2,068 veh/h/lane) at a 6.8% gap-RMSE cost; the demand level fitted on the first half of the 15-minute study period is 1.25 (held-out RMSPE 31.5%). The rerun battery (`artifacts/us101_validation_calibrated.json`, 20 seeds) stays 1 PASS / 5 FAIL: speed RMSPE 36.6% → 27.9%, GEH pass fraction 56% → 22% because the level overshoots flow to stand in for the missing on-ramp merge, wave speed unchanged (site artifact). `scripts/m3_us101_validate.py` gained a `calibrated` arm, `--artifact-out` and a 150 s observed table. `docs/US101_CALIBRATED.md`.
- `scripts/i24_build_replica.py --coverage-estimator {equilibrium,gap_mixture,section_gap_mixture,recommended}` reads a gap-based coverage from `artifacts/i24_coverage.json` for the corrected arm (default unchanged, so config hashes are unchanged) and records the source in the inputs artifact.
- **Tracking-coverage estimators without the equilibrium assumption.** `calibration.coverage`: the IDM-equilibrium ratio, a random-thinning moment estimator, a maximum-likelihood geometric-gamma spacing mixture with censoring and duplicate removal, a capacity lower bound with the `max(estimate, bound)` rule, and a section-crossing variant; `synthetic_validation` recovers a known coverage on thinned synthetic lanes and pins the failure modes. `scripts/i24_coverage.py` → `artifacts/i24_coverage.json`: the recommended coverage is 0.56–0.67 over 06:30–08:30, 8–25% above the equilibrium factor, lowering the peak corrected inflow from 1,935 to 1,786 veh/h/lane — the correction the speed fit found independently. 50 new tests.
- **Wave-speed detector made explicit and benchmarked.** `validation.waves` gained a registry of named detector recipes (`standard`, `stripe`, `relative`, and a new threshold-free slant-stack estimator `stack`), a planted-stripe benchmark generator, and a benchmark measured in the tests: on congested backgrounds the standard 40 km/h detector found no backward front at any congested fraction, the relative detector none below 90% congestion, the stripe detector was biased by up to 4.4 km/h near its own threshold, and the stack recovered a planted 16 km/h within 0.1 km/h everywhere. `CriteriaProfile.wave_detector` (default `stack`) and every evaluated wave-speed row now names the recipe behind its value; `scripts/i24_validate.py` computes all four and scores the profile's (results schema 5); the report labels its row as the standard detector that `compute_metrics` uses. Artifacts written earlier keep their standard-detector rows until rerun.
- **Out-of-sample fitter for ramp levels, boundary discharge and gap acceptance** (see the step-3 entry below).

### Schema: merge lane-change parameters (config hashes change)

- `FleetSpec` gains `lc_cooperative` (SUMO `lcCooperative`, [0, 1]), `lc_assertive` (`lcAssertive`, > 0) and `lc_speed_gain` (`lcSpeedGain`, ≥ 0), written on every vType only when they differ from SUMO's default 1.0 (route files of existing scenarios stay byte-identical). Exposed for the Old Hickory merge calibration (`docs/I24_CAPACITY.md` §6); `scripts/i24_merge_experiment.py` gained cooperation, gap-acceptance and speed-gain variants and reports first-hour and held-out-hour RMSPE. **Every config hash changes** because the snapshot carries the new fields; goldens under `tests/golden/` were regenerated with unchanged physics (summary statistics identical, only the recorded hashes differ — the PR note required by `tests/golden/README.md`).

### Scientific validity: three-arm rerun on the calibrated population

- `scripts/i24_validate.py` rerun (cloud VM, 20 seeds per arm, ring rows evaluated, results schema 4): **3 PASS / 3 FAIL per arm** — ring emergence and dampening 20/20, replicates ≥ 20; link-flow GEH, speed RMSPE and wave speed still fail in all arms. Corrected arm: RMSPE 36.8% → 33.7%, fronts 8.7 → 10.4 km/h (standard) and 12.3 → 14.2 km/h (stripe, now inside 14–22 against 16.0 observed), throughput 5,266 → 5,576 veh/h. Fitted arm (`i24_replica_speedcal`): 95.5% of demand inserted, RMSPE 36.0%, throughput 5,710. The residual is spatial: the Old Hickory merge holds a standing queue at the entry; everything from 2.2 km on is within a few km/h of the recording (`docs/I24_VALIDATION.md` §0). `--reuse-runs` analyses complete replicates on disk.

### Scientific validity: capacity calibration of the I-24 fleet

- `scripts/i24_capacity_experiment.py` → `artifacts/i24_capacity_experiment.json`: the car-following population fitted on congested episodes (`idm_i24.json`, T = 1.51 s) saturates at ≈ 1,650 veh/h per lane on a straight four-lane road, below the 1,775 veh/h per lane the instrument *tracked* (the FD `q_max` lower bound); spreading insertion over three edges frees the buffer and changes nothing downstream. The replica's insertion cap (82–84%) is a capacity limit, not an insertion artifact (`docs/I24_CAPACITY.md`).
- `scripts/i24_calibrate_capacity.py` → `artifacts/idm_i24_capacity.json` (+ `.calibration.json`): FHWA Vol. III step 1. Population mean T scaled by 0.875 (1.511 → 1.322 s) so the straight-road capacity meets the tracked lower bound; covariance, other means, lane-change parameters and demand untouched; population-mean gap RMSE on 1,500 sampled episodes 5.313 → 5.294 m.
- `scripts/i24_build_replica.py` now builds both replica arms on the calibrated population: config hashes `i24_replica` 5e15ca999c19 → 17e9e80ffd83 and `i24_replica_corrected` a3efae6955bd → de82b62e4ef6. The §1.5 sweep in flight on the cloud VM runs the previous corrected hash and is documented as such.
- `scripts/i24_fit_demand_scale.py` (FHWA step 2): one scale factor on the tracked mainline and on-ramp inflows fitted to observed segment speeds over 06:30–07:30 CST only, 07:30–08:30 held out → `artifacts/demand_scale_i24.json` and `scenarios/i24_replica_speedcal.yaml` (third arm).

### Cloud compute

- The bootstrap now arms an automatic shutdown five minutes after the sweep finishes (`--no-auto-stop` to keep the VM up), after a VM idled about 40 hours on 2026-09-04/05 because the session that launched it lost connectivity.

- `scripts/gcp/`: bootstrap for a fresh Debian VM (system deps, uv, workspace, resumable sweep under nohup), a results fetcher, and a README with the exact `gcloud` commands and costs. `data/osm/i24_motion.osm` (89 KB, ODbL) is now versioned so the I-24 replica runs from a plain clone.

### Embeddable simulation

- `embed/`: a static Vite + TypeScript page (no backend, ~21 kB of script)
  that replays real engine output: 45 `ring_sugiyama` runs generated by
  `scripts/website_sim_pack.py` (18/22/26 vehicles × 0/1/2 FollowerStopper
  vehicles × switch-on from start or after 5 min × seeds 42/7/123, stored as
  uint16 binaries, 4.8 MB) plus the observed I-24 westbound field. Top-down
  ring, time-space diagram, live readouts, per-minute σ_v against the
  same-seed baseline, provenance line (SUMO version, config hash, seed),
  keyboard control, reduced-motion respect, `?embed=1` mode with a
  postMessage height for iframe hosts. Unit tests plus a data-pack integrity
  test; Dockerfile (nginx) with `embed/fly.toml`, and a root `render.yaml`
  Blueprint for a Render static site. CI gains a `web` job that typechecks,
  tests and builds both `frontend/` and `embed/`.

### Website brief

- `docs/WEBSITE_BRIEF.md`: the design prompt for the public site, and `scripts/website_hero_data.py` → `docs/website/hero_data.json` (the observed I-24 westbound speed field copied from `artifacts/i24_wb_overview.json` plus three seeded `ring_sugiyama` runs, one with the single FollowerStopper vehicle switched on at 300 s). No scenario changes; the ring config hashes are the CI-gated ones.

### Added

- **Streaming I-24 MOTION loader** (`calibration.loaders.i24motion`):
  `iter_i24_documents` decodes the INCEPTION MongoDB export one document at a
  time straight out of the zip (the 30 Nov 2022 run is a 19.5 GB JSON array of
  816,694 documents in a 5.8 GB zip; it is never extracted), and
  `convert_i24_to_parquet` writes filtered, decimated Parquet with a
  per-fragment table and provenance `meta.json`. The schema was reconciled
  against the real file and the official v1.x data documentation:
  `_id` is `{"$oid": ...}`, `x_position` is the *back*-center roadway
  coordinate in feet (MM 60 ≡ 316,800 ft), `y_position` is positive westbound
  with lane 1 (HOV) at 12–24 ft, and every document is a trajectory
  **fragment**. The loader's `x` is the front bumper on a travel-oriented axis
  so the existing bumper-to-bumper gap logic applies unchanged; the loader
  tests' expected gap changed accordingly (spacing minus *follower* length,
  not minus leader length). `scripts/i24_extract.py` runs the conversion
  (westbound: 576,511 fragments → 42.8 M rows at 5 Hz, 993 MB, 309 s).
- **Ramps and boundaries on OSM corridors** (`flowstate_core.config.RampSpec`,
  `OSMNetwork.ramps`, `OSMNetwork.boundary`; docs/CONTRACTS.md §2). A real
  corridor exchanges traffic at interchanges; the I-24 westbound testbed has
  two on-ramps and two off-ramps inside the span whose flows are a sizable
  share of the mainline, and the US-101 replica's missing merge was a
  documented structural failure. On-ramps insert their own seeded demand on
  the ramp's first edge; off-ramps divert a per-vehicle seeded fraction; ramp
  edges are kept through OSM pruning and their connectivity is checked before
  SUMO starts; `meta.json` records per-ramp planned/departed/exiting counts.
  The measured-boundary schedule now also applies to the last corridor edge
  of an OSM network, and OSM insertion uses the entry edge's real lane count.
  Integration-tested on a hand-built interchange fixture
  (`tests/test_microsim/test_microsim_osm_ramps.py`). Two SUMO facts learned
  and encoded in that fixture: a ramp must feed an auxiliary lane (a ramp
  squeezed into a same-width edge faces priority-junction gap acceptance and
  never merges against a steady stream), and hand-drawn ramps must leave at
  gore-like shallow angles (SUMO caps turning speed by curvature, so a steep
  link is an artificial 5 m/s bottleneck).
- `FleetSpec.lc_strategic` (SUMO `lcStrategic`, default 1.0 = SUMO's own
  default, written on vTypes only when changed). On the I-24 replica the
  default leaves 894 stalled 10-s samples in two hours at the Hickory Hollow
  diverge and a 29.8 km/h mean speed over the kilometre upstream of it — a
  fixed bottleneck the data does not have — against 44 samples / 84.7 km/h at
  5.0 and 3 / 86.8 at 20.0 (same seed, same demand). The replica uses 5.0.
- `FleetSpec.lc_keep_right` (SUMO `lcKeepRight`, default 1.0). The default
  encodes a keep-right obligation US freeways do not have: on the I-24
  replica it spreads vehicle-time 24/25/25/27% across the four lanes (left
  to right) but crawls at 23–27 km/h in the two right lanes through the Old
  Hickory merge while the left lanes run at 65–97 km/h — 11,535 stalled
  10-s samples in two hours. At 0 the lane shares become 32/26/22/20%
  against the observed 30/24/20/26% (all observed lanes at 30–33 km/h) and
  the stalls fall to 200 (same seed, same demand). The replica uses 0.
- **I-24 replica validation (ROADMAP §1.4)** — `scripts/i24_validate.py`,
  `artifacts/i24_validation_{tracked,corrected,observed}.json`,
  `artifacts/i24_validation_waves_relative.json`, docs/I24_VALIDATION.md,
  auto-reports under `docs/reports/i24_replica/`. Honest outcome: **1 PASS /
  5 FAIL in both demand arms**, 20 seeds each. With demand as tracked (a lower
  bound) the span free-flows (RMSPE 183%); dividing demand by the
  instrument's apparent coverage produces a stop-and-go field that looks like
  the recording (RMSPE 36.8%, mean speed 28.0 vs 33.5 km/h observed), but the
  replica inserts only 82–84% of that demand, its jams are shallower than the
  real ones, and its backward fronts run at 8.7 km/h with the standard
  detector (12.4 with the relative one) against 14.2 (16.4) observed. The
  wave-speed prediction of docs/WAVE_SPEED_DIAGNOSIS.md is therefore **not
  confirmed on the corridor**; the same fleet reaches the 14–22 km/h band on a
  ring only above ~80 veh/km per lane, a density the replica does not reach.
- **I-24 calibration artifacts.** `artifacts/idm_i24.json`: IDM population
  fitted on all 17,652 ≥ 30 s leader-follower episodes of the westbound day
  (7× NGSIM's count; holdout gap RMSE 5.29 m vs 6.44 m for NGSIM; T = 1.51 s,
  a_max = 1.06 m/s², s0 = 2.53 m). `artifacts/demand_i24.json`: mainline
  inflow per 5 min for 06:30–08:30 CST, a lower bound at the instrument's
  tracking coverage. `artifacts/i24_replica_inputs.json`: every derived input
  of the replica (geometry mapping, ramp flows, exit fractions, boundary
  schedule, coverage factors). See docs/I24_DATA.md.
- **Deferred-commitment JAD** (`controllers.jad`, parameter `commit_delay_s`,
  default 0 = the original controller; ROADMAP B4). From CRUISE, slow-in now
  starts only once a wave has been detected continuously for
  `commit_delay_s`; the deferral that a 30–60 s detection latency supplied by
  accident in 2.1.0 (docs/JAD_ORACLE_RESULTS.md) becomes an explicit design
  rule usable with a perfect sensor. Unit-tested (persistence, transient
  detections reset the clock, the full cycle still runs); the experiment is
  `scripts/jad_deferral_experiment.py` → `artifacts/jad_deferral_summary.json`.
- **Relative-threshold wave detection** (`validation.waves.detect_waves(...,
  relative_frac=f)`, ROADMAP D1): thresholds at `f × p90` of the field's
  non-empty bin speeds, so stripes inside a field that is congested
  everywhere are segmented instead of being merged into one pinned blob (the
  documented failure above ~80 veh/km, docs/WAVE_SPEED_DIAGNOSIS.md). Unit
  tests plant a −16 km/h stripe in a 22 km/h field: the absolute detector
  returns one 0 km/h blob, relative mode recovers −16 km/h. A labeled variant;
  the §7.1 criterion stays on the absolute threshold.
- **Bounded-memory trajectory capture.** `microsim.runner` now streams the
  trajectory table to Parquet in 500k-row groups during the run instead of
  holding every sampled row as Python lists until the end. A 7,800 s
  four-lane I-24 run captures ~10 M rows; the old buffer cost several GB per
  process and eight concurrent workers took a 16 GB machine down twice
  during the I-24 validation. File content is unchanged (the byte-exact
  determinism test still passes); peak memory per worker drops to well under
  1 GB. `validation.metrics.compute_metrics` and the report generator's
  contour renderer now read only the trajectory columns they use, which cuts
  their peak from ~7 GB to a few GB on the same runs.
- `calibration.fd_fit.fit_triangular_fd(n_procs=...)` refits bootstrap
  resamples in a process pool; the resample draws are made up front from the
  seeded generator in serial order and each refit is deterministic, so the
  artifact is identical to the serial path (each exact-LP quantile refit costs
  ~2 min on the 237k-bin I-24 data set).

### Changed

- **Config hashes change for every scenario**: `OSMNetwork` gained
  `boundary` and `ramps` (defaults `None` / `[]`) and `FleetSpec` gained
  `lc_strategic` (default 1.0), and `config_hash` covers the whole serialized
  config (CLAUDE.md §0.5) — the same situation as the `OracleSpec` addition
  in 2.1.0. Physics is unchanged for every existing scenario: the defaults
  reproduce SUMO's own behaviour and the route files they generate are
  byte-identical (`tests/test_microsim/test_microsim_vehicles.py::
  TestLcStrategic`). Hashes quoted in earlier documents (the gallery's
  `7529e2b0dd63` / `f8c6011feb3e`, the M3 and 2.1.0 experiments) refer to the
  pre-Phase-6 schema.
- `runs/` (12 GB of regenerable per-replicate trajectories) was deleted to
  make room for the I-24 processing; the small machine-readable result files
  the results documents cite were preserved verbatim under
  `artifacts/run_summaries/<experiment>/` (see its README).

## [2.1.0] — 2026-09-02

### Added

- **Delayed / noisy wave-detection oracle** (`OracleSpec` on `AVSpec`),
  completing CLAUDE.md §4.3's requirement that the oracle be swappable and that
  every headline JAD result also be reported under a degraded oracle. `delay_s`
  makes the controller read the traffic state as it was `delay_s` ago;
  `amplitude_noise_frac` applies seeded multiplicative error per bin. Default is
  a perfect oracle, so existing configs are unchanged.
- `docs/jad_derivation.md` — the JAD intercept-timing derivation with geometry
  that CLAUDE.md §4.3 required and that was never written.

### Changed

- **Diagnosed the US-101 wave-speed criterion failure** as a site/operating-density
  artifact rather than a calibration defect. The calibrated fleet produces a mean
  emergent backward wave speed of 14.6 km/h on a ring at 60 veh/km (71% of fronts
  inside the 14-22 km/h band) - matching the independently fitted macroscopic FD's
  w = -14.6 km/h from a completely separate estimation path. Near critical density
  fronts are slower (11.4 km/h at 40 veh/km), close to the replica's measured
  10.7 km/h. The criterion still FAILS as measured on the 640 m replica and that
  stands in the validation table; what a passing test needs is now identified.
  Also recorded: an open 10 km corridor cannot reach the instability band with
  these parameters (insertion caps density near 25 veh/km vs a 31.8 veh/km
  threshold), and threshold-based wave detection breaks down above ~80 veh/km
  where the whole field reads as jammed. See `docs/WAVE_SPEED_DIAGNOSIS.md`.

- **The "free lunch" framing is now qualified by a real-geometry check.** The
  penetration ladder was rerun on the `us101_replica` (640 m of real 5-lane
  US-101, calibrated fleet, real demand, measured downstream boundary), 6 cells
  x 20 CRN seeds. The σ_v dose-response replicates cleanly (-8.2% at 1% to
  -53.2% at 20%, every step resolved). The no-cost result does not: that
  saturated site shows a resolved throughput cost of 0.3-1.6% and a resolved
  fuel *increase* of 1.4-2.7% at 1-10% penetration, against a 2.8-5.7% saving on
  the synthetic corridor. Smoothing holds; "for free" is corridor-dependent.
  See `docs/US101_PENETRATION.md`.

- **Config hashes changed for every scenario** when `OracleSpec` was added to
  `AVSpec`: `config_hash` covers the whole serialized config (CLAUDE.md §0.5),
  so a new field with a default still changes the digest. Hashes recorded in
  artifacts produced before this change (M2, M3, the US-101 validation) refer to
  the pre-Phase-5 schema and will not reproduce against current code, though the
  *physics* is unchanged: the `corridor_10km` baseline gives bit-identical
  metrics before and after (temporal σ_v 3.3851, CI lower bound 2.8283 in both
  the pre-change `pi_retune` run and the post-change `jad_oracle` run), which is
  the intended evidence that a default `oracle` is inert.

- **JAD's M3 bimodality is explained and resolved.** The perfect oracle was the
  cause: it fires the instant any bin in the 2 km lookahead qualifies, so the AV
  completes slow-in/hold/fast-out before the front arrives and re-triggers —
  30.7 acceleration sign-reversals per run against 16.6 under a 30 s delay —
  and each abrupt fast-out can seed a secondary wave. Under a perfect oracle
  5/20 seeds end worse than the uncontrolled baseline (one goes 1 -> 11 waves)
  and the wave-count benefit is not resolved. With 30-60 s latency and +/-20%
  noise, **no seed is worse than baseline** and wave count (-3.50 [-4.71,
  -2.29]), sigma_v and fuel all improve with resolved CIs. Realistic detection
  is what makes JAD reliable here. See `docs/JAD_ORACLE_RESULTS.md`.

### Fixed

- **PI-with-saturation now implements Stern et al. (2018) Eqs. (3)–(5).** The
  M3 sweep's headline failure — `pi_saturation` gridlocking the open corridor,
  94% throughput collapse — was caused by the CLAUDE.md §4.2 *simplification*
  (`v_target = 0.75 · platoon mean`), not by the literature controller. That
  factor does not appear in the paper: its target is the AV's own ≈38 s mean
  speed plus a bounded, non-negative gap-scheduled catch-up term, which cannot
  ratchet downward. Re-run on the same scenario and seeds, the faithful
  controller calms the corridor — σ_v −29.7% [−1.35, −0.66], waves −41.6%,
  fuel −2.9%, no resolved throughput cost (n = 20, paired) — and still dampens
  the ring (σ_v 2.07 → 0.46 m/s). The simplification is retained as
  `controllers.pi_meanfrac`, clearly labeled, so the M3 result stays
  reproducible. CLAUDE.md §4.2 corrected against the source per its own §13.
  See `docs/PI_CONTROLLER_FIX.md` and `artifacts/pi_retune_summary.json`.
- `pi_saturation`'s output range is `[0, U + v_catch]`, not `[0, U]`; the
  property test and docs/CONTRACTS.md §8 record the exception with its citation.

## [2.0.0] — 2026-08-29

Complete rewrite of FlowState (M0–M5, developed and hardened 2026-08-29).
v2 replaces the v1 single-engine LWR study with a two-tier, calibrated,
honestly-validated corridor platform.

### Two-tier simulation engine (ADR-1)

- **Microscopic tier (primary):** Eclipse SUMO 1.27.1 + IDM/EIDM via
  libsumo, `microsim` package. String instability is **emergent** — waves
  grow from calibrated car-following dynamics and insertion jitter, never
  from hand-seeded shocks (seeded runs are permitted but labeled
  `seeded=True` everywhere).
- **Macroscopic tier (screening):** the v1 LWR/CTM Godunov engine ported
  into `macrosim` with a full test battery (Riemann exactness, mass
  conservation, CFL guards) and Numba compilation. It is repurposed for fast
  parameter screening and future state estimation; it makes **no** claims
  about phantom-jam formation or dissipation (LWR is string-stable by
  construction). Every macro artifact carries `tier="screening"` and can
  never back a validation report.

### Ring gate (M1)

- Permanent CI integration tests on the 230 m / 22-vehicle Sugiyama ring:
  stop-and-go waves **emerge** without any seeded perturbation (Sugiyama et
  al. 2008), and a single FollowerStopper AV dampens them (Stern et al.
  2018). These gates must stay green for any change to merge.
- Interpretable controller library (`controllers`): FollowerStopper,
  PI-with-saturation, Jam-Absorption Driving, and a VSL segment controller,
  all pure functions with literature-default parameters, plus a Gymnasium
  environment hook for future RL work (ADR-2: RL deferred).

### NGSIM calibration (M2)

- Real-data calibration pipeline (`calibration`) fed by the public NGSIM
  US-101 Socrata dump (provenance-hashed): triangular fundamental-diagram
  fit with bootstrap CIs and an IDM population fit from reconstructed
  car-following episodes with held-out gap RMSE (docs/M2_RESULTS.md).
- Loaders for NGSIM, PeMS, highD and I-24 MOTION formats; demand fitting
  for corridor scenarios.

### 540-run sweep (M3)

- Penetration × compliance × controller battery on the synthetic
  `corridor_10km` EIDM scenario: 27 cells × 20 common-random-number
  replicates = **540 emergent micro runs**, aggregated as mean ± 95% t CIs
  (docs/M3_RESULTS.md, `artifacts/m3_sweep_summary.json`). Standard metrics
  only: throughput, mean travel time, σ_v, fuel proxy, wave count/amplitude.

### US-101 validation with honest criteria (M3)

- `us101_replica` validated against the real NGSIM US-101 recording with
  FHWA-style acceptance criteria (GEH, segment-speed RMSPE, wave speed).
  Headline stated honestly: imposing the measured downstream boundary
  improves RMSPE from 72.8% to 36.6% and produces backward-propagating
  waves in all 20 replicates, but the replica **fails** the acceptance
  criteria for documented structural reasons (640 m site, missing on-ramp
  merge, IDM discharge behavior) — see docs/M3_US101_VALIDATION.md. No
  validated-corridor claim is made.
- End-to-end auto-report generation (`validation.report`), which refuses
  macro-only run sets.

### Product layer (M4)

- FastAPI service + RQ/Redis job queue + SQLite (WAL) metadata store
  (`api`): scenarios, runs, sweeps, calibrations, reports — no endpoint
  executes a simulation synchronously; every response carries
  `config_hash`. Single API-key auth.
- Mission-control dashboard (React/Vite) served single-origin by the API;
  one-command Docker deploy (`docker compose up -d --build`) with the same
  image for API and workers.

### Hardening and release (M5, this release)

- Load test: 10 concurrent macro sweep jobs plus a micro (SUMO) sweep
  through the full Docker stack with 2 workers — 42/42 runs done, zero
  failed, all 42 metrics endpoints HTTP 200, `/healthz` p95 under load
  ≤ 11.9 ms against a 500 ms budget. The run's own JSON output is committed
  as `artifacts/m5_load_test.json`; machine spec, per-phase tables and
  provenance for the numbers the JSON does not carry are in
  docs/M5_LOAD_TEST.md (`scripts/m5_load_test.py`).
- `pyarrow` constrained to `>=18,!=24.0.0` across the workspace. Pinning to
  24.0.0 (the version matching libsumo 1.27.1's bundled libarrow, which
  silences libsumo's import-time mismatch warning) was tried and **rejected
  with evidence**: on macOS the two identically-named `libarrow.2400.dylib`
  copies interact fatally — parquet writes after `import libsumo`
  intermittently livelock (hard spin in mimalloc's
  `mi_bitmap_clear_once_set`; reproduced in `tests/test_microsim`, which
  hung >50 min, and bisected to the `pyarrow.Table.from_pandas`/write in
  `microsim.runner`; the same file passes in 3.3 s on pyarrow 25.0.1).
  The cosmetic version-mismatch warning therefore stays; the real
  filesystem-factory-registry clash remains handled by the parquet-path
  shim in `tests/test_microsim/conftest.py` (verified still required on
  25.0.1), and production code writes parquet through open file objects and
  is unaffected on either version.
- Version 2.0.0 across the workspace (root + all packages).

### Retired from v1 (CLAUDE.md §12 — do not reintroduce)

- Phantom-jam formation/dissipation claims from LWR; the ">15% AV
  penetration → human gap exploitation" artifact; the non-standard
  "Efficient Frontier score" as a headline metric; consumer nav-app
  advisory-push features (Waze CIFS cannot carry speed advisories; Google
  Routes speed data is read-only); per-step unseeded compliance coin-flips;
  unseeded `np.random` calls; the v1 Flask endpoints.

## [1.0-legacy] — superseded

FlowState v1 — controlled dissipation of *seeded* shocks in a first-order
LWR model, with a Flask service and the "Efficient Frontier" score. Kept as
motivated preliminary work at
[abc000cool/FlowState](https://github.com/abc000cool/FlowState). Its engine
survives in v2 as the ported, tested `macrosim` screening tier, and its
hard-coded fundamental diagram as the labeled-uncalibrated
`v1_legacy` FD preset (`macrosim.fundamental.v1_legacy_fd`).
